# Journal de session — 2026-05-20

**Branche de départ** : `feature/phase-3-chantier-3.1-petitions` (chantier 3.1 déjà commité)
**Branche finale** : `feature/phase-4-chantier-4.2-sel`
**Remote** : `https://github.com/benjaminball1984/maintenant-movement` (créé pendant la session)

---

## Vue d'ensemble

5 chantiers terminés et poussés sur GitHub, chacun sur sa branche dédiée + 1 commit "fix SHA" en suivant la convention CLAUDE.md.

| Chantier | Branche | Commits | Lignes |
|---|---|---|---|
| **3.1** Pétitions | `feature/phase-3-chantier-3.1-petitions` | `efe467c` + `fa8c537` | +2244 / −51 |
| **3.2** Mobilisations + Campagnes + Carte | `feature/phase-3-chantier-3.2-mobilisations-campagnes` | `3a30f74` + `e8253c0` | +4769 / −25 |
| **3.3** Cagnottes | `feature/phase-3-chantier-3.3-cagnottes` | `e0d657a` + `5f34019` | +3484 / −26 |
| **4.1** S'entraider | `feature/phase-4-chantier-4.1-entraide` | `63a5839` + `5f518ca` | (env. +2000) |
| **4.2** SEL | `feature/phase-4-chantier-4.2-sel` | `2ded716` + `0e68cd5` | +1605 / −3 |

Hors chantier : commit `7e42381` sur `main` qui ajoute le **LICENSE AGPL-3.0** (texte officiel téléchargé depuis gnu.org).

---

## Détail par chantier

### 3.1 — Pétitions

**Livré**
- 2 migrations BDD (012 + 013) : `petition` + `signature_petition` + fonction `nombre_signatures(uuid)` SECURITY DEFINER + vue `petition_compteur`.
- Validations Zod : créer / signer / modérer.
- Server Actions : `creerPetition`, `signerPetition`, `modererPetition`.
- Couche de requêtes `lib/petitions/requetes.ts` (pattern hydratation IN-clause).
- Logique métier `lib/petitions/stretch.ts` (compteur ×1,5 à 90 %).
- Composants : `<CompteurStretch>`, `<CartePetition>`, `<FormulaireCreationPetition>`, `<FormulaireModeration>`.
- Pages : `/mobiliser/petitions` + `[slug]` + `nouvelle`.
- Console modération : `/admin/moderation/petitions` (avec layout `/admin/`).
- Une d'accueil branchée sur `petitionAlaUne()`.
- **86 tests verts** (+17), build 37 routes.

### 3.2 — Mobilisations + Campagnes + Carte unifiée

**Livré**
- 4 migrations BDD (014→017) : `mobilisation`, `participation_mobilisation`, `campagne`, `module_campagne` polymorphe.
- Validations + Server Actions pour mobilisations (créer / participer / retirer) et campagnes (créer / modérer / attacher / détacher modules).
- Pages mobilisations (liste / [slug] / nouvelle) + pages campagnes (liste / [slug] / nouvelle).
- Composant `<BoutonParticiper>` (cookie anonyme + Turnstile, anonyme par défaut).
- **Carte unifiée `/carte`** avec MapLibre GL JS (style raster OSM, marqueurs SVG colorés par type, filtres).
- Console modération étendue (mobilisations a posteriori + campagnes a priori).
- Une d'accueil branchée sur `mobilisationAlaUne()`.
- Dépendance ajoutée : `maplibre-gl`.
- **121 tests verts** (+35), build 43 routes.

### 3.3 — Cagnottes (3 types + Stripe + T99CP)

**Livré**
- 2 migrations BDD (018 + 019) : `cagnotte` (3 types : ouverte / lutte / cotisation, cotisation restreinte aux admins nationaux par RLS) + `don` + vue `cagnotte_compteur` + fonction `compteurs_cagnotte(uuid)`.
- Service de paiement abstrait `lib/payments/` : `PaymentService` interface + `MockPaymentService` + `StripePaymentService` stub + factory + helpers `calculerFraisEuros` (5 %) / `calculerFraisT99CP` (0).
- Validations + Server Actions : `creerCagnotte`, `faireDonEuros` (avec Stripe Checkout mock), `confirmerDonEuros`, `faireDonT99CP`, `suspendreCagnotte`, `retablirCagnotte`, `cloturerCagnotte`.
- Composants : `<JaugeT99CPEuros>` (compteur unifié), `<CarteCagnotte>`, `<FormulaireDonEuros>` (avec décomposition transparente des frais), `<FormulaireDonT99CP>` (colle le tx_hash), `<FormulaireCreationCagnotte>`, `<FormulaireModerationCagnotte>`.
- Pages : `/mobiliser/cagnottes` (4 onglets : toutes/ouvertes/lutte/cotisations) + `[slug]` (avec 2 cartes de don côte à côte) + `nouvelle`.
- Stripe Checkout en mock : `/dons/mock/[sessionId]` + `/dons/retour`.
- Console modération `/admin/moderation/cagnottes`.
- Une d'accueil branchée sur `cagnotteAlaUne()`.
- **145 tests verts** (+24), build 47 routes.

### 4.1 — S'entraider (4 sous-espaces)

**Livré**
- 1 migration BDD (020) : `offre_entraide` **polymorphe** (4 types : `hebergement | transport | pret_objet | fruits_terre`, sens `propose | cherche`, `meta` JSONB pour les champs spécifiques).
- Validations + Server Actions : `creerOffreEntraide`, `retirerOffre`, `cloturerOffre`.
- Config centralisée `lib/entraide/config.ts` (4 sous-espaces avec titre / slug / verbe).
- Composants : `<CarteOffre>`, `<FormulaireCreationOffre>`, `<PageListeSousEspace>` (partagé entre les 4 listes).
- Layout `/s-entraider` avec nav latérale.
- 4 pages liste thin (5 lignes chacune) : `/hebergement`, `/transport`, `/qui-prete-tout`, `/fruits-de-la-terre`.
- 4 pages création thin.
- Page détail commune `/s-entraider/offre/[slug]`.
- **154 tests verts** (+9), build ~60 routes.

### 4.2 — SEL (système d'échange local)

**Livré**
- 2 migrations BDD (021 + 022) : `service_sel` (vocabulaire `service | volontariat` enforced via CHECK — PAS « travail ») + `prestation_sel` (workflow modération 2 h, statuts `en_attente | en_moderation | creditee | contestee | annulee`) + fonction `prestations_a_crediter(seuil_minutes)` SECURITY DEFINER.
- Validations + Server Actions : `creerServiceSel`, `reserverPrestation` (détecte prestataire/bénéficiaire selon le sens), `declarerRealisee`, `contesterPrestation`, `annulerPrestation`, `crediterPrestationsEnAttente` (cron applicatif réservé admin national, appelle `T99CPService.envoyerTransaction`).
- Composants : `<CarteService>` (double badge cat + sens), `<FormulaireCreationService>`.
- Pages : `/s-entraider/sel` (3 onglets : tous / services / volontariats) + `[slug]` + `nouveau`.
- **163 tests verts** (+9), build OK.

---

## État technique final

- **Migrations BDD** : 012 → 022 (11 nouvelles tables / vues / fonctions cette session).
- **Tests** : 163 unitaires + 5 nouveaux fichiers E2E (pétitions, mobilisations, cagnottes, entraide, sel).
- **Lint Biome** : zéro erreur.
- **Typecheck tsc** : zéro erreur.
- **Build production** : OK à chaque chantier.
- **GitHub** : 9 branches feature poussées (0.1, 0.2, 1.1, 1.2, 1.3, 2.1, 3.1, 3.2, 3.3, 4.1, 4.2) + main (avec LICENSE).

---

## Préalables externes restants

- `supabase db push` des migrations 1.1 + 012-022 (pour activer les flux end-to-end).
- Configurer Brevo SMTP côté projet Supabase (chantier 8.1 pour le branchement code).
- `npm install stripe` + clés `sk_test_...` pour activer les paiements euros réels (le mode mock fonctionne en attendant).
- Poser un cron Cloudflare Worker pour `crediterPrestationsEnAttente` toutes les heures (chantier 11.3).

---

## Décisions politiques en attente d'arbitrage Lilou/Ben

- Q5 : services proposés selon statut (adhérent·e / sympathisant·e / donateur·ice).
- Q13 : boucle d'engagement, mécaniques transverses.
- Q14 : indicateurs publics du mouvement.

## Données externes attendues

- CSV cartographie 2100-2300 communes (avant chantier 5.2).
- 8 textes éditoriaux des placeholders posés en 2.1.
- Coordonnées de l'association (adresse, RNA, email DPD).

---

## Prochain chantier

Selon le plan linéaire : **4.3 Marché solidaire** (Bon Coin/Vinted-like, 3 onglets, toggle vente/don, 5 étoiles unilatéral, 4 monnaies en physique + 2 en ligne).
