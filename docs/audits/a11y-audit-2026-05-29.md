# Audit d'accessibilité exhaustif (WCAG 2.1 AA) — 2026-05-29

> Audit de TOUT le site (156 pages + 7 layouts + 134 composants), avec exigence
> forte : utilisabilité au lecteur d'écran + clavier seul par un·e **admin /
> membre malvoyant·e** (toute la console admin, tous les dashboards, tous les
> backends). Revue de code, corrections sans nouvelle dépendance, sans
> régression (lint/typecheck/tests verts après chaque lot).
>
> Méthode : revue manuelle des fondations + balayage par 10 sous-agents
> spécialisés. Chaque constat vérifié dans le code avant correction.

Légende statut : `[ ]` à faire · `[x]` corrigé · `[~]` partiel · `[-]` écarté (faux positif / déjà conforme, justifié).

---

## LOT 1 — Fondations (layouts) : lien d'évitement + landmarks ✅ FAIT (commit a11y lot 1)

- [x] **Lien d'évitement** ajouté. Nouveau composant `components/layout/LienEvitement.tsx` (Server Component, libellé CMS `a11y.lien_evitement`), câblé dans `app/(public)/layout.tsx`, `app/(membre)/profil/layout.tsx`, `app/admin/layout.tsx`, avec `id="contenu"` + `tabIndex={-1}` sur chaque `<main>`. (Layout `(auth)` : pas de nav à éviter, skip-link non requis.)

## LOT 2 — Éditeur rich text TipTap (BLOQUANT admin malvoyant·e) ✅ FAIT (commit a11y lot 1)

- [x] `EditeurRiche.tsx` + `EditeurRicheAvecToolbar.tsx` — `role="textbox"` + `aria-label` (prop `labelA11y`, défaut « Zone d'édition de texte riche ») + `aria-multiline="true"`.
- [x] `BarreOutilsRiche.tsx` `ajouterImage` — demande désormais un texte alternatif (vide = décoratif).
- [x] `BarreOutilsRiche.tsx` bouton « Code » — `aria-pressed` aligné sur `toggleCode` (inline) seul.
- [x] `labelA11y` parlant branché sur les 6 usages : journal (article), décider (OJ + PV), campagne (présentation), CMS inline + ContenuEditableAdmin (`Édition riche de {clé}`), bio profil. Textarea Markdown de `ContenuEditableAdmin` reçoit `aria-label`.

## LOT 3 — Champs recherche/filtre sans label (BLOQUANT admin malvoyant·e) ✅ FAIT (commit a11y lot 2)

Tous les champs des barres de filtres (9 pages admin national + communes publiques + recherche réseau) ont reçu un `aria-label` contextuel. Vérifié typecheck + lint verts.

Ajouter `<label sr-only htmlFor>` ou `aria-label` à chaque champ :
- [ ] `app/admin/national/personnes/page.tsx:130-147` (recherche + select statut)
- [ ] `app/admin/national/communes/page.tsx:142-167` (recherche + dep + statut)
- [ ] `app/admin/national/federations/page.tsx:93-110`
- [ ] `app/admin/national/sondages/page.tsx:106-134`
- [ ] `app/admin/national/moments/page.tsx:112-140`
- [ ] `app/admin/national/medias/page.tsx:107-135`
- [ ] `app/admin/national/campagnes/page.tsx:87-104`
- [ ] `app/admin/national/groupes-entraide/page.tsx:92-109`
- [ ] `app/admin/national/reservations/page.tsx:116-144`
- [ ] `app/(public)/agir/communes/page.tsx:101-107` (recherche communes)
- [ ] `app/(public)/s-informer/reseau/recherche/page.tsx:123-132`

## LOT 4 — Actions de modération sans nom d'objet + objets sans titre (BLOQUANT admin malvoyant·e) ✅ FAIT (commit a11y lot 4)

Prop optionnelle `libelleObjet` ajoutée à `ControleModeration`, `FormulaireModeration`, `FormulaireModerationCampagne`, `FormulaireModerationCagnotte`, `LigneEditionAdmin` → injectée dans l'`aria-label` des boutons. Répercutée depuis 11 pages appelantes (titres pétition/campagne/cagnotte/sondage/SEL/moment/produit/média/édition ; aperçu de texte pour réseau ; nom pour autres-moyens). Textareas motif reliés (aria-describedby + aria-invalid). Gestion du focus après action : NON traitée (reportée, risque). Files SEL « Prestations » / marché « Notations » : affichage seul sans bouton, rien à câbler.

- [ ] `components/admin/moderation/ControleModeration.tsx:150-157` — boutons d'action sans l'objet. Prop `libelleObjet` → `aria-label` (« Retirer : {titre} »). Propager depuis les pages appelantes.
- [ ] `components/petitions/FormulaireModeration.tsx:151-155` — idem (titre pétition).
- [ ] `components/campagnes/FormulaireModerationCampagne.tsx:141-145` — idem.
- [ ] `components/cagnottes/FormulaireModerationCagnotte.tsx:131,135` — idem.
- [ ] `app/admin/moderation/sel/page.tsx:64` — joindre `service_sel.titre` (au lieu de `Prestation {id court}`).
- [ ] `app/admin/moderation/marche/page.tsx:64-66` — joindre titre produit ; `★` → texte.
- [ ] `app/admin/national/journal/LigneEditionAdmin.tsx:26-40` — boutons Publier/Brouillon/Archiver sans n° d'édition. `aria-label` contextualisé.
- [ ] `app/admin/national/droits/page.tsx:95` (`BoutonRetirerDroit`) — nommer le·la bénéficiaire.
- [ ] Gestion du **focus** après remplacement boutons→Alert : `ControleModeration:81-87`, `FormulaireModeration:128-137`, `FormulaireModerationCampagne:118-127` (déplacer focus sur l'Alert, `tabIndex=-1`+`focus()`).

## LOT 5 — Erreurs de formulaire reliées (`aria-invalid` + `aria-describedby`) ✅ FAIT (commit a11y lot 3)

Tous les champs des formulaires auth (inscription, connexion mdp, magic link, reset, nouveau mdp), profil informations (+ ajout `aria-invalid` qui manquait), suppression compte, et modale signature ont leurs erreurs reliées. Wrappers `Input`/`Textarea`/`ChampMotDePasse` forwardent bien les props. Reste à traiter (textareas motif modération) : voir LOT 4.

- [ ] `app/(auth)/inscription/FormulaireInscription.tsx` (tous les champs)
- [ ] `app/(auth)/connexion/FormulaireConnexionMdp.tsx`, `FormulaireMagicLink.tsx`
- [ ] `app/(auth)/mot-de-passe-oublie/FormulaireDemandeReset.tsx`
- [ ] `app/(auth)/reinitialiser-mot-de-passe/FormulaireNouveauMotDePasse.tsx`
- [ ] `app/(membre)/profil/informations/FormulaireInformations.tsx` (manque même `aria-invalid`)
- [ ] `app/(membre)/profil/confidentialite/SectionSuppression.tsx`
- [ ] `components/modales/ModaleSignaturePetition.tsx:240+` (champs)
- [ ] textareas motif : `FormulaireModeration`, `FormulaireModerationCampagne`, `FormulaireModerationCagnotte`
- [ ] `components/petitions/FormulaireEditionPetition.tsx`, `components/sondages/FormulaireVote.tsx`, `components/marche/FormulaireNotation.tsx` (vérifier)

## LOT 6 — Statuts dynamiques annoncés (`aria-live` / régions live persistantes)

- [ ] Stratégie `Alert` : pour les **succès** montés dynamiquement, garantir une région live persistante (`role="status"`/`aria-live="polite"` toujours présente) ou ajouter `aria-live`. Concerne : connexion, inscription, informations, signatures, `FormulaireAccorderDroit`, `FormulaireInitierReversement`, `EditeurInlineCMS`, `ContenuEditableAdmin`, `FormulaireCreerSalle`, `FormulaireCreerEdition`, modales.
- [ ] Actions optimistes réseau muettes : `BoutonSuivre` (+`aria-pressed`), `CartePost` (soutien/commentaire/suppression), `FilConversation`, `BoutonParticiper`, réservations (succès), `notifications-recues` (tout marquer lu).
- [ ] Uploads sans annonce d'état : `components/ui/ChampDocument.tsx`, `TeleverseurImage.tsx` (aria-live + aria-busy).
- [ ] Comboboxes admin sans annonce de résultats : `FormulaireAccorderDroit` (recherche bénéficiaire + commune), `RechercheBeneficiaire`/`RechercheCommune` (`aria-live` du nombre de résultats, idéalement pattern combobox).

## LOT 7 — Libellés humains au lieu d'enum bruts ✅ FAIT (commit a11y lot 3)

Toutes les valeurs d'enum brutes affichées (réservations, trésorerie, sondages, moments, médias, décider, mes-créations, signatures) passent par un libellé humain avec fallback `?? valeurBrute`. Les fonctions de couleur (`variantStatut`) intactes.

- [ ] `app/admin/national/reservations/page.tsx:173` (`STATUTS.label`), `tresorerie/page.tsx:135` (statut caisse), `sondages:169`, `moments:180/182`, `medias:182/184`, `decider:103/104` (espaceType/visibilité), `reservations:174` (offreType), `mes-creations/page.tsx:159` (statut), `ListeMesSignatures.tsx:79` (statut pétition).

## LOT 8 — Tableaux de données sémantiques (backend)

- [ ] `app/admin/national/tresorerie/[caisseId]/page.tsx` — flux financiers en cartes : ajouter intitulés en clair manquants (montant, statut, canal) ; idéalement `<table>` + `<th scope>`.
- [ ] `app/admin/national/personnes/page.tsx:165-224` — email/id nus dans `<code>` sans intitulé ; ajouter libellés sr-only.
- [ ] `app/admin/page.tsx:174-181` — 26 cartes de stats en `<p>`/`<p>` : relier via `<dl>`/`<dt>`/`<dd>` ou `aria-label`.

## LOT 9 — Divers BLOQUANT / IMPORTANT ponctuels ~ PARTIEL (commit a11y lot 4)

Fait : `<Link><Button>` imbriqués corrigés (depuis-petition, pres-de-chez-moi) ; saut h1→h3 home corrigé (Une* en niveau 2) ; media [slug] alt redondant→vide + `<track>` vide retiré + ExternalLink aria-hidden ; BadgesMonnaies aide en aria-label ; OAuth groupes role=group+aria-labelledby ; logo décoratif auth alt vide.
Fait aussi (commit a11y lot 5, V2.5.61) : bug scroll modale signature corrigé + état « merci » en région live.
RESTE (reporté, plus risqué/volumineux) : QR code 2FA (présenter la clé en texte) ; HeaderProfilMenu pattern menu clavier ; CartePost (window.confirm + aria-expanded). Labels OJ/PV/bio en mode Riche : atténué (l'éditeur a déjà un nom via labelA11y).

## RESTE À FAIRE (lots non démarrés)

- **LOT 6 — `aria-live` sur statuts dynamiques** ✅ FAIT (commit a11y lot 6) : régions live persistantes masquées (`<span class="sr-only" aria-live="polite" aria-atomic>`) sur ~23 composants : BoutonSuivre (+aria-pressed), CartePost (+aria-expanded commentaires, aria-label soutien), FilConversation (+émetteur sr-only), ComposerPost, ModaleMessage, 5 composants réservation, BoutonParticiper(+Moment), notifications-recues (marqueur « Non lue » sr-only), ChampDocument + TeleverseurImage (+aria-busy), FormulaireAccorderDroit (comptage résultats combobox + succès), FormulaireInitierReversement (+labels champs manquants + aria-describedby bouton désactivé), EditeurInlineCMS, ContenuEditableAdmin, FormulaireCreerSalle, FormulaireCreerEdition, InvitationInterne, BoutonArchiverEntite. Pas de `role=status/alert` sur div (refusé par biome) : `aria-live` only.
- **LOT 8 — Tableaux de données sémantiques** : trésorerie caisse + personnes admin en `<table>`/`<th scope>` (ou intitulés sr-only) ; stats admin en `<dl>`. Plus gros refactor structurel.
- **Résidus LOT 9** : QR 2FA, HeaderProfilMenu (menu clavier), CartePost (confirm + aria-expanded).
- **Résidus LOT 10** : flèches `←` portées par valeurs CMS (retirer du défaut) ; `<dl>` sans `<dt>` ; emojis décoratifs (👋, ✓) ; Avatar double nom.

Détail initial :

- [ ] `app/(public)/agir/depuis-petition/page.tsx` (57-101) + `agir/communes/pres-de-chez-moi/page.tsx` (130-162) — `<Link><Button>` imbriqués (`<a><button>` invalide). Styliser le `<Link>` directement.
- [ ] `app/(membre)/profil/securite/2fa/FormulaireEnrollementTotp.tsx:85-101` — clé secrète manuelle accessible par défaut (pas repliée), `role="img"`+alt sur le QR, état chargement `role="status"`.
- [ ] `components/modales/ModaleSignaturePetition.tsx:155-167` — **bug** : `dialog` n'émet pas d'event `show` → scroll de fond jamais bloqué. Bloquer le scroll dans `ouvrir()`. + état « merci » (214-238) en `role="status"` + focus.
- [ ] `components/layout/HeaderProfilMenu.tsx:64-114` — `role="menu"` sans nav clavier flèches/focus/Escape→retour ; soit pattern menu complet, soit downgrader en liste de liens. + `aria-controls`/`id`.
- [ ] `components/reseau/CartePost.tsx:85-86` — `window.confirm` suppression → confirmation accessible. + `aria-expanded` sur toggle commentaires (152-160).
- [ ] `app/(public)/page.tsx` (Une*) — saut h1→h3 : `UnePetition/Mobilisation/Cagnotte/Section` passer le titre en `niveau={2}` `apparenceComme={3}`.
- [ ] `app/(public)/s-informer/media/[slug]/page.tsx:186-190` — `alt={media.titre}` redondant → `alt=""`. + `:209-214` `<track>` vide à retirer. + `:169` `ExternalLink` `aria-hidden`.
- [ ] `components/contenu/ContenuEditableAdmin.tsx:176-182` — textarea Markdown sans nom accessible (`aria-label`). + succès non annoncé (84-90).
- [ ] `components/admin/BoutonSupprimerEntite.tsx:95-103` — champ confirmation sans label ; emoji ⚠️ → texte « Attention : » ; `role="alertdialog"`.
- [ ] `components/marche/BadgesMonnaies.tsx:54` — aide via `title` → `aria-label`/sr-only.
- [ ] Labels « bio »/OJ/PV qui pointent dans le vide en mode Riche : `FormulaireInformations:236`, `FormulaireMajEdition:143`, `FormulaireMajReunion:161-235` (associer le label à l'éditeur riche).
- [ ] `app/(auth)/connexion/BoutonsOAuth.tsx:51,70` — groupes de boutons sans `role="group"`+`aria-labelledby`.
- [ ] `app/(auth)/layout.tsx:48-55` — grand logo décoratif redondant → `alt=""`.

## LOT 10 — MINEUR exhaustif (icônes décoratives, flèches, `<dl>`) ~ PARTIEL (commit a11y lot 2)

Fait : `aria-hidden="true"` sur les icônes lucide décoratives des cartes (CarteBoutique, CarteMinimarche, CarteProduit, CarteMomentSolidaire, CarteCommune, CarteMobilisation, CarteOffre) et des pages détail (mobilisation, moments-solidaires, communes, sel, marché produits/boutiques/minimarches) ; flèches `↓ → ` admin enveloppées `aria-hidden`. Reste : flèches `←` portées par des valeurs CMS (`FALLBACKS.retour`) — à retirer du défaut CMS et ajouter en span séparé (chantier ultérieur) ; `<dl>` sans `<dt>` ; Une* home ; emojis décoratifs ; Avatar double nom.

Détail initial :

- [ ] Icônes lucide décoratives sans `aria-hidden="true"` : `components/marche/CarteBoutique.tsx` (62,67,73), `CarteMinimarche.tsx` (61,67), `components/moments/CarteMomentSolidaire.tsx` (49,53,57), `components/communes/CarteCommune.tsx` (38,43), `components/mobilisations/CarteMobilisation.tsx` (43,47,51), `components/marche/CarteProduit.tsx:99`, `components/entraide/CarteOffre.tsx` (105,132,158), pages détail mobilisation (248,265), moments-solidaires/[slug] (199,209,227,228,242,243), communes/[slug] (209,227), sel/[slug] (221,260), marche/produits/[slug] (203,218,223), marche/boutiques/[slug] (129,135), marche/minimarches/[slug] (117,136).
- [ ] Flèches `← → ↓` dans le texte des liens → `<span aria-hidden>` : Une* (home), liens « retour » des pages liste/nouvelle (mobiliser, s-entraider), `agir/communes/[…]`, exports CSV admin (communes:116, federations:84, sondages:96, moments:102, medias:97, personnes:117), `admin/page.tsx` (48,57), liens « Droits → » personnes:217, campagne module:271, tresorerie:72, audit transition.
- [ ] `<dl>` avec `<dd>` sans `<dt>` : `CarteCommune`, `CarteMomentSolidaire`, `CarteBoutique`, `CarteMinimarche`, `CarteMobilisation`, `marche/boutiques/[slug]`. Ajouter `<dt>` sr-only ou passer en `<div>`/`<p>`.
- [ ] Badges/compteurs numériques nus sans libellé : `reseau/messages` (non lus), `reseau/page` (non lus), `reseau/[numero]` cover `role="img"` superflu.
- [ ] Emojis décoratifs lus : 👋 dashboard, ✓ « tu participes », séparateurs `·`.
- [ ] `components/ui/Avatar.tsx:50-54` — double nom (wrapper `aria-label` + img `alt`).

---

## Points déjà conformes (à préserver, ne pas casser)

- `lang="fr"` racine ; `:focus-visible` anneau global ; `prefers-reduced-motion`.
- `IconButton` exige `aria-label` (typé) ; `Label` exige `htmlFor` ; `Input` gère `aria-invalid`.
- `BarreOutilsRiche` : boutons `aria-label`+`aria-pressed` (sauf « Code »).
- `NavOnglets` profil : `<nav aria-label>` + `aria-current="page"`.
- Cartes (marqueurs) : `<button aria-label>` ; carte `role="region"`.
- `<progress>` natif + `aria-label` : `JaugeT99CPEuros`, `CompteurStretch`.
- `<dialog>` natif (focus trap + Escape) : `ModaleMessage`, `InvitationInterne`, `BoutonAttacherACampagne`.
- `FormulairePosterMessage` : modèle de référence (label sr-only, compteur aria-live, erreur role=alert).
- `Pagination`, `Skeleton`, `Alert` (rôles), `emails-preview` iframe titrée+sandbox.
