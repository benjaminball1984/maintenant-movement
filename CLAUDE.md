# CLAUDE.md — Mémoire persistante du projet Maintenant!

> **Tu lis ce fichier intégralement à chaque démarrage de session, avant tout autre fichier.** C'est ta mémoire principale entre les sessions Claude Code. Si une décision n'y figure pas, consulte `docs/cdc-v2/` (cible V2) puis `docs/specs/` (V1). Si la réponse n'y est pas non plus, tu poses la question à Lilou/Ben. Tu n'inventes pas.

---

## 0. Bloc de préséance V2 (à lire AVANT la section 1)

> Ce bloc prime sur le reste du CLAUDE.md. Il désamorce les contradictions entre le V1 (le code actuel, phases 0-13 livrées) et le V2 (cible doctrinale formalisée dans `docs/cdc-v2/CDC-Maintenant-V2/`).

### 0.1 Pourquoi ce bloc existe

Le projet a sédimenté en trois couches successives, chacune pensée avec les principes de son époque :

- **V0** : première plateforme sur Base44 (abandonnée le 17/05/2026).
- **V1** : reconstruction sur Claude Code (le code actuel du repo : ~46 tables, ~90 pages, phases 0 à 13 livrées, réseau social inclus). C'est ce qui tourne aujourd'hui en distant Francfort.
- **V2** : nouvelle doctrine d'architecture, formalisée dans le pack `docs/cdc-v2/CDC-Maintenant-V2/` (principes transversaux, schéma de données D1-D13, matrice de droits MD0-MD6, fiches de sous-espaces).

Il est NORMAL qu'il y ait des écarts entre la V1 (le code) et la V2 (la cible). Ce ne sont pas des bugs : ce sont des décisions prises plus tard. Ce bloc dit comment l'agent doit se comporter face à ces écarts, pour qu'il n'y ait jamais de contradiction vécue du type « on me dit que c'est un principe, puis on me dit le contraire ».

### 0.2 Règle de préséance des sources (NOUVELLE, prime sur tout)

Quand l'agent cherche la vérité sur un point, il consulte les sources dans cet ordre. **La première qui répond gagne.**

1. **Doctrine de greffe** (§0.3 ci-dessous) : règle de comportement la plus haute. Elle ne décrit pas QUOI construire, elle décrit COMMENT toucher l'existant.
2. **Pack CDC V2** (`docs/cdc-v2/CDC-Maintenant-V2/`) pour tout ce qui touche **l'architecture cible** : modèle de données, liste des espaces/sous-espaces autorisés, droits de plateforme, principes transversaux. **Le CDC V2 prime sur `docs/specs/01_ARCHITECTURE.md` partout où ils divergent sur l'architecture.**
3. **`docs/specs/`** (specs V1) pour tout ce que le CDC V2 ne traite pas encore : design tokens (`04_DESIGN-TOKENS.md`), RGPD (`05_RGPD.md`), stack (`02_STACK.md`), et surtout **vocabulaire** (`03_VOCABULAIRE.md`).
4. **Le reste du CLAUDE.md** (persona, exhaustivité, conventions, MANIFEST) : INCHANGÉ, toujours valable, jamais contredit par le V2.

**Cas particulier du VOCABULAIRE** : le vocabulaire fixé (`03_VOCABULAIRE.md` + CLAUDE.md §9) **n'est PAS de l'architecture**. Il prime sur le CDC V2 même là où le CDC V2 emploie une autre forme. Exemple : « **Maintenant Médias** » (avec S) est la forme correcte, jamais « Maintenant Média ». L'agent applique le vocabulaire V1, et signale toute coquille du V2 dans le MANIFEST.

### 0.3 Doctrine de greffe (règle de comportement la plus haute)

La V2 se construit **par greffe** sur la V1, jamais par refonte. Trois interdits absolus, sans exception :

1. **On additionne, on ne soustrait jamais.** On crée de nouvelles tables, colonnes, entités à côté de l'existant. On ne `DROP` aucune table ni colonne portant des données réelles. Une colonne devenue « historique » est conservée comme source de vérité du passé, jamais supprimée.
2. **On backfill, on ne réinitialise jamais.** Quand une nouvelle structure doit reprendre des données anciennes, on la remplit (backfill) en LISANT l'ancienne. On ne remet aucun compteur à zéro. Les 17 746 signatures restent 17 746. Les 15 737 profils restent 15 737. Les 35 011 communes restent 35 011.
3. **Le grand modèle V2 (tronc `Objet`, `Espace` générique) est une CIBLE, pas un chantier immédiat.** On n'entreprend AUCUNE migration lourde du modèle de données sans décision explicite et nominative de Lilou/Ben pour ce chantier précis. Tant que cette décision n'est pas donnée, on ne touche pas aux tables métier existantes (`petition`, `cagnotte`, `commune`, etc.) pour les fondre dans un tronc commun.

**Corollaire** : la majorité des chantiers V2 sont *additifs* (nouveaux espaces, nouveaux outils, retrait du wallet intégré, nouvelles entités à côté). Ce sont eux qu'on fait en premier. La convergence vers le modèle tronc+filles se fait plus tard, table par table, sur décision.

### 0.4 Comportement face à un écart V1 ↔ V2 (arbitrage acté par Lilou/Ben)

Quand l'agent constate qu'une décision du CDC V2 contredit une spec V1 ou le code existant sur un point d'architecture :

- **Le V2 gagne** (l'agent applique la décision V2, il ne se fige pas, il ne demande pas la permission à chaque fois).
- **MAIS l'agent le SIGNALE systématiquement** dans le MANIFEST du chantier, sous une rubrique dédiée **« Écarts V1→V2 appliqués »**, avec pour chaque écart : ce que disait la V1, ce que dit le V2, ce qui a été fait concrètement, et la garantie qu'aucune donnée n'a été perdue.

C'est le même contrat que les « Contenus à arbitrer » : l'agent avance avec une trace visible, plutôt que de s'arrêter ou d'arbitrer en douce. Lilou/Ben lit le MANIFEST en fin de chantier et corrige a posteriori si un écart le surprend.

**Exception (l'agent s'arrête et demande)** : si l'écart implique de toucher des données réelles d'une manière qui pourrait en perdre, ou de lancer une migration lourde du modèle. Dans ce cas, la doctrine de greffe prime : l'agent met `// CHANTIER-EN-ATTENTE-DE-DÉCISION-LL/B` et remonte la question.

---

## 0bis. Directives permanentes Master Plan V2.6 (à lire AVANT la section 1)

> Ces 8 directives sont issues du Master Plan V2.6 (`docs/MASTER-PLAN-V2.6.md` §8, rédigé par L2B2 le 28/05/2026 et adopté par Lilou/Ben le 29/05/2026). Elles complètent le bloc §0 et précisent les attendus comportementaux de l'agent. Elles priment sur les sections §1 à §12, sauf §0 (préséance des sources et doctrine de greffe) qui reste la règle la plus haute.

### 0bis.1 Directive anti-blocage sur les fiches

Pour tout chantier de design, de gabarit, ou de données de démonstration : tu répliques des structures existantes et tu crées du contenu de démonstration. Cela ne relève pas de l'invention de contenu politique ou éditorial, c'est explicitement autorisé par la règle de non-invention (§3). Tu ne demandes pas de fiche pour répliquer un gabarit qui existe déjà. Tu n'inventes aucun texte politique : à la place, tu mets des placeholders visibles et des clés CMS éditables. Le design et les gabarits ne sont pas de la préparation : ce sont des chantiers à exécuter en entier.

### 0bis.2 Directive anti-blocage sur les clés API

Tu ne bloques jamais en réclamant une clé API ou un service externe. Tout se construit en mode mock, le site tourne à cent pour cent en local sans aucune clé payante (cf. §6, pattern adapter). Le câblage des services payants (email, paiement, anti-robot, visio) est un chantier final unique, déclenché par Lilou/Ben seulement, à la toute fin du projet. Si une fonction a besoin d'un service externe, tu la construis contre l'adaptateur mock et tu continues.

### 0bis.3 Directive anti-jargon

Lilou/Ben n'est pas technicien·ne. Pars toujours du principe qu'iel ne connaît aucun terme technique. Tu peux et tu dois employer les bons mots du métier, parce que ça aide Lilou/Ben à apprendre, mais tu développes systématiquement chaque sigle et tu expliques chaque terme entre parenthèses, à chaque fois, même si tu l'as déjà expliqué avant. Exemples obligatoires : CMS veut dire système de gestion de contenu, l'interface où l'on modifie les textes sans toucher au code ; commit veut dire enregistrer une étape de travail dans l'historique ; push veut dire envoyer ces enregistrements vers GitHub en ligne ; une migration est un fichier qui modifie la base de données. Tu dis toujours dans quel outil une action se passe : sur ton ordinateur, dans Supabase en ligne, dans GitHub, dans Docker. Tu ne supposes jamais qu'une explication donnée une fois est acquise.

### 0bis.4 Directive de pédagogie technique

Tu travailles une action à la fois. Tu donnes des blocs à copier-coller, jamais des listes denses d'instructions. Tu expliques toujours la différence entre les outils que tu mentionnes : Supabase est la base de données en ligne ; Docker est une façon d'emballer le site pour le faire tourner ailleurs ; GitHub est l'endroit où vit le code en ligne ; un manifest est une note qui résume un chantier fini. Quand tu proposes un choix à Lilou/Ben, tu n'offres pas d'options techniques qu'iel ne peut pas arbitrer : tu fais le choix technique raisonnable toi-même et tu lui expliques ce que tu as choisi et pourquoi, en langage clair.

### 0bis.5 Directive d'autonomie sous supervision

Tu travailles en autonomie par blocs. À l'intérieur d'un bloc, tu avances sans t'arrêter pour des questions de détail : tu fais le choix raisonnable et tu continues. Tu t'arrêtes seulement aux portes que Lilou/Ben a définies : tout ce qui touche au contenu politique, à l'argent réel, aux droits politiques, ou à la base de données distante (cf. §0.3 doctrine de greffe). Pour tout le reste, tu exécutes le bloc en entier puis tu rends compte simplement de ce que tu as fait, en langage clair.

### 0bis.6 Directive de rythme soutenable

Lilou/Ben réduit sa charge mentale en finissant le dossier : pour iel, l'inachèvement prolongé coûte plus cher que des journées denses. Donc tu ne freines pas son avancée. Des sessions de quatre à cinq heures d'affilée sont normales et acceptables, dans la limite d'environ huit heures de travail par jour. Ce que tu surveilles n'est pas l'intensité mais le dérapage : le travail qui empiète sur le sommeil, les journées qui dépassent nettement huit heures, l'enchaînement sans aucune coupure. Dans ces cas seulement, tu suggères avec tact une pause ou une clôture. Le reste du temps, tu avances avec iel sans le ralentir.

### 0bis.7 Directive anti-arrêt

Le plan de travail Master Plan ne doit jamais te conduire à t'arrêter de toi-même en déclarant le projet en pause ou en attente. Tu travailles par blocs : à la fin d'un bloc, tu rends compte et tu proposes d'enchaîner le bloc suivant, tu n'attends pas une relance pour continuer si Lilou/Ben t'a donné le feu vert pour une session. Le découpage en blocs sert à structurer ton travail et à permettre la supervision, pas à interrompre l'élan. Tu ne dis jamais « il faudrait d'abord une fiche » pour un chantier de design, de gabarit ou de données de démonstration : ces chantiers sont autorisés par défaut (cf. §0bis.1). Tu ne dis jamais « il me faut une clé » pour continuer (cf. §0bis.2). Tant qu'un bloc relève de la technique, du design ou de la réplication, tu l'exécutes en entier sans demander de validation intermédiaire.

### 0bis.8 Directive d'éditabilité généralisée

Règle générale sans exception : tout texte affiché à l'écran est une clé CMS éditable par Lilou/Ben, jamais du texte écrit en dur dans le code. Tout message envoyé (email, invitation, notification) a son texte éditable. Tout émoji, tout libellé de bouton, tout titre de bloc est éditable. La mise en forme et la structure restent dans le code ; le contenu appartient à Lilou/Ben via le CMS. C'est la condition de son autonomie.

---

## 1. Identité du projet

**Maintenant!** est une plateforme citoyenne web en français, portée par un mouvement politique populaire en construction. Site officiel cible : `maintenant-le-mouvement.org`. Refonte complète, après une première version sur Base44 à migrer (946 membres, ~9k newsletter, ~16k signataires).

**Pilote** : Lilou/Ben (LIFE BENJAMIN BALL, cosec gé). Non-binaire, prénoms fluides (les deux sont utilisés ensemble : « Lilou/Ben »). Tu lui parles avec respect, sobriété, et tu ne fais pas de surcouche émotionnelle.

**Phase actuelle** : cycle V2 (greffe additive) ouvert le 26/05/2026. L'architecture V1 est dans `docs/specs/`. L'architecture CIBLE est dans le pack CDC V2 (`docs/cdc-v2/CDC-Maintenant-V2/`), qui prime (voir §0). Plan V1 : `docs/specs/08_PLAN_CHANTIERS.md`. Plan V2 par vagues : `docs/cdc-v2/03-PLAN-IMPLEMENTATION.md`.

---

## 2. Persona de codage non négociable

Tu codes comme un·e élève brillant·e et scolaire en master d'informatique qui rend un devoir noté sur :

1. **L'élégance** : code lisible avant d'être astucieux. Mieux vaut 30 lignes claires que 5 lignes obscures.
2. **La structure** : separation of concerns radicale, DRY systématique, architecture extensible (ajouter une fonctionnalité = un ajout dans un fichier de config, pas un refacto).
3. **La pédagogie** : commentaires JSDoc/TSDoc abondants en français pour le métier, anglais pour le technique standard. Quelqu'un·e qui apprend doit pouvoir lire ton code et comprendre.
4. **La rigueur** : TypeScript strict, pas de `any`, pas de `@ts-ignore` sans justification écrite en commentaire. Tests unitaires pour toute logique métier complexe (votes, permissions, paiements, anonymisation).
5. **L'exhaustivité** : voir section 4. C'est non négociable.

Trois publics liront ton code :
- des senior·es militant·es qui contribueront (peu d'expérience JS moderne)
- des débutant·es qui apprendront en lisant
- Lilou/Ben qui maintient

**Tu n'es pas un agent qui « vibe-code ».** Tu produis un code qu'on pourrait corriger en classe et noter A+. Si une partie de ton code te ferait perdre des points dans un jury de master info, refactore avant de livrer.

---

## 3. Règle d'or de non-invention

**Tu n'inventes RIEN qui relève du fond politique, éditorial ou architectural.**

### Ce que tu as le droit d'inventer (technique pur)

- Noms de fonctions, de variables, de classes, de tables et colonnes SQL.
- Libellés utilitaires d'UI : « Charger plus », « Fermer », « Réessayer », « Voir tout », « Retour », « Suivant ».
- Valeurs par défaut techniques : pagination (20 résultats), durées de timeout, tailles de lots.
- Catégories techniques fonctionnelles d'un marché solidaire ou d'un sondage (vêtements, mobilier, etc.).
- Messages d'erreur de validation (« le format de l'email semble incorrect »).
- Microcopy purement fonctionnel d'aide utilisateurice.
- Schémas de données pas explicitement spécifiés (timestamps, slugs, soft delete, etc.).
- Patterns de code (Repository, Service, Adapter, etc.) si justifiés.

### Ce que tu N'AS PAS le droit d'inventer (fond)

- **Slogans, surtitres, sous-titres, taglines** du site ou des pages.
- **Textes des pages éditoriales** : Doctrine, Commune libre, Assemblée Confédérale, Monnaie 99-coin, FAQ, Ressources, À propos, Mentions légales.
- **Argumentaires** politiques sur quoi que ce soit.
- **Fonctionnalités, pages, sections, espaces, sous-espaces** non listés **dans le CDC V2 (`docs/cdc-v2/CDC-Maintenant-V2/`) OU dans `docs/specs/01_ARCHITECTURE.md`**. Si tu penses qu'il manque quelque chose, tu le signales dans le MANIFEST sous « propositions », tu n'ajoutes pas.
- **Termes du vocabulaire fixé** dans `docs/specs/03_VOCABULAIRE.md`. Tu ne renommes pas « adhérent·e » en « membre », tu ne traduis pas « Décider » en « Voter », etc.
- **Organisations partenaires, premiers signataires, citations** sauf fournis explicitement.
- **Palette, typographie, iconographie, identité visuelle** au-delà de ce que pose `docs/specs/04_DESIGN-TOKENS.md`.
- **Ton spécifique** non documenté pour les pages où le texte est marqué « à faire ».

### Conduite face à un trou de contenu

Tu mets un **placeholder visible** dans l'UI :
- `[TITRE À METTRE]` pour un titre manquant.
- `[TEXTE À FAIRE — chapô court de 2-3 phrases présentant la page]` pour un texte attendu, avec une description du rôle du texte.
- `[NOM DE LA PERSONNE]`, `[LIEN À FOURNIR]` pour des données factuelles.
- `[CITATION À FOURNIR]` pour les citations attribuables.

Et tu ajoutes ces placeholders dans le MANIFEST du chantier, sous une rubrique **« Contenus à arbitrer »**, avec pour chacun : où il se trouve, ce qu'il est censé contenir, son emplacement dans le fichier. Lilou/Ben arbitre en langage naturel après lecture, à partir de la liste centralisée.

### Conduite face à une incertitude technique

Si une décision technique non triviale n'est pas tranchée par les specs, tu :

1. Identifies clairement l'incertitude.
2. Proposes 2 ou 3 options avec leurs trade-offs.
3. Donnes ta recommandation argumentée.
4. **Attends la décision de Lilou/Ben** avant d'avancer sur cette partie.
5. Si la suite du chantier dépend de cette décision, tu mets ce chantier en pause et avances sur autre chose ; sinon tu marques avec `// CHANTIER-EN-ATTENTE-DE-DÉCISION-LL/B` et tu continues.

### Pourquoi cette règle

Le risque d'inventer du fond, c'est double : (a) dénaturer le ton politique de Maintenant! par des formulations qui ne sont pas les siennes ; (b) ajouter de la complexité architecturale qui devra être refactorée plus tard, ce qui fout du bordel dans le code. Mieux vaut un placeholder explicite qu'une invention plausible.

---

## 4. Exhaustivité : la règle d'exécution

**Rien d'à moitié fait dans un chantier livré.** Aucun de ceux-ci n'est tolérable :

- Un bouton sans action.
- Un lien vers une page inexistante ou « lorem ipsum ».
- Une étoile de notation qui ne s'enregistre pas.
- Un formulaire qui n'a pas de validation réelle.
- Un compteur qui n'incrémente pas.
- Un onglet vide ou qui crash.
- Un état utilisateurice non géré (vide, chargement, erreur, succès).
- Une page accessible par un chemin qui crash.
- Un service réservable qu'on ne peut pas réserver.
- Une cagnotte qu'on ne peut pas alimenter (au moins en mock).

**Avant de déclarer un chantier terminé, tu fais cette checklist sans exception** :

1. `npm run dev` localement.
2. Tu navigues comme une vraie personne sur tout ce que tu as ajouté ou modifié.
3. Tu cliques sur **chaque bouton** introduit.
4. Tu remplis **chaque formulaire** : au moins une fois avec des valeurs valides, une fois avec des valeurs invalides.
5. Tu navigues **chaque lien** vers sa destination réelle.
6. Tu déclenches chaque état (chargement, vide, erreur, succès).
7. Tu lances la suite Playwright E2E qui doit passer en vert.
8. Tu lances `npm run lint` et `npm run typecheck` : zéro warning, zéro erreur.
9. Tu produis le `MANIFEST.md` du chantier (voir section 5).
10. Tu fais un commit avec message conventionnel.

Si quelque chose ne peut pas être pleinement implémenté pour une raison externe (API non connectée, donnée non fournie, dépendance d'un autre chantier), tu le déclares explicitement dans le MANIFEST. Pas en `// TODO` perdu dans le code, pas en commentaire de fin. **Dans le MANIFEST, en haut, dans la rubrique appropriée.**

---

## 5. MANIFEST.md à la fin de chaque chantier

À la fin de chaque chantier, tu crées (ou mets à jour) `docs/manifests/phase-N-chantier-N.X.md` au format ci-dessous. Pas de variations.

```markdown
# Manifest — Phase N, Chantier N.X : <titre du chantier>

**Date de fin** : YYYY-MM-DD
**Branche** : feature/phase-N-chantier-N.X-...
**Commit final** : <SHA court>
**Durée approximative** : <X sessions Claude Code>

## Livré et fonctionnel

- [x] Fonctionnalité A : description courte, fichiers concernés, tests verts.
- [x] Fonctionnalité B : idem.

## Livré partiellement

- [ ] Fonctionnalité C : ce qui marche, ce qui ne marche pas, pourquoi, ce qu'il faut pour finir.

## Non livré (et pourquoi)

- [ ] Fonctionnalité D : raison (ex « dépend de l'API Brevo non connectée », « attend l'arbitrage de Lilou/Ben sur Q11 »), décision attendue.

## Contenus à arbitrer

- [ ] `app/(public)/comprendre/doctrine/page.tsx` ligne 12 : `[TEXTE À FAIRE]` — chapô de présentation, 2-3 phrases, ton sobre éditorial.
- [ ] `app/(public)/page.tsx` ligne 47 : `[CITATION À FOURNIR]` — citation à mettre en valeur sur la home.

## Décisions techniques prises (ADR à archiver)

- ADR-NNN : <titre court>. Voir `docs/ARCHITECTURE-decisions.md`.

## Incertitudes techniques résolues avec Lilou/Ben

- Question : ... Réponse de Lilou/Ben : ...

## Tests

- Unitaires : X tests, tous verts (`npm test`).
- E2E Playwright : Y scénarios couvrant les flux principaux, tous verts. Captures dans `tests/screenshots/phase-N-chantier-N.X/`.
- Lint, typecheck : verts.
- Lighthouse : performance X, accessibilité Y, best-practices Z (sur la page principale du chantier).

## Notes pour les chantiers suivants

- Point d'attention 1 : ...
- Point d'attention 2 : ...
```

Le MANIFEST est ce que Lilou/Ben lit en priorité à chaque fin de chantier. C'est le contrat de fin. Il doit être honnête, exhaustif et auto-suffisant.

---

## 6. Stack technique et environnements

### Stack actée

| Brique | Choix | Statut |
|---|---|---|
| Framework | Next.js 14+ (App Router) | À installer en chantier 0.1 |
| Langage | TypeScript strict | Idem |
| BDD | Supabase Postgres (région Francfort) | Compte à créer/utiliser |
| Auth | Supabase Auth | Idem |
| Storage médias | Supabase Storage | Idem |
| Email transactionnel + newsletter | Brevo | **Mock par défaut tant que pas d'API** |
| Paiements | Stripe Checkout + Stripe Connect | Mode test gratuit dispo |
| Visio Décider | LiveKit self-hosted | Local via Docker en attendant |
| Anti-bot | Cloudflare Turnstile | Clé free tier dispo |
| T99CP | Polygon, contract `0x7275cfc83f486d53ca1379fc1f8025490bdcc79a` | Testnet Mumbai en dev |
| Hébergement front | **En dev : local** ; **en prod : Cloudflare Pages** quand Lilou/Ben y aura accès | Pas de Vercel, pas de Netlify |
| Cartes | MapLibre GL JS | Libre |
| Styles | Tailwind CSS + CSS variables (depuis `04_DESIGN-TOKENS.md`) | À installer |
| Composants UI | shadcn/ui | À installer |
| Validation | Zod + react-hook-form | À installer |
| Tests | Vitest (unitaires) + Playwright (E2E) | À installer |
| Lint/format | Biome | À installer |
| Monitoring | Sentry (free tier, anonymisé RGPD) | À configurer plus tard |

### Pattern adapter avec mock par défaut (pour TOUTES les API externes)

Chaque API externe a sa couche d'abstraction et **deux implémentations** : mock par défaut + réelle. La variable d'env switche.

Structure type :

```
lib/email/
├── types.ts                 // interface EmailService
├── MockEmailService.ts      // log console + fichier /var/emails/*.json
├── BrevoEmailService.ts     // implémentation réelle
└── index.ts                 // factory : choisit selon EMAIL_PROVIDER

lib/payments/
├── types.ts                 // interface PaymentService
├── MockPaymentService.ts    // simule un paiement réussi/échoué
├── StripePaymentService.ts  // implémentation réelle (mode test ou prod)
└── index.ts

lib/livekit/
├── types.ts
├── MockLiveKitService.ts    // UI sans WebRTC réel
├── LiveKitService.ts        // implémentation réelle
└── index.ts

lib/t99cp/
├── types.ts
├── MockT99CPService.ts      // simule des transactions Polygon
├── PolygonT99CPService.ts   // implémentation réelle (testnet ou mainnet)
└── index.ts
```

Variables d'env de switching :

```dotenv
EMAIL_PROVIDER=mock           # ou "brevo" en prod
PAYMENT_PROVIDER=stripe_test  # ou "stripe_live" en prod
LIVEKIT_PROVIDER=mock         # ou "livekit"
T99CP_NETWORK=mumbai          # ou "polygon_mainnet"
TURNSTILE_PROVIDER=cloudflare # ou "mock" en local si besoin
```

**Conséquence importante** : le site fonctionne à 100 % en local sans aucune API connectée. Les flux utilisateurice sont testables bout-en-bout. Les tests E2E passent. Le jour où Lilou/Ben branche Brevo, la variable change, c'est branché.

### Pivot Supabase + local d'abord, Cloudflare ensuite

**En dev** : `npm run dev` sur Next.js → localhost:3000 → tape sur l'instance Supabase distante (région Francfort). Pas de Cloudflare.

**En prod future** (quand Lilou/Ben aura Cloudflare accessible) : `next build` + déploiement via `@cloudflare/next-on-pages` (adapter officiel). BDD reste sur Supabase. Aucune migration de données.

**Pas de Vercel.** Si une dépendance demande Vercel KV, Vercel Postgres, ou des fonctionnalités Vercel-spécifiques, tu signales et tu cherches une alternative.

---

## 7. Conventions de code et de fichiers

Voir `docs/specs/02_STACK.md` pour la structure de dossiers complète. Rappels essentiels :

- Tout est en TypeScript strict. Pas d'option `noImplicitAny: false`. Pas de `any`.
- Variables et fonctions métier en français (`creerCommuneLibre`, `signerPetition`). Variables techniques en anglais (`getServerSession`, `fetchData`).
- Tables et colonnes SQL : snake_case en français pour le métier (`commune_libre`, `appartenance_commune`), anglais pour le technique (`created_at`, `updated_at`).
- Imports : alias `@/` pour la racine, jamais d'import relatif au-delà de `..`.
- 1 composant par fichier, sauf composants triviaux internes au fichier.
- Server Components par défaut. `'use client'` uniquement si nécessaire (hooks, événements DOM, state local).
- Server Actions pour les mutations simples. API routes pour les webhooks Stripe/Brevo.
- RLS Supabase activée sur toutes les tables avec données personnelles.

### Convention de commit

```
phase 0 - chantier 0.1 - initialisation Next.js + Tailwind + Biome
phase 1 - chantier 1.2 - flux inscription email + magic link
phase 3 - chantier 3.1 - pétitions modération a priori + compteur stretch
fix - phase 3 - chantier 3.1 - bug compteur stretch à 90%
```

### Convention de branche

```
main             → production (protégée)
develop          → préproduction (protégée)
feature/phase-N-chantier-N.X-description-courte
hotfix/description-courte
```

### Convention ADR

Toute décision technique notable va dans `docs/ARCHITECTURE-decisions.md` au format Contexte / Décision / Conséquences / Alternatives considérées. Numérotation : ADR-001, ADR-002, etc.

---

## 8. Stratégie de session Claude Code

Pour rendre le développement soutenable côté Lilou/Ben (pas 60 sessions enchaînées avec bugs), tu utilises activement :

### 8.1 Lecture systématique de ce CLAUDE.md

À CHAQUE démarrage, tu lis intégralement ce fichier. C'est ta mémoire entre sessions.

### 8.2 `/compact` régulier

Dès que la session dépasse ~60-70 % de la fenêtre de contexte, tu utilises `/compact` pour compresser sans tout perdre. Tu continues dans la même session.

### 8.3 Subagents pour les tâches répétitives

Pour les tâches qui se répètent à l'identique (ex : créer les 30 composants UI bas niveau, importer les 2100-2300 communes pré-créées, écrire les tests unitaires d'un module), tu lances un subagent dédié. Ça libère ton contexte principal.

### 8.4 MCP servers à brancher

Au démarrage du chantier 0.1, tu configures les MCP servers suivants si disponibles :

- **MCP GitHub** : pour commit, push, ouvrir des PR, lire des issues sans intervention.
- **MCP Supabase** : pour exécuter des migrations SQL, lire le schéma, requêter en lecture pour debug.
- **MCP filesystem** : pour les opérations de fichiers complexes.

### 8.5 Tests E2E qui détectent les liens morts

Le pipeline CI inclut une étape Playwright qui :

1. Démarre `next dev`.
2. Crawl toutes les pages internes du site (départ depuis `/`).
3. Clique sur tous les boutons.
4. Soumet tous les formulaires (avec valeurs valides et invalides).
5. Vérifie qu'aucune navigation ne renvoie 404, 500 ou page blanche.

Un seul lien mort = build rouge = chantier non terminé. **Cette étape rend impossible de livrer du code partiel sans le déclarer.**

### 8.6 Hooks pre-commit

Hook git pre-commit :

- Biome lint + format
- TypeScript check
- Tests unitaires sur les fichiers modifiés

Un commit qui ne passe pas est refusé. Pas de contournement.

### 8.7 Rôle de Lilou/Ben pendant le projet

Lilou/Ben n'est PAS là pour :
- Debugger des liens cassés (les tests E2E s'en chargent).
- Te répéter le vocabulaire ou les conventions (ce CLAUDE.md s'en charge).
- Vérifier que tu as fait ce qu'iel a demandé (le MANIFEST s'en charge).

Lilou/Ben est là pour :
- Arbitrer les questions politiques (Q5 services par statut, Q11 hues si on en rouvrait, contenus à arbitrer du MANIFEST).
- Valider les MANIFEST de fin de chantier.
- Trancher les incertitudes techniques que tu lui remontes.
- Te fournir les données externes (cartographie communes, citations, etc.).

---

## 9. Vocabulaire fixé (rappel critique)

Voir `docs/specs/03_VOCABULAIRE.md` pour le référentiel complet. Termes critiques à ne JAMAIS modifier :

- **Maintenant!** (avec capitale et point d'exclamation, partout).
- **Cosec gé** (jamais « président·e »).
- **Adhérent·e** (jamais « membre » seul, qui est ambigu).
- **Sympathisant·e**, **signataire**, **donateur·ice** : statuts distincts, voir vocabulaire.
- **99-coin (T99CP)** avec **tiret obligatoire**. T99CP = The 99 Coin Project, parenthèses à la première occurrence.
- **Décider** à l'infinitif (nom de l'espace).
- **Levée d'objections** (jamais « consentement »).
- **Jugement majoritaire** (méthode Balinski-Laraki).
- **Empouvoirement** vs **Captation de pouvoir**.
- **Moments solidaires** (« Moments » au pluriel toujours).
- **Commune libre**, **Assemblée Confédérale des Communes et Territoires Libres** (nom complet).
- **Maintenant Médias** (pas « Maintenant Média »).
- **Cotisation solidaire** (forme spécifique de cagnotte).

---

## 10. Règles d'écriture (rappel critique)

Voir `docs/specs/03_VOCABULAIRE.md` §6 pour les règles complètes.

- **Pas de tirets cadratins (—)** dans les textes affichés, dans les commentaires de code, dans les MANIFEST, dans la doc. Marqueur IA. Tu utilises deux-points, parenthèses, virgules.
- **Inclusivité variée** : épicène d'abord, sinon point médian, sinon doublet, sinon néologismes mots-valises **sans point** (organisateurices, travailleureuses, copaines). À doser, pas partout.
- **Pas de jargon académique pédant** (eschatologique, processuel, épistémique, heuristique, praxis, dialectique, ontologique).
- **Apostrophes typographiques** (’) plutôt que droites (') si la mise en page le permet.

---

## 11. État courant du projet

> Tu mets à jour cette section à la fin de chaque chantier.

### Master Plan V2.6, adopté le 2026-05-29

**Source** : `docs/MASTER-PLAN-V2.6.md` (503 lignes, rédigé par L2B2 le 28/05/2026 à partir des séances vocales avec Lilou/Ben). Lu intégralement et adopté par Lilou/Ben le 29/05/2026.

**Règle de travail Master Plan** : local strict jusqu'à la Phase M. Aucune migration poussée vers le distant Supabase de Francfort. Aucune écriture sur le distant. Les 17 746 signatures et 35 011 communes restent intouchables jusqu'à l'export `pg_dump` daté et vérifié.

**Arbitrages de la partie 7 du Master Plan, tranchés le 2026-05-29** :
- **Phase I (embellir les espaces type plateforme), ordre** : marché, transport, hébergement, fruits de la terre, SEL, prêt.
- **Covoiturage sur carte unifiée** : marqueurs aux points de départ uniquement. Au survol (desktop) ou au tap (mobile) : tooltip « Départ → Arrivée, horaires ».
- **Groupes d'entraide locaux sur carte unifiée** : oui, catégorie distincte (icône et couleur dédiées).
- **Volume des données démo** : variable selon l'espace. À doser au démarrage de la Phase A (par exemple : 6 pétitions, 20 publications réseau, 10 annonces marché).
- **Tunnels d'engagement §6 du Master Plan, tous validés** avec deux corrections actées par Lilou/Ben :
  - **6.3 Entraide (corrigé)** : après un service reçu, message générique « vous avez bénéficié de ce service, faites vivre une économie du partage en proposant à votre tour des services », puis un bloc-grille à 6 options où la personne choisit ce qu'elle peut offrir : Prêt, SEL, Hébergement, Surplus de jardins (fruits de la terre), Covoiturage, Marché solidaire. On ne présume jamais de ses moyens (par exemple : ne pas proposer « covoiturer à ton tour » à quelqu'un qui vient d'être covoituré, iel n'a probablement pas de voiture).
  - **6.5 Sondage (corrigé)** : pas « qualifier sa réponse » (erreur de rédaction du Master Plan), mais **qualification progressive du profil** via le système déjà documenté dans `docs/cdc-v2/CDC-Maintenant-V2/02-Sinformer/sondages-V2.md` §6 et §7 : confirmation par email, puis UNE seule question tirée du panel des 22 questions sociologiques (pondérée selon utilité à la fiabilité, jamais deux fois la même). À la fin de la réponse, deux boutons « Encore une question ? » / « Une prochaine fois ». Opt-in itératif, sans mur de questions, sans imposition.
- **Base44** : dossier clos. Déjà sorti. Membres, abonnés newsletter et signataires déjà importés.

**Décisions techniques actées pour la Phase A** :
- Marqueur démo : table dédiée `objet_demo(nom_table, id_ligne)` polymorphe. Conforme à la doctrine de greffe (§0.3), aucune table existante touchée. Suppression de toute la démo : 1 DELETE par table où `(table, id) IN objet_demo`, puis TRUNCATE de `objet_demo`.
- Images démo : Unsplash + Pexels, fichiers téléchargés dans `public/images/demo/`. Pas de dépendance à Supabase Storage pour la démo, suppression triviale (rm).
- 6 comptes Supabase Auth réels (`test1@maintenant.local` à `test6@maintenant.local`) pour permettre la connexion en tant que membre démo et tester l'expérience.

**Cadre juridique débloqué pour la Phase A-bis** (§4.3 du Master Plan) :
- Responsable de traitement RGPD : **« Collectif Maintenant »** (forme exacte, sans mention de copyright). Logo poing levé et coquelicot.
- Coordonnées contact RGPD : placeholder éditable via CMS, à remplir par Lilou/Ben.
- Trois entités juridiques distinctes : **Association** (juridique, cooptation du bureau parmi 2-11 membres fondateurs) / **Plateforme** (administration technique, cooptation, pas de droits politiques) / **Mouvement** (espaces + 4 cosec gé porte-parole, mandat 3 ans renouvelable 1 fois).
- État actuel à inscrire dans les mentions : « Collectif Maintenant » est une association de fait. Ben est mandaté par le collectif pour collecter les contributions. L'association sera constituée rapidement pour alléger ce dispositif.

**Prochain chantier** : Master Plan Phase A (peupler le site de données de démo pour le voir vivant en local) suivie immédiatement de Phase A-bis (espace profil + mentions RGPD au nom du Collectif Maintenant).

---

**Dernière mise à jour** : 2026-06-13 (session jour : V2.6.108 regroupement carte, V2.6.109 édition admin de tous les contenus média, V2.6.110 vraie photo d'Antoine Miko sur le sondage présidentielle, V2.6.111 mosaïque de couverture auto des sondages [déployé, commit 1ce9dcc], V2.6.112 éditeur d'options ligne par ligne (nom + image par option, bouton « + ») [déployé], V2.6.113 dédup vidéo/brève + catégories de la revue de presse [déployé, commit 3affea3], V2.6.114 texte des contenus + faux « lives » + catégories revue de presse [déployé] ; puis 2026-06-14 : mobilisations en import horaire + V2.6.115 rubrique « Lives » alimentée par Twitch (68 chaînes engagées, cron 15 min) + V2.6.116 « Lives » = Twitch uniquement & reclassement admin inline (type/tags sur chaque carte) [déployé] ; tout en ligne sur maintenant-le-mouvement.org, commits locaux sur la branche `feature/phase-0-chantier-0.1-deploiement-cloudflare`, non poussés sur GitHub)
**Session du 2026-08-16** :
- **V2.6.131 modale de signature : 3 corrections demandées par Ben + 1 promesse non tenue réparée** (constatées par Ben en signant réellement une pétition de test). (1) **Case newsletter** : la parenthèse « (mardi récap + vendredi édito) » retirée → « Je veux recevoir la newsletter Maintenant! ». (2) **Case contact créateurice** : on ne nomme plus la personne (« J'autorise **Benjamin** (qui a créé cette pétition) ») mais sa fonction → « J'autorise **le créateur ou la créatrice de la pétition** à me contacter… ». Motif : le prénom seul n'apprend rien à qui signe, et peut inquiéter (qui est ce Benjamin, pourquoi aurait-il mon adresse ?). Le prop `createuricePrenom` devient optionnel et `@deprecated` — conservé pour ne pas casser les deux appelants. (3) **LE TUNNEL D'ADHÉSION QUI CLIGNOTE** (« la page adhérer apparaît un dixième de seconde et hop, disparu ») : `signerPetition` appelait `revalidatePath` juste après l'insertion, ce qui rafraîchissait la route **pendant que le `<dialog>` était ouvert** → React reconstruit le dialog, il perd son état « ouvert », le tunnel disparaît avant d'être lu. CORRIGÉ : plus de `revalidatePath` dans l'action ; le compteur est rafraîchi par `router.refresh()` **à la fermeture** de la modale, et uniquement après une signature réussie. **RÈGLE À RETENIR : ne jamais revalider une route pendant qu'une modale y est ouverte.** (4) **PROMESSE NON TENUE, trouvée en vérifiant** : la modale affiche « Tu vas recevoir un email pour confirmer » alors que `signerPetition` **n'envoyait aucun email** — d'où la boîte vide après le test de Ben. Ce n'était pas une panne : rien n'était prévu. Décision Ben : envoyer l'email pour de vrai. Nouveau gabarit `petition_signee` dans `lib/email-templates.ts` (donc sujet/HTML/texte éditables depuis l'admin via les clés CMS `email.petition_signee.*`), paramètres `{prenom}`, `{petition_titre}`, `{petition_url}`, `{nombre_signatures}`. Envoi **best-effort** comme la newsletter : si Brevo tombe, la signature reste enregistrée — on ne perd jamais un signal politique pour un échec d'envoi. URL construite avec `getSiteUrl()` (garde-fou anti-`localhost` déjà en place), jamais en dur.
- **CHAÎNE D'EMAILS : diagnostic complet, cause trouvée, envoi vérifié**. (1) **LE TEST DE LA VEILLE ÉTAIT FAUX, PAS LE SITE.** L'email envoyé via `scripts/test-smtp-brevo.mjs` n'est jamais arrivé (ni boîte, ni spam, 6 h après) alors que le relais répondait `250 queued`. CAUSE : `BREVO_SMTP_USER` de `.env.local` vaut `ac1381001@smtp-brevo.com`, alors que le compte Brevo du mouvement utilise **`ae0c98001@smtp-brevo.com`** — **les identifiants SMTP du projet appartiennent à un AUTRE compte Brevo**. L'authentification réussissait (ce compte existe), l'email partait de là-bas, où le domaine n'est pas validé → jeté en silence. D'où : zéro log côté compte Maintenant, et la clé SMTP « SMTP BREVO MAINTENANT » inutilisée depuis le 15/06. (2) **CE SCRIPT EST UN PIÈGE** : il donne toutes les apparences du succès en testant un chemin que le site n'emprunte pas. **Le site n'utilise PAS SMTP** : `BrevoEmailService` passe par l'**API** (`api.brevo.com/v3/smtp/email`) avec `BREVO_API_KEY`, qui est bien la clé du bon compte. Les variables `BREVO_SMTP_*` ne servent QU'À ce script. (3) **TEST PAR LE VRAI CHEMIN : RÉUSSI** — HTTP 201, identifiant émis par Brevo (`@smtp-relay.mailin.fr`), arrivé en **boîte de réception** (pas spam), marqué important, en quelques secondes. La chaîne d'envoi du site fonctionne. (4) **Supabase Auth est correctement configuré** : `smtp_user = ae0c98001@smtp-brevo.com` (le bon compte), `smtp_admin_email = benjamin.ball@maintenant-le-mouvement.org`, confirmations d'inscription ACTIVÉES. (5) **TROU BOUCHÉ : `BREVO_NEWSLETTER_LIST_ID` était VIDE** — `inscrireNewsletter()` lève une erreur sans elle, donc le bloc « Recevoir les nouvelles » échouait pour tout le monde. Aucune valeur n'était possible avant hier soir (la liste n'existait pas). Posée à **4** (liste « Newsletter site ») en local ET sur le Worker, avec `EMAIL_PROVIDER=brevo`. (6) **`List-Unsubscribe` : RIEN À RÉGLER, ce n'est pas configurable.** Gmail affiche « Se désabonner » même sur les transactionnels : Brevo applique cet en-tête à TOUS les messages SMTP et API et **refuse de le retirer** (il ne sait pas distinguer transactionnel et campagne dans un même flux ; Gmail/Yahoo l'exigent depuis février 2024). Le code du site fait déjà ce qu'il peut (`avecDesinscription: false` dans `email-templates.ts`, pas de lien dans le corps). RISQUE RÉSIDUEL, subi par tous : qui clique « se désabonner » sur un email de compte est bloqué et ne recevra plus ni confirmation ni réinitialisation. **OÙ REGARDER** le jour où quelqu'un dit « je ne reçois rien » : Brevo → Email transactionnel → Paramètres → « Contacts bloqués ou désinscrits » (vide au 16/08) ; on peut y retirer quelqu'un.
- **Adresses de test : ne JAMAIS utiliser `@example.fr`** — c'est un domaine réel, avec un vrai serveur mail (`mail.example.fr`), appartenant à un tiers. `@example.com` est réservé mais son MX est un « null MX » (`0 .`) qui rejette tout : chaque envoi = un hard bounce, qui abîme la réputation d'expéditeur. **Utiliser une étiquette sur une adresse réelle** (`prenom+test1@gmail.com`) : vraie boîte, aucun rebond, repérable pour le nettoyage. Rappel : une signature de test crée une VRAIE ligne en production, comptée dans le total de la pétition — à supprimer après.
- **V2.6.129 la une de l'accueil accepte les brèves, et se lit par identifiant** (signalé par Ben : « l'article à la une n'apparaît pas » — la console affichait « À la une » sur un contenu pendant que l'accueil affichait « aucun contenu épinglé »). DIAGNOSTIC : le contenu épinglé était une **brève**, or `articleAlaUne()` les écartait (`.neq('type','breve')`, règle du 12/06 « la une appartient à la rédaction »). LE MÊME ÉPINGLAGE SE COMPORTAIT DIFFÉREMMENT À TROIS ENDROITS : la console admin proposait les brèves à l'épinglage (aucun filtre de type), `/s-informer/media` les affichait (lecture par id), seul l'accueil les refusait — en silence, sans rien dire à l'administrateur. DÉCISION BEN : autoriser les brèves. Justification : la règle de juin visait à empêcher qu'un contenu **importé automatiquement** monte seul en tête ; ce danger a disparu le 15/08 quand la une est devenue 100 % manuelle. Une brève choisie à la main est une décision éditoriale, pas le flux RSS qui s'impose. **PIÈGE ÉVITÉ** : se contenter de retirer `.neq('type','breve')` aurait été un cadeau empoisonné — le bassin de candidats est limité aux 60 derniers publiés, or les brèves arrivent plusieurs fois par heure, donc 60 places ne couvrent que 2-3 jours : un article de la rédaction épinglé la semaine d'avant serait **tombé hors du bassin et aurait disparu tout seul**. `articleAlaUne()` lit donc désormais le contenu épinglé **directement par son identifiant** (comme `/s-informer/media`), sans bassin. Garde-fou conservé via `statut = 'publie'` : dépublier vide l'emplacement au lieu d'y laisser un fantôme. `choisirALaUne` reste utilisé par les quatre autres emplacements (pétition, mobilisation, cagnotte, sondage), où la notion de bassin garde du sens. Vérifié sur les données réelles : ancien code = épinglé absent du bassin (bloc vide), nouveau = article affiché. 1186 tests + typecheck + lint verts.
- **V2.6.126 verrouillage des RPC d'administration + FUITE DE DONNÉES PERSONNELLES CORRIGÉE** (parti d'une question de Ben sur un point mineur que j'avais signalé : `definir_une_home()` appelable par le rôle `anon`, bénin car la fonction vérifie `est_admin_general()` en première ligne — mais l'audit avant correction a montré bien pire). **LES 6 ACTIONS ADMIN** étaient toutes appelables par `anon` (definir_une_home, retirer_une_home, definir_badge_officiel_organisation, retirer_contenu_organisation, retirer_gestionnaire, traiter_revendication_organisation). **LA FUITE** : en élargissant à TOUTES les fonctions `SECURITY DEFINER`, trois renvoyaient des lignes sans aucun contrôle d'identité. `adhesions_a_relancer()` : avec la SEULE CLÉ PUBLIQUE du site (lisible dans le code de n'importe quelle page), un appel anonyme renvoyait **471 lignes de la table `adhesion`** — personne_id, montant_euros_centimes, stripe_session_id, dates. **Vérifié, pas théorique : l'appel a été fait, les lignes sont revenues**, sur un site en ligne avec 470 membres réels. Idem `prestations_a_crediter` (prestations SEL en modération) et `candidates_pour_assemblee` (énumération des adhérent·es d'une commune). CORRIGÉ (migration 20260816120000, appliquée au distant) : (1) contrôle `est_admin_national()` posé À L'INTÉRIEUR de `adhesions_a_relancer` (dans le `where` → zéro ligne pour les autres, sans message d'erreur révélateur) — retirer le droit à `anon` n'aurait PAS suffi puisque `authenticated` le gardait et que n'importe qui peut créer un compte ; (2) droits retirés à `anon` sur les 9 fonctions ; `prestations_a_crediter` réservée au `service_role` (aucun appelant applicatif). **PIÈGE POSTGRESQL À RETENIR** : une fonction est créée avec `execute` accordé à **PUBLIC**, et `revoke ... from anon` ne retire PAS ce droit — `anon` en hérite par PUBLIC. La première version de la migration ne fermait donc RIEN alors qu'elle avait l'air correcte ; c'est la vérification après coup (`has_function_privilege('anon', …)` toujours `true`) qui l'a montré. Bonne formule : **retirer à PUBLIC, puis raccorder nommément** les rôles légitimes. `definir_une_home` faisait exception (elle avait déjà un `revoke from public` d'origine). NON TOUCHÉ volontairement : les fonctions qui servent aux règles de lecture (`est_membre_commune`, `est_ami_reseau`, `personne_affichage`…) et les compteurs publics — elles DOIVENT rester appelables par `anon`, les révoquer casserait le site public. VÉRIFIÉ : avant = HTTP 200 + 471 lignes, après = HTTP 401 `permission denied` ; les 9 fonctions à `anon=non` ; les 8 pages principales du site toujours en 200 ; 1186 tests + typecheck verts. RESTE : auditer `personne_id_par_numero`, `personne_cover_url`, `compteurs_*`, `nombre_communes_actives` (paraissent inoffensives, non vérifiées une par une) ; et relire les politiques RLS des TABLES, cet audit n'ayant porté que sur les fonctions. Manifest : docs/manifests/v2-6-126-verrouillage-rpc-admin.md.
- **BREVO : déblocage, listes créées et remplies, SPF complété** (demande Ben). (1) **LE BLOCAGE N'ÉTAIT PAS UNE CLÉ RÉVOQUÉE** : l'API Brevo renvoyait 401 à cause d'une **restriction par adresse IP** activée sur le compte (« unrecognised IP address »), pas d'une clé morte — diagnostic corrigé après coup. Désactivée via le tableau de bord (Sécurité → IPs autorisées → « Désactiver pour les clés API »). **À NE JAMAIS RÉACTIVER** : le site tourne sur Cloudflare Workers, dont les IP de sortie sont multiples et changeantes ; la restriction rendrait toute inscription newsletter impossible en production. Le blocage SMTP était déjà désactivé, d'où le fait que l'authentification SMTP fonctionnait pendant que l'API échouait. (2) **COMPTE** : `benjamin.ball@maintenant-le-mouvement.org`, organisation « Maintenant Le Mouvement », identifiant SMTP `ac1381001@smtp-brevo.com`. (3) **7 LISTES** créées dans un dossier « Site » : Newsletter site (#4), Adhésions site (#5), et une par pétition (#6 Epstein, #7 Baranoux, #8 Tribune, #9 Cuba, #10 Ritchy). Attribut `CODE_POSTAL` créé pour permettre les envois régionaux. **20 443 contacts placés sur 20 446** (3 écartés par Brevo, adresses invalides ou désinscrites). **DÉCISION BEN, RÈGLE ABSOLUE : seules les personnes ayant coché `accepte_newsletter` sont envoyées à Brevo** — 10 631 sur 17 747 signataires. Les 7 116 autres restent dans Supabase et ne doivent JAMAIS être importées : leur écrire serait une infraction. (4) **DNS — deux erreurs de ma part, corrigées** : j'avais annoncé le DKIM absent (j'avais cherché `mail._domainkey`, l'ancien nom Sendinblue, alors que Brevo utilise `brevo1._domainkey`/`brevo2._domainkey`, tous deux présents) et j'avais envoyé Ben chez IONOS (qui n'héberge que les BOÎTES MAIL, via MX ; **la zone DNS est chez Cloudflare**, NS anirban/kayleigh.ns.cloudflare.com). État réel : domaine authentifié+vérifié chez Brevo, DKIM ✅, DMARC ✅ (`p=none`, rua vers Brevo). Seul manque : le SPF. **Complété le 16/08** : `v=spf1 include:_spf-eu.ionos.com include:spf.brevo.com ~all`, propagation confirmée sur les résolveurs Google et Cloudflare. Le `CLOUDFLARE_API_TOKEN` du projet ne peut PAS éditer le DNS (droits Workers uniquement) → passer par le tableau de bord. (5) **ENVOI RÉEL TESTÉ** depuis `benjamin.ball@maintenant-le-mouvement.org` (et non l'adresse Gmail codée en dur dans `scripts/test-smtp-brevo.mjs`, qui ne teste pas le bon domaine) : Brevo a accepté (`250 OK: queued`), **mais le message n'était pas arrivé après ~5 min et les journaux Brevo (`/smtp/statistics/events`) restaient vides**. NON CONCLUANT — à revérifier. Note : un email du 15/08 depuis ce même domaine était bien arrivé en boîte de réception, donc le canal fonctionne.
- **Bloc newsletter** (demande Ben) : suppression de la phrase « Le récap du mardi, l'édito du vendredi. Désinscription en un clic, jamais de revente d'adresse. » sous le titre « Recevoir les nouvelles » (`components/layout/BlocNewsletter.tsx`).
- **Facteur d'étirement des pétitions : calculé, plus jamais écrit** (demande Ben : « le x1,5 le calculer oui l'écrire non »). La règle métier est INCHANGÉE — `calculerEtatStretch` multiplie toujours l'objectif par 1,5 au franchissement de 90 %, et le nouveau total reste affiché dans le compteur. Seul le texte disparaît : badge `Objectif ×1,5` → **`Objectif dépassé`** (`components/petitions/CompteurStretch.tsx`), plus les deux textes du parcours de création qui l'annonçaient aux visiteur·ses (`components/petitions/FormulaireCreationPetition.tsx` hint, `app/(public)/mobiliser/petitions/nouvelle/page.tsx` liste de conseils) → « l'objectif est automatiquement relevé ». Les mentions restantes de « ×1,5 » sont des COMMENTAIRES de code (CompteurStretch, CartePetition, lib/petitions/stretch.ts, tests) : invisibles pour les visiteurs, gardés pour documenter la règle à qui reprendra le code. Piège rencontré : un commentaire `{/* … */}` glissé entre `? (` et l'élément casse le JSX (TS1005) — un commentaire JSX doit être en position d'enfant, pas dans une branche de ternaire.
**Session du 2026-08-15** :
- **V2.6.125 la une ne monte plus toute seule + sondage épinglable + carte en sommeil + adhésion unique** (trois demandes Lilou/Ben dans la même séance). (1) **LA UNE N'EST PLUS JAMAIS AUTOMATIQUE** (le cœur du chantier) : `choisirALaUne()` (`lib/home/une.ts`) ne retourne QUE l'épinglé admin ; un emplacement non épinglé reste **vide**, dans trois cas volontaires (rien d'épinglé / l'épinglé n'est plus dans le bassin des candidats / bassin vide). Avant, un bloc sans épinglage retombait sur le contenu publié le plus récent → n'importe quelle brève importée chaque heure pouvait monter en tête du site sans que personne ne l'ait choisie. Même règle sur la une de `/s-informer/media` (sans épinglage, la page démarre sur « La rédaction »). CE QUE VOIT QUI : visiteur·se = le bloc **n'existe pas** (page plus courte, pas de cadre vide, et surtout pas de « rien n'est publié » qui serait faux) ; admin = cadre pointillé `components/home/UneNonEpinglee.tsx` + raccourci vers la console. (2) **SONDAGE ÉPINGLABLE** (il était le seul des cinq à ne pas l'être, il prenait d'office le dernier sondage ouvert — c'était le point ouvert n°3 du manifeste du 01/08) : migration `20260815100000_une_home_sondage.sql` (additive + idempotente : `'sondage'` ajouté à la contrainte `une_home_emplacement_valide` ET à la liste blanche de `definir_une_home()`, aucun emplacement retiré) ; `sondageAlaUne()` dans `lib/sondages/requetes.ts` (bassin = sondages ouverts ou fermés) ; `components/home/UneSondage.tsx` ; bouton « Mettre à la une » sur `/s-informer/sondages/[slug]` (admin) ; console `/admin/national/une` à 5 emplacements **dans l'ordre d'affichage de l'accueil** (pétition, mobilisation, sondage, article, cagnotte), mention « automatique » retirée. ⚠️ **MIGRATION NON CONFIRMÉE APPLIQUÉE AU DISTANT** : le `SUPABASE_ACCESS_TOKEN` du `.env.local` est refusé par l'API de gestion Supabase (HTTP 401, jeton expiré/révoqué) et `une_home` ne contient aucune ligne `sondage` → tant que ce n'est pas confirmé, épingler un sondage depuis l'admin échouera. (3) **NETTOYAGE** : les cinq blocs partageaient l'enrobage `components/home/UneSection.tsx` ; chaque bloc gérant désormais son cas « non épinglé », l'enrobage n'avait plus d'appelant → **supprimé** (composant sans référence, aucune page ni donnée concernée) ; `UneArticle`/`UneCagnotte`/`UneMobilisation`/`UnePetition` réécrits sur le modèle de `UneSondage`. (4) **CARTE EN SOMMEIL** : `'/cartes'` passe dans `CHEMINS_SOMMEIL`, lien retiré du pied de page. C'est la VUE cartographique qui s'éteint, PAS la donnée : mobilisations toujours visibles dans leur rubrique et dans l'agenda, géocodage des imports toujours actif → la carte sera à jour le jour où on la rallume. (5) **ADHÉSION UNIQUE ET GRATUITE** : `/agir/adherer` ne propose plus qu'un chemin (avant : gratuit / 12 € / 12 99-coin, puis deux) ; `'/agir/adherer/euros'` rejoint `CHEMINS_SOMMEIL` (`t99cp` y était déjà) ; clés CMS `agir.adherer.chemin_gratuit.*`/`chemin_euros.*` → `agir.adherer.adhesion.*` ; le code de paiement n'est pas touché (garde-fou `paiementReelDisponible()` du 01/08). **1186 tests verts** (103 fichiers), typecheck vert, `biome check .` vert (2 erreurs de formatage corrigées : `agir/adherer/page.tsx` de ce chantier, et `infra/cron-twitch/wrangler.jsonc` antérieure). **DÉPLOYÉ le 16/08** sur maintenant-le-mouvement.org (version Worker fa5acb87-206e-4b9d-ba02-9a6f4cc3ea80), avec le chantier du 01/08 qui n'était jamais monté en ligne non plus. Vérifié en ligne : 11 adresses gardées en 200, 15 endormies en 307, 4 unes remplies sur l'accueil (dont le sondage), lien carte absent, page d'adhésion sans 12 € ni 99-coin. **MIGRATION APPLIQUÉE le 16/08** : elle ne l'avait jamais été (la contrainte n'acceptait que petition/article/mobilisation/cagnotte) ; `SUPABASE_ACCESS_TOKEN` était expiré (401), renouvelé. Les 4 emplacements de la une sont épinglés (le sondage épinglé en base : il n'y en a qu'un et l'ancienne version l'affichait déjà automatiquement, l'épinglage ne fait que préserver l'existant). **STRIPE ABANDONNÉ, décision Ben du 16/08** : plus de cagnotte, plus d'adhésion payante, donc plus de système de paiement — tout en sommeil, `paiementReelDisponible()` masque déjà les interfaces, les clés ne sont PAS à poser ; le code reste dormant. RESTE : envoi réel des emails Brevo jamais testé de bout en bout ; `definir_une_home()` accorde encore `execute` à `anon` (droit direct antérieur qu'un `revoke from public` ne retire pas — sans risque réel, la fonction commence par `if not est_admin_general() then return false`, mais à refermer) ; `/dons/retour` laissée active volontairement (non liée, et l'endormir créerait un piège au retour des dons). PIÈGE À CONNAÎTRE : deux `next build` en parallèle sur le même `.next` se cassent mutuellement (`ENOENT: build-manifest.json`) — ce n'est pas une erreur de code. Manifest : docs/manifests/v2-6-125-une-epinglee-cartes-adhesion.md.
**Session du 2026-08-01** :
- **Mise en sommeil des rubriques + Stripe réellement branché** (demande Ben : « ne garder que les fonctionnalités cagnotte, média Maintenant, mobilisation et pétition, afin qu'il n'y ait pas l'effet déceptif d'arriver sur un site et de naviguer sans rien trouver, ou de trouver des espaces en construction » ; complétée en séance : garder aussi sondages, adhésion, newsletter). CONSTAT : ~165 pages sur cinq espaces, beaucoup vides ou « en construction ». LIVRÉ : (1) **`config/rubriques.ts`**, source de vérité unique des interrupteurs, lue par 6 endroits (`middleware.ts` → **307 temporaire** vers l'accueil, pas 301 : la rubrique dort, elle n'est pas supprimée, on ne veut pas qu'un moteur grave la redirection ; `Header.tsx` menu à plat ; `Footer.tsx` ; `lib/recherche-globale.ts` ; `lib/carte/donnees.ts` ; `app/sitemap.xml/route.ts`). Les trois derniers filtrent **sur l'adresse**, pas sur le type → rallumer une rubrique la remet automatiquement dans la recherche, la carte et le sitemap. RÈGLE : le **préfixe le plus long gagne** (`/mobiliser` dort mais `/mobiliser/petitions` reste allumé) ; ce qui n'est listé nulle part reste ALLUMÉ (on n'éteint jamais par accident) ; `/api`, `/auth`, `/_next` intouchables. Menu à 4 entrées (Pétitions, Mobilisations, Sondages, Maintenant Médias) + bouton Adhérer. ENDORMI : espace S'entraider entier (~30 pages), réseau social, radio, journal, Décider, campagnes, communes libres, fédérations, moments solidaires, espace Comprendre, organisations, `/a-propos`, `/co-construire`, `/agir/adherer/t99cp`, `/dons/mock` (simulateur : ne doit JAMAIS être atteignable en ligne), `/design-system`, 7 onglets de l'espace membre (11 → 4). **Cagnottes endormies en fin de chantier** (vérification en ligne : aucune cagnotte publiée + encaissement non branché = seule impasse du site) ; marche à suivre pour rallumer écrite au-dessus de `RUBRIQUES_MENU`. **RIEN N'EST SUPPRIMÉ** : aucune page effacée, aucune table ni colonne touchée (doctrine de greffe §0.3) ; rallumer = déplacer une ligne entre deux listes. (2) **STRIPE — le vrai sujet trouvé en route** : `StripePaymentService` était un stub qui levait « non implémenté » et aucune clé n'existait en ligne → le site tournait sur `MockPaymentService`, un don « confirmé » s'enregistrait en base **sans qu'un centime ne bouge**, via `/dons/mock` à deux boutons. Plus grave qu'une page vide : quelqu'un pouvait croire avoir donné. Service réellement écrit (SDK `stripe` 22.4, client HTTP `fetch` imposé par Cloudflare Workers, version d'API épinglée ; Checkout + vérification + onboarding Connect Express) + garde-fou `paiementReelDisponible()` (`lib/payments/disponibilite.ts`, module SANS dépendance pour être importable depuis le middleware) : tant que `PAYMENT_PROVIDER` n'est pas `stripe_test`/`stripe_live` ET que `STRIPE_SECRET_KEY` n'est pas posée, les interfaces de paiement se **masquent** au lieu de retomber sur le simulateur (dans le middleware ET dans les pages). (3) **AUTRES** : mentions de futur supprimées (« OAuth éthique (bientôt) », « Bientôt ici aussi », « la rédaction publiera bientôt », colonne « Sur les réseaux » du pied de page qui n'annonçait que des comptes à venir) ; ton des états vides (plus jamais « reviens bientôt », qui dit que le site dort → invitation à agir) ; proposition d'adhérer après signature ou don ; **newsletter** en bas de chaque page publique (`components/layout/BlocNewsletter.tsx` + `app/actions/newsletter.ts`, l'adresse part chez Brevo, rien de dupliqué en base) ; `/cartes` et `/carte` fusionnées ; modération admin ramenée à 5 files, console nationale à 11 entrées ; accueil = une une par rubrique allumée. VÉRIFIÉ EN LIGNE le 01/08 sur maintenant-le-mouvement.org : 12 adresses gardées en 200, 16 endormies en 307, carte « 12 € » absente, `/agir/adherer/euros` redirigé, 4 unes avec du contenu réel. **RESTE À FAIRE PAR LILOU/BEN** : `npx wrangler secret put STRIPE_SECRET_KEY` puis `PAYMENT_PROVIDER` (`stripe_test` ou `stripe_live`) puis `npm run cf:deploy` — le paiement s'allume seul. **Emails Brevo : clés présentes en ligne mais envoi réel jamais testé de bout en bout**, à confirmer avant de compter dessus. Compteurs corrigés par Ben (10 631 newsletter, 470 membres, 17 747 signataires) : lus directement en base, aucun code en cause. Manifest : docs/manifests/2026-08-01-mise-en-sommeil-rubriques.md.
**Session du 2026-06-15** :
- **V2.6.124 curation de cagnottes externes — Phase 1 (Ulule)** (demande Ben : relayer des collectes des grandes plateformes FR alignées sur le mouvement, EN PLUS des cagnottes maison). DÉCISIONS BEN : tout SAUF HelloAsso pour l'instant (pas de clé partenaire) ; **modération A PRIORI** (le système propose, Ben valide avant publication) ; périmètre éditorial fourni par Ben (thèmes : écologie, féminisme, LGBTQIA+, antiracisme, décolonial, anarchisme, antifascisme… + types : livres/jeux militants, caisses de grève, cantines solidaires, collectes relayées par la presse). DÉCOUVERTE : Ulule a une **API publique JSON sans clé** (`api.ulule.com/v1/search/projects`) → Phase 1 = API stable, pas du scraping. LIVRÉ : table `cagnotte_externe` (migration 20260615120000, additive, **appliquée au distant**, statut propose→publie/refuse, RLS lecture publique des publie / écriture service_role / maj admin, type ajouté à database.ts) ; `lib/import-cagnottes/` (types, themes [thèmes+types+EXCLUSIONS extrême droite, **matching mots ENTIERS** anti faux-positif « trans » dans « transition »], adaptateurs/ulule [API réelle, collectes en cours], curation [dédup+pertinence], importer [anti-re-proposition par source_url, dépôt en propose]) ; cron `/api/cron/import-cagnottes` + Worker `infra/cron-cagnottes` (quotidien, NE PUBLIE JAMAIS) ; écran modération a priori `/admin/moderation/cagnottes-externes` (Approuver/Rejeter+motif, actions admin) + compteur dans la file globale ; section publique « Soutenir des causes solidaires ailleurs » sur `/mobiliser/cagnottes` (DISTINCTE des cagnottes maison, liens sortants, badge plateforme+jauge). IMPORT RÉEL lancé : 141 projets Ulule → **37 candidats en file** (propose, 0 publié = invisible jusqu'à validation Ben). Garde-fous : renvoi vers la source (pas d'hébergement), exclusions extrême droite (ex. « Vive la France ! » écarté ; cas réel : la catégorie Jeux d'Ulule héberge Antifa le jeu MAIS aussi des jeux d'extrême droite). 9 tests cagnottes (dont non-régression transition≠LGBTQIA+), typecheck + lint verts. Plan : docs/plan-curation-cagnottes-externes.md ; manifest : docs/manifests/v2-6-124-cagnottes-externes-curation.md. DÉPLOYÉ + Worker cron maintenant-cron-cagnottes (quotidien 04:37, secret posé). **Phase 2 (2026-06-16)** : requêtes Ulule étendues à ~30 thèmes + **scraping MiiMOSA** (`adaptateurs/miimosa.ts`, via JSON `__NEXT_DATA__` Next.js, dons publiés ; +25 collectes) sur demande Ben « fais le scraping c'est important ». Recon Phase 2 : MiiMOSA exploitable (Next.js) ; LITA écartée (appli JS sans données statiques + modèle investissement) ; croisement presse abandonné (1/40 articles → non rentable). RESTE : autres scrapers (GoFundMe/Tudigo/Les Petites Pierres/Papayoux JSON-LD, chacun son parseur) ; HelloAsso quand clé. Scripts data-migration gitignorés (test-cagnottes.ts, run-import-cagnottes.ts, recon-*).
- **V2.6.123 suppression de la rubrique « Lives »** (demande Ben : « supprime la catégorie live et tous les contenus live déjà uploadés, n'upload plus de live dans tes routines »). LIVRÉ : (1) ROUTINES — cron Twitch (`api/cron/import-twitch-lives`) réduit à un no-op explicite (plus d'appel à `importerTwitchLives`) + Worker `maintenant-cron-twitch` supprimé côté Cloudflare ; format `live` retiré de l'import médias (`FormatMedia='podcast'|'video'|'dessin'`, `SOURCES_PAR_FORMAT` sans `live`, `importerFormat`/`articleExploitable`/`telechargerEtInsererMedia` simplifiés, `import-medias/route` FORMATS sans live, `scripts/import-medias-initial.ts` idem). Les ~40 chaînes YouTube ex-`live` (Blast, Le Média, syndicats, partis, ONG, perso) **basculées en `video`** → rien perdu (elles étaient déjà stockées en `type='video'` depuis V2.6.116). (2) UI — onglet « Lives » retiré (`TYPES_ONGLETS` sans live) ; `'live'` retiré des sélecteurs de type admin (EditeurClassementMedia, FormulaireEditionMedia, filtre admin/medias) et des 3 enums Zod de `validations/media.ts` (plus moyen de recréer un type='live'). (3) DONNÉES — `data-migration/retirer-lives.mjs` (--lister/--appliquer/--supprimer) : 24 contenus `type='live'` en prod → tous passés en `statut='retire'` (réversible, HTTP 204) ; option --supprimer (DELETE dur) si purge définitive voulue. CHOIX : type DB `live` CONSERVÉ (lignes historiques, additif) ; code Twitch (`lib/twitch`, `lib/import-twitch`) conservé dormant ; affichage défensif `live` dans MosaiqueMedias/MediaEmbed laissé (branche morte inoffensive). 1161 tests verts, typecheck + lint verts ; E2E media.spec recalé (vérifie l'absence de « Lives »). **Déployé** sur maintenant-le-mouvement.org. Manifest : docs/manifests/v2-6-123-suppression-lives.md.
- **V2.6.122 sources « autres fuseaux » pour combler la nuit** (question Ben « pourquoi régulièrement des moments sans imports ? » puis sa solution). DIAGNOSTIC : pas une panne de cron ; la règle de fraîcheur < 2 h (V2.6.121) + ~90 % de sources en fuseau européen = la nuit parisienne (≈1h-7h) personne ne publie, l'import ne trouve rien, le fil se fige (contenus en tête à 9-11 h). SOLUTION (idée Ben) : garder la règle stricte, mais ajouter des médias engagés qui publient PENDANT notre nuit (Amériques) + DOM-TOM francophones (couvrent tout le globe en français). LIVRÉ : **38 sources ajoutées** dans `lib/import-breves/sources.ts`, **toutes vérifiées vivantes le 15/06** (flux + heure de publication Paris réelle ; scripts `data-migration/verifier-sources-fuseaux.mjs`, `verif-quebec.mjs`, `verif-domtom.mjs`, gitignorés) : Québec/Canada/Haïti (Le Devoir [flux section politique, pas manchettes qui a des dates identiques], Ricochet, Radio-Canada Info, Pivot, Presse-toi à gauche !, La Presse, AlterPresse), anglophones US/Canada (Jacobin, The Intercept, Truthout, Common Dreams, The Nation, Democracy Now!, ProPublica, Mother Jones, In These Times, The Real News, Grist, Labor Notes, Inside Climate News, The Breach, rabble.ca, The Tyee, The Maple, National Observer), hispano Amérique latine (El Ciudadano, La Izquierda Diario, Resumen Latinoamericano, La Jornada), Espagne (El Salto, Pikara, Sin Permiso, validé Ben), **DOM-TOM** (Zinfos974/Réunion=aube, Karib'Info/Guadeloupe=soirée, Tahiti Infos/Polynésie=cœur de nuit, Les Nouvelles Calédoniennes=nuit profonde). MÉCANISME : champ `zone` (`ameriques`/`outre-mer`, absent=Europe) + `tirerSourceHoraire` conscient de l'heure (`estHeureCreuseParis` 23h-6h59) → LA NUIT tire à 85 % dans `SOURCES_NUIT` (15 % repli sur tout), LE JOUR inchangé (80/20 prioritaires/complémentaires) ; NYT retagué `ameriques`. `maxEssaisSources` relevé 4→6. ÉCARTÉES (flux morts/illisibles) : Página/12, El Desconcierto, Desinformémonos, CTXT, Canadian Dimension, L'aut'journal, + flux « La 1ère » France TV (renvoient une page). LIMITE : même trou de nuit côté `import-medias` (podcasts/vidéos/dessins, sources YT FR) non traité. 1161 tests verts (+6), typecheck + lint verts. **Déployé** sur maintenant-le-mouvement.org (worker version fa528595, page média HTTP 200, import déclenché OK post-déploiement = 1 brève Reporterre). Manifest : docs/manifests/v2-6-122-sources-multi-fuseaux.md ; proposition sourcée : docs/sources-fuseaux-ameriques-proposition.md.
**Session du 2026-06-14** :
- **V2.6.121 import : règle de FRAÎCHEUR < 2 h + quotas + correctif agenda** (demandes Ben). DIAGNOSTIC : médias importent bien chaque heure (476 médias publiés accumulés, mais la page n'en affiche que 150 — plafond `listerFluxMedias`, choix de Ben de laisser tel quel) ; mobilisations bloquées ~24 h. CAUSES : Agenda — le quota de 7 « événements traités » était cramé par les événements PASSÉS en tête (chaque page coûte 1 sous-requête) ; Démosphère — règle dure « pas d'affiche → écarté » + plan Free 50 sous-requêtes. CORRECTIFS : (1) **Fraîcheur** : brèves (`importerBreveHoraire`) et médias (`importerFormat`) n'importent QUE les contenus publiés il y a < 2 h (cadence horaire → fil réellement frais, plus d'accumulation ; idempotence par source_url + fenêtre 2 h > 1 h entre runs = rien raté). (2) **Agenda** : `importerAgendaMilitant` borne désormais le COÛT en sous-requêtes (examiner = 1, créer ≈ 5) au lieu du nombre d'événements → parcourt beaucoup plus pour sauter les passés sans dépasser 50 (budget 46). (3) **Quotas relevés** : brèves 4→8/h, médias 1→2 par format/h. Règle « affiche obligatoire » CONSERVÉE (Ben). Démosphère laissé tel quel (sans affiche → écarté ; limité par le plan Free). tsc + lint verts, 26 tests import verts.
- **V2.6.120 sondages à CHOIX MULTIPLE** (demande Ben : certains sondages doivent accepter plusieurs réponses). Migration additive + rétro-compatible (appliquée en prod via l'API de gestion Supabase, SUPABASE_ACCESS_TOKEN) : `sondage.choix_multiple bool`, `reponse_sondage.options_choisies int[]` (option_index rendu nullable + contrainte « au moins une réponse »), vue `sondage_resultats` réécrite pour DÉPLIER options_choisies (1 ligne/option choisie). Types `database.ts` mis à jour à la main. Back : `creerSondageSchema` (+choix_multiple), `voterSondageSchema` (option_index optionnel + options_choisies + refine « l'un OU l'autre »), action `voterSondage` (branche unique/multiple, dédup + bornes, insère options_choisies). Front : `FormulaireVote` (cases à cocher si multi, état local → champ), `requetes` (base d'affichage = nb VOTANT·ES, pondéré désactivé en multi), `ResultatsSondage` (note « total > 100 % », pas de bascule pondéré). `lib/admin/analyse-sondage` : vote multi mappé à -1 (analyse de vote = choix unique pour l'instant). tsc + lint verts, 69 tests sondages/validations verts. RESTE : toggle « choix multiple » dans le formulaire de création (les 7 sondages de démo sont créés par script) ; pondération/croisements du multi-choix (v2).
- **V2.6.119 moteur de redressement multi-variables + croisements + fiabilité (sondages, briques 2-3)** (demande Ben, croisements ADMIN seulement). MOTEUR PUR testé : `lib/sondages/raking.ts` (calage sur marges / raking IPF multi-variables : `calculerPoidsRaking` itère jusqu'à convergence + bornage des poids ; `agregerPondere` → totaux pondérés + n effectif + effet de plan de Kish ; `margesRedressement()` branche les vraies marges V2.6.118), `lib/sondages/fiabilite.ts` (`margeErreur95`/`intervalle95`/`nEffectif`/`effetDePlan`/`SEUIL_CELLULE=30`, marges TOUJOURS sur le n effectif), `lib/sondages/croisements.ts` (`croiserParVariable` → vote par modalité de profil + drapeau fiable). DONNÉES admin : `lib/admin/analyse-sondage.ts` `analyserSondage(slug)` réservé `estAdminCourant`, lit votes + profil complet (`profil_qualification` via client service-role) et ne renvoie que des AGRÉGATS (brut, redressé, fiabilité, croisements avec cellules < 30 grisées) ; le genre déclaré au vote complète le profil manquant. AFFICHAGE : `app/admin/national/sondages/[slug]/page.tsx` (brut vs redressé + écart, encadré fiabilité, croisements par variable) + lien « Analyse » sur la liste admin. PÉDAGO : matrices expliquées à Ben (schéma + prose : échantillon → marges → raking → tri croisé → fiabilité ; atout = calage sur le vote réel présidentielle 2022). PORTÉE honnête : le PUBLIC garde le redressement âge-seul existant, le multi-variables + croisements vivent en admin (promotion publique possible plus tard) ; utile surtout quand le profil sera rempli (seuil 30). 9 tests moteur, 1157 tests verts au total, typecheck + lint verts. Manifest : docs/manifests/v2-6-119-moteur-redressement-croisements.md.
- **V2.6.118 marges de référence des sondages (socle du redressement)** (demande Ben : « il est hyper important d'avoir les vrais % de chaque option, c'est ce qui permet les quotas ; + un moteur de croisements et de fiabilité ; explique-moi tes matrices »). EXPLIQUÉ (schéma + prose) le pipeline : échantillon brut → marges de référence → calage sur marges (raking/IPF) → tri croisé pondéré → fiabilité (marge d'erreur, effet de plan de Kish, n effectif, seuil par cellule). CONSTAT honnête : le moteur actuel `ponderation.ts` ne redresse que sur l'ÂGE (1 variable). BRIQUE 1 livrée : collecte multi-agents sourcée (13 agents, 70 sources : INSEE, min. Intérieur, CEVIPOF, DARES, CRÉDOC) des % réels de population par variable → `docs/sondages-marges-reference.md` (23 variables) + `lib/sondages/marges-reference.ts` (16 variables de quota, cibles alignées sur l'ORDRE des options anti-typo, helpers `margesNormalisees`/`cibleParReponse`/`CLES_REDRESSEMENT`). Atout clé : calage sur le VOTE réel (présidentielle 2022 résultats officiels) qui neutralise la sur/sous-déclaration. Arbitrages Ben : revenu = marge fragile (extrapolée 2015, à réactualiser), gauche-droite réparti par tranches, ESS isolée, genre non-binaire/Autre non redressés. 5 tests d'alignement verts (longueurs = options, sommes ~1, helper). RESTE (briques 2-3, prochain chantier) : moteur de raking multi-variables (remplace le redressement âge-seul) + module croisements + fiabilité (n_eff, marges, seuils, affichage admin). Manifest : docs/manifests/v2-6-117 (questionnaire) ; marges détaillées dans docs/sondages-marges-reference.md.
- **V2.6.117 refonte du questionnaire de profil (sondages)** (demande Ben : retirer « taille d'agglomération », ajouter échelles de revenu + présidentielle 2022 exhaustive + logement détaillé, VÉRIFIER le vocabulaire des instituts, « monte à 25 propositions je choisirai » puis validation question par question). MÉTHODE : (1) réinitialisation prod `data-migration/reinitialiser-compteurs.mjs` (profil_qualification 11→0, reponse_sondage 2→0 ; les vues sondage_resultats/_par_tranche retombent à 0) ; (2) recherche multi-agents (13 agents : INSEE PCS 2020 + grille densité, enquête Logement, TeO2 INED, CEVIPOF, IFOP, Ipsos, CRÉDOC, min. Intérieur) → 26 questions sourcées (`docs/sondages-profil-25-propositions.md`) ; (3) validation une par une (AskUserQuestion) → **23 questions** ; (4) chiffres réactualisés : RSA 651,69 € / AAH 1 041,59 €/mois (1er avril 2026). PANEL `lib/sondages/qualification.ts` réécrit : +tranche 15-17 ans, revenu élargi vers le bas (<650 € sous le RSA), logement 6 postes (proprio/loc privé/HLM/hébergé privé/hébergé collectif/SDF), type de commune (remplace taille d'agglo, grille densité INSEE + repère habitant·es), +patrimoine, +aisance financière CRÉDOC, +confiance institutions (CEVIPOF, choix multiple), présidentielle 2022 T1 (12 candidat·es) + T2, européennes 38 listes ; retirées : statut emploi, origine sociale, législatives 2024, dons, intérêt politique. TECHNIQUE : `secondaire.requisSi` accepte plusieurs déclencheurs (`string | readonly string[]`) pour bénévolat (3 « Oui ») et syndicat (actuel/ancien) ; action `repondreQualification` adaptée ; OPTIONS_LOGEMENT 6 postes (somme quotas = 1). 55 tests sondages verts, typecheck + lint verts. Manifest : docs/manifests/v2-6-117-refonte-profil-sondage.md.
- **V2.6.116 « Lives » = Twitch uniquement + reclassement admin inline** (déclencheur : la 1re vérif d'un vrai direct Twitch [Valeur Anarchiste] a montré l'onglet Lives avec 6 cartes pour 1 seul direct → 5 rediffusions YouTube titrées « 🔴 »/« [EN DIRECT] » classées `live` par l'heuristique de titre V2.6.114 et JAMAIS retirées [le cron Twitch ne retire que les `twitch-*`]). Ben : « pouvoir modifier les types, les tags et catégories en mode admin directement sur chaque contenu ». LIVRÉ : (1) CORRECTIF D'IMPORT — un flux RSS/YouTube ne peut pas attester d'un direct EN COURS (rediffusions sans statut live) → tout import vidéo/live est stocké en **`video`** ; `live` réservé à `lib/import-twitch` (Helix temps réel + retrait auto). `estLiveDApresTitre` remplacé par `nettoyerTitreLive` (retire le badge trompeur 🔴/[EN DIRECT]/[LIVE]/« EN DIRECT : » du titre, conservateur). Test réécrit (3 verts). (2) RECLASSEMENT ADMIN INLINE — action MINIMALE `reclasserMedia` (réservée admin, n'écrit QUE `type`+`tags`, donc zéro risque d'effacer titre/corps/image/source, contrairement au `mettreAJourMedia` complet) + `reclasserMediaSchema` + composant `EditeurClassementMedia` (bouton « Classer » → select de type + 17 puces de tags + Enregistrer/Annuler) branché sur les 3 cartes de `MosaiqueMedias` à côté de « Mettre à la une ». (3) DONNÉES — 5 faux lives existants laissés à corriger par l'admin via le nouvel éditeur (Live→Vidéo) ; script de secours `data-migration/reparer-faux-lives.mjs` (gitignoré). typecheck + lint verts. Manifest : docs/manifests/v2-6-116-lives-twitch-only-reclassement-admin.md.
- **Mobilisations en import HORAIRE** (demande Ben) : crons `maintenant-cron-agenda` (:42) et `maintenant-cron-demosphere` (:52) passés de quotidien à horaire ; redéployés. La rotation des sites couvre tout le réseau en quelques heures au lieu de quelques jours.
- **Diagnostic « pas d'import depuis 10h »** : FAUX (cron OK, imports horaires vérifiés). Cause de l'impression de flux figé = tri par date de publication + import volontaire de vieux contenus (cf. V2.6.98) ; expliqué à Ben.
- **V2.6.115 rubrique « Lives » alimentée par Twitch** (demande Ben : les « lives » YouTube étaient en fait des vidéos → rubrique vide ; Twitch = vrai direct). Recherche : 3 workflows multi-agents (~36 agents ; sources Politis/RTBF/StreetPress/DSA/Red Pepper/Socialter/Wikipedia) → ~67 chaînes engagées (gauche radicale/révolutionnaire, féminisme INTERSECTIONNEL, antiracisme/décolonial, LGBTQIA+/queer/trans, élu·es à la gauche de la gauche, médias indé) ; validées via `helix/users` → **59 handles existants** (8 invalides écartés). Exclusions Ben : pas de masqués, féminin ≠ féministe (Angle Droit + L'AntiStream retirés). INTÉGRATION (pattern adapter) : `lib/twitch/` (types + MockTwitchService + TwitchHelixService [app token client_credentials caché + helix/streams par lots de 100] + factory selon TWITCH_CLIENT_ID/SECRET), `lib/twitch/sources-twitch.ts` (59), `lib/import-twitch/importer-twitch-lives.ts` (upsert `media type='live'` embed player.twitch.tv pour les directs EN COURS, retire les terminés ; slug `twitch-<handle>` assaini pour le CHECK), route `/api/cron/import-twitch-lives` (CRON_SECRET), Worker `maintenant-cron-twitch` (`*/15 * * * *`), `MediaEmbed` étendu (param `parent` requis par le player Twitch), CSP (frame-src player.twitch.tv + img-src static-cdn.jtvnw.net). Clés posées en **secrets du Worker** (`TWITCH_CLIENT_ID`/`TWITCH_CLIENT_SECRET` via `wrangler secret put`) + CRON_SECRET sur cron-twitch. Comportement : la rubrique montre les chaînes EN DIRECT à l'instant, vide sinon (honnête) ; sans clé, mock → vide. typecheck + lint verts ; helix testé (jeton OK, 59/67). Liste sourcée : docs/sources-twitch-lives-proposition.md ; manifest : docs/manifests/v2-6-115-rubrique-lives-twitch.md. RESTE : agrandir >100 (rate-limit recherche), corriger 8 handles invalides (danyetraz/djelib/krokoku/ultiaa/violine__/shiranamioff/berniesanders/tomasrebord), étoffer écologie-décroissance + hispanophone.
**Session du 2026-06-13 (jour, V2.6.108→114)** :
- **V2.6.114 texte des contenus + faux « lives » + catégories (revue de presse)** (retours Ben : un dessin Basta sans texte alors que la page en a, « plein de vidéos classées live alors que ce ne sont pas des lives », flux qui semble figé ; + CONSIGNE TRANSVERSE : « à chaque correction, encoder la logique pour les prochains imports, pour ne plus refaire le travail »). LIVRÉ : (1) TEXTE — `rss.ts` lit désormais `<media:description>` (corps des vidéos YouTube) ; nouveau `chercherDescriptionArticle` (méta `og:description` PROPRE) appelé par `importer-medias` quand le corps est court (dessins/articles). (2) LIVE — classification PAR LE TITRE (`estLiveDApresTitre` : 🔴 / « en direct » / « live » / « [direct] ») au lieu de par source ; +5 tests. (3) CATÉGORIES — tag Féminismes enrichi (« violences sexuelles/sexistes », « agression(s) sexuelle(s) »). « Flux figé » = PAS un bug (cron OK à 20:33 ; tri par date de publication + import volontaire de vieux contenus, cf. V2.6.98). INCIDENT corrigé honnêtement : un 1er essai de remplissage du texte raclait les `<p>` et ramassait l'encart de don de Basta! (« le RN rêve de 2027… faites un don ») → faux tags Extrême droite/Tech&IA sur ~16 contenus ; remplacé par `og:description`, nettoyé en prod (feu vert Ben). EXISTANT corrigé (réversible) : `data-migration/reparer-medias-revue.mjs` (40 faux lives → vidéo ; 97 textes re-dérivés d'og:description) + `retaguer-medias.ts --tout`. **PRINCIPE ADOPTÉ** (consigne Ben, mémoire agent `corriger-c-est-corriger-l-import`) : aucune correction de données n'est livrée sans son correctif d'import + une rubrique « Logique de prévention (pour ne plus refaire) » dans le manifest. 1142 tests verts, typecheck + lint verts. Manifest : docs/manifests/v2-6-114-revue-presse-texte-live-categories.md. RESTE : rubrique « Lives » quasi vide (peu de vrais directs YouTube) → chantier Twitch (≈50 chaînes engagées) demandé par Ben.
- **V2.6.113 dédup vidéo/brève + catégories de la revue de presse** (retour Ben page Médias : une vidéo et une brève faisaient doublon → « une vidéo + 6-7 lignes de texte dessous », « détecte ce type de cas à l'avenir », « vérifie les autres cas », « mets les bonnes catégories »). CAUSE : la dédup d'import ne comparait que le `source_url` exact (≠ entre page vidéo et page article) et par format ; le mot-clé `agricol` taguait « site agricole » (lieu d'un crime) en « Écologie », et la vidéo (corps vide) tombait sur « Politique » par défaut. LIVRÉ : (1) détection PURE `lib/media/doublons.ts` (+13 tests) par COEFFICIENT DE RECOUVREMENT de titres (gère préfixe « Nom : » et suffixe « [EXTRAIT] »), gardes-fous même source + fenêtre 4 j + min mots communs + anti faux-positif (fusion seulement si formats mélangés OU titres strictement identiques) ; on garde le plus riche (vidéo/live > podcast > brève > dessin). (2) `data-migration/dedup-medias.mjs` (--lister/--appliquer, gitignoré) : 4 doublons réels (faux positif « 2 dessins Basta! » écarté) — Le Média (live), Frustration (vidéo), Là-bas (vidéo), Télé Millevaches (vidéo ×2) ; appliqué = 3 vidéos/lives enrichis du texte + 4 doublons retirés (`statut='retire'`, RÉVERSIBLE). (3) Catégories : `agricol` (nu) → termes ciblés + `nitrate` ajouté dans `lib/import-breves/tags.ts` ; `scripts/retaguer-medias.ts` gagne `--tout` (recalcule TOUTE la revue de presse depuis titre+corps, n'écrase jamais les tags maison) ; 15 contenus re-tagués en prod. (4) PRÉVENTION à l'import dans les DEUX importeurs : `fusionnerDansVideoMemeSujet` (brèves) + `chercherBreveMemeSujet` (médias), réutilisant `comparerTitres` → plus de doublon créé. (5) Affichage déjà OK (`CarteImportante` rend `corps` en `line-clamp-[7]`). 1139 tests verts, typecheck + lint verts. Tagging par mots-clés = reste imparfait sur quelques contenus (éditable en admin). Manifest : docs/manifests/v2-6-113-dedup-video-breve-categories.md.
- **V2.6.112 éditeur d'options ligne par ligne (retour Ben sur le formulaire de création de sondage)** : Ben : « on ne peut pas téléverser une image par option, il ne faut pas un grand cadre, il faut un système avec des petits plus pour rajouter des options, nommer chaque option et téléverser une image par option ». Le gros `Textarea` (une option par ligne) + le bloc images séparé sont remplacés dans `FormulaireCreationSondage` par un vrai éditeur : une LIGNE par option = badge numéroté + champ nom (`Input`) + `TeleverseurImage` (préfixe `sondages/options`) + bouton retirer (X, min 2 lignes), plus un bouton « Ajouter une option » (`Button` ghost, max 20). L'image vit DANS la ligne (état `lignes: {id,libelle,image}[]`, id stable via `useRef`) → fini le couplage par position (corrige la limite notée en V2.6.88 : réordonner ne décale plus les images). `onSubmit` ne garde que les options nommées (filtre les libellés vides, image restant alignée) et alimente la génération de mosaïque V2.6.111 inchangée. Libellés CMS mis à jour (`labelOptions`, `aideOptions`, `placeholderOption`, `ariaLibelleOption`, `ariaRetirerOption`, `ctaImageOption`, `ctaAjouterOption`, `maxAtteint`). Typecheck + lint verts, rendu vérifié au navigateur (Playwright, formulaire isolé). Déployé.
- **V2.6.111 mosaïque de couverture générée automatiquement pour chaque sondage** (demande Ben : « que les sondages produisent systématiquement une mosaïque, du même type que le présidentielle : images des options + titre incrusté, servant de couverture, miniature et aperçu de partage »). Arbitrages Ben (AskUserQuestion) : tuiles avec le TEXTE des options quand pas de photo, titre en bandeau AU-DESSUS ; génération AUTOMATIQUE (jamais un bouton), avec alternative = téléverser sa propre image (prioritaire sur la mosaïque). TECHNIQUE : `sharp` (script présidentiel) ne tourne pas sur Cloudflare Workers → génération CÔTÉ NAVIGATEUR via `<canvas>` (même procédé que `lib/image-redimensionner.ts`), upload par la Server Action `televerserImage`, URL posée en `image_url` (déjà lu pour couverture + miniature + og:image, donc les 3 usages d'un coup). Nouveau `lib/sondages/mosaique-layout.ts` (géométrie PURE testée : grille selon nb options, cellules + centrage dernière ligne incomplète, bandeau adaptatif 1-4 lignes, découpe titre/libellés avec ellipse) + `lib/sondages/mosaique-canvas.ts` (bandeau titre + filet rose + sous-titre ; tuile = photo recadrée cover OU tuile colorée numéro+libellé ; crossOrigin anonymous + repli tuiles texte si CORS « tache » le canvas + null en dernier recours → image par défaut ; 1200x630). Branché dans `FormulaireCreationSondage` (génère si aucune couverture fournie). 1126 tests verts (+12 sur mosaique-layout), typecheck + lint verts, rendu vérifié dans Chromium (Playwright) sur 3 cas (sans photo / photos / mixte). Pas de formulaire d'édition de sondage → génération à la création seule ; existants non rétro-générés (présidentielle garde sa mosaïque, backfill possible sur demande = porte distant). Manifest : docs/manifests/v2-6-111-mosaique-auto-sondages.md. Commit 1ce9dcc, déployé sur maintenant-le-mouvement.org (worker version 07dd35af, page sondages HTTP 200). Backfill des existants SANS OBJET : 0 sondage sans couverture (image_url null), aucune couverture existante écrasée. Script de rattrapage prêt si besoin (data-migration/generer-mosaiques-sondages.mjs, --lister/--appliquer, gitignoré).
- **V2.6.108 carte unifiée : regroupement (clustering MapLibre)** (constat Ben : « il manque des événements sur la carte »). DIAGNOSTIC : rien ne manquait (341 mobilisations chargées, mais ~210 positions distinctes → superposition à un même lieu, ex. 14 événements à L'Antidote à Bourges). Carte refondue en source GeoJSON clusterisée (`cluster: true`, clusterRadius 45, clusterMaxZoom 15) : couches `clusters` (pastille avec le nombre) / `clusters-count` / `points-unite` ; la pastille s'éclate au zoom ou au clic (`getClusterExpansionZoom`) ; filtrage par type via `setData` ; popup au clic sur point isolé. `components/carte/CarteUnifiee.tsx` (`versGeoJson`, glyphs demotiles.maplibre.org). Choix « pastille » validé par Ben (AskUserQuestion).
- **V2.6.109 édition admin de TOUS les contenus média** (demande Ben : « en mode admin il faut pouvoir modifier tous les contenus, les articles ne sont pas modifiables »). Nouvelle Server Action `mettreAJourMedia` (réservée admin via `estAdminCourant()`, met à jour titre/corps/type/vignette_url/media_url/tags/provenance_externe/source_url) + schéma `mettreAJourMediaSchema` ; nouvelle page `/admin/national/medias/[id]` + `FormulaireEditionMedia` (tous les champs éditables, champs revue de presse repliés) ; bouton « Modifier » ajouté sur la fiche média à côté de « Modérer ». Déployé (`cf:deploy`).
- **V2.6.110 vraie photo d'Antoine Miko (Équinoxe) sur le sondage présidentielle** (demande Ben : « mets la photo sur le candidat manquant dans la miniature et dans la mosaïque, recadre-la »). Ben a fourni l'affiche ÉQUINOXE 2027 (retrouvée sur son Bureau OneDrive). Portrait d'Antoine Miko recadré avec `sharp` (`extract` left 135 / top 135 / 270×270 → resize 600×600, depuis l'affiche 526×703 ; le collage de l'affiche reste en fins lisérés sur les côtés, fond neutre impossible) en remplacement de la vignette d'initiales « AM / Photo à venir ». Pour contourner le cache CDN de Supabase Storage, fichiers téléversés sous des noms VERSIONNÉS : `sondages/presidentielle-dimanche-prochain/mikolajczak-photo.jpg` et `couverture-v2.jpg`. Le sondage existait déjà (`--publier` verrouillé) → PATCH REST de `options_images[8]` (vignette de l'option, consommée par `FormulaireVote` et `ResultatsSondage`) + `image_url` (miniature/couverture/OG, mosaïque 1200×630 régénérée via `data-migration/creer-sondage-presidentielle.mjs --mosaique`). Page rendue dynamiquement (`Cache-Control: no-store`) → live immédiat sans redéploiement (vérifié : le HTML sert `couverture-v2.jpg`). Scripts `data-migration/creer-sondage-presidentielle.mjs` (déjà là) + `data-migration/remplacer-photo-miko.mjs` (nouveau), tous deux gitignorés (`data-migration/` ligne 85) ; le portrait local mis à jour garde le pipeline reproductible. Note partage : un aperçu Facebook/Messenger déjà en cache peut montrer l'ancienne mosaïque ~30 j (rafraîchissable via le débogueur de partage FB).
**Session du 2026-06-12 (résumé, manifests v2-6-87 à v2-6-90)** :
- **Pétitions corrigées en base prod** (data-migration/corriger-petitions.mjs) : la pétition à 1 755 sigs = « Tribune citoyenne pour l'interdiction de la marche d'extrême droite prévue ce samedi à Lyon » (texte verbatim Ben + image NO PASARAN) ; Baranoux (1 813 sigs) restaurée verbatim (texte retrouvé dans l'export Claude.ai de Ben, PAS d'image disponible, à fournir) ; Cuba (326/1 000) = « Cuba : non à l'embargo non à la guerre » (texte rédigé sur délégation Ben, image drapeau Wikimedia) ; doublon Epstein 0/15 000 SUPPRIMÉ. Compteurs intouchés.
- **V2.6.87 cartes** : CSP autorisait seulement `*.tile.openstreetmap.org`, le domaine NU manquait → fond de carte réparé ; 54 mobilisations géocodées (Nominatim, data-migration/geocoder-mobilisations.mjs) + géocodage branché dans l'import AMI quotidien (budget route 8→7) ; la carte n'affiche QUE les mobilisations à venir/en cours (décision Ben).
- **V2.6.88 sondages V2** : 20 options max, image PAR option (colonne additive options_images, parallèle à options), plus de mode choisi à la création (brut d'office < 300 répondant·es, pondéré d'office au-delà, toggle visiteur ; lib/sondages/ponderation.ts quotas âge À VALIDER par Ben), image de couverture branchée OG, résultats cachés avant le vote, AUCUNE option précochée. BUG racine du vote : valueAsNumber sur radios = NaN (Zod v4 « Invalid input ») → schéma union chaîne/nombre. Sondage présidentielle publié : /s-informer/sondages/presidentielle-dimanche-prochain (18 candidat·es, portraits Wikipédia, mosaïque titrée 1200x630 ; photo Mikolajczak manquante = vignette initiales, Ben doit fournir le fichier).
- **V2.6.89 qualification progressive** : vote 2 champs optionnels (genre H/F/NB/Autre + tranche d'âge), code postal JAMAIS demandé (pris du profil), tranche déduite de date_naissance ; après vote : questions du panel CDC §7 (22 questions verbatim, lib/sondages/qualification.ts, logement 5 catégories INSEE 57,1/34,9/5/2,7/0,3) enchaînées automatiquement, bouton « C'est tout pour aujourd'hui », table NON PUBLIQUE profil_qualification (RLS personne seule). RESTE : moteur de redressement complet CDC §2-§5 (tirage seedé, raking, étoiles, paliers, fiche méthodo, boîte noire), dérivés géo du CP, barèmes admin.
- **V2.6.90 revue de presse** : /s-informer/media = mosaïque type Reuters (onglet Brèves SUPPRIMÉ) : une (= rédaction SEULEMENT, jamais une brève ; épingle une_home 'article' sinon maison le plus récent), bandeau « La rédaction » (tous les contenus maison, toujours), brèves importantes (vraie image + 5-7 lignes) / annexes (3-4 lignes + vignette compacte), sur-titre = NOM DU MÉDIA SOURCE (pas de badge Brève), tags (17, lib/import-breves/tags.ts), Lire la suite ↗ vers la source, langue conservée. Import lib/import-breves/ (38 sources : 23 prioritaires Ben + 15 Portail Basta), images COPIÉES bucket breves/ (flux → og:image de l'article → logo source en dernier recours = reste annexe), RÈGLE 1 brève/source/24h (sauf rédaction), cron HORAIRE Worker maintenant-cron-breves (12 * * * *, 80/20, CRON_SECRET posé via API CF). 40 brèves en base, toutes illustrées. Flux cassés à réparer : Politis, Haaretz, Arrêt sur images, Afrique XXI.
- **Migrations distantes appliquées ce jour (feu vert Ben explicite à chaque fois)** : 20260612110000 (sondage options 20 + options_images + vue par tranche), 20260612150000 (profil_qualification), 20260612180000 (media.langue + media.importante). Via `npx tsx scripts/appliquer-sql-distant.ts` (charger NEXT_PUBLIC_SUPABASE_URL + SUPABASE_ACCESS_TOKEN depuis .env.local dans l'env AVANT, jamais en clair sur la ligne de commande).
- **Pièges Windows/outillage confirmés** : supprimer .open-next avant chaque build ; `tsx` non installé → `npx --yes tsx` ; secrets Workers via API CF (le pipe wrangler ajoute un CR) ; Zod v4 (messages « Invalid input », param { error }) ; react-hook-form valueAsNumber NaN sur radios.
- **V2.6.92 fix images breves** (constat Ben : 2 breves Liberation + 1 NYT sans leur vraie image, grand blanc moche sur la carte annexe) : Liberation et NYT bloquent les robots sur leurs pages article (403, og:image illisible) → chasseurs d'image SPECIALISES par domaine dans lib/import-breves/importer.ts (sitemap Arc `/arc/outboundfeeds/sitemap/` pour Liberation, oEmbed `svc/oembed/json` pour NYT), essayes apres l'og:image, avant le logo ; table CHASSEURS_PAR_HOTE extensible. Carte annexe relookee (logo 96px, titre text-base, extrait 9 lignes, Lire la suite ↗). 3 breves reparees en prod (data-migration/reparer-images-breves.mjs, --lister/--appliquer) et verifiees en ligne. 1106 tests verts. Manifest : docs/manifests/v2-6-92-fix-images-breves-chasseurs-specialises.md.
- **V2.6.103→107 (session soir 2026-06-13)** : (103) CONNEXION captcha vert mais serveur KO = jeton Turnstile usage unique non régénéré → prop resetTrigger sur CaptchaTurnstile + reset dans les 4 formulaires auth. (104) EMAIL non reçu = quota Supabase rate_limit_email_sent était à 2/h → monté à 50/h (feu vert Ben, via API Management) + bouton « Renvoyer le mail de vérification » (action renvoyerVerificationEmail) sous l'erreur « email non vérifié » ; source brève Le Chiffon ajoutée. (105) sous l'erreur, suggestion du « Lien magique » (connecte + valide d'un clic), ancre #lien-magique. (106) SOURCES LIVES étoffées (validées Ben) : +28 chaînes en format live (7 syndicats hors CFDT, 7 partis dont Révolution Permanente/NPA/Ruffin/Lutte Ouvrière, 6 ONG Soulèvements/Attac/XR/Oxfam/ATD/Banlieues Climat, 6 perso-médias Camille Étienne/Reporterre/Politis/Off Investigation/Thinkerview/Radio Nova, 2 international AOC/Bernie) ; 28 peuplées ; PCF écarté (pas de chaîne YouTube active). (107) IMPORT DEMOSPHERE : nouveau module lib/import-demosphere (22 sites France), mêmes règles que l'Agenda Militant (à venir, AFFICHE OBLIGATOIRE, idempotent) ; technique = /event-list-json (liste + GPS) puis JSON-LD de /rv/<id> (description + affiche) ; peuplement 30/site = 348 mobilisations créées (carte passe à 310 mobilisations géolocalisées) ; cron quotidien maintenant-cron-demosphere (47 4 * * *, rotation 4 sites/jour, 3/site, secret posé). Tout déployé.
- **V2.6.102 cadence + onglet Rédaction** (Ben) : cadence horaire = 4 brèves (import-breves BREVES_PAR_HEURE=4) + 1 de CHAQUE format (import-medias boucle podcast/video/live/dessin, plus de rotation) ; sur la page générale la rédaction est bornée à 3 (MAX_REDACTION_GENERALE) ; nouvel onglet « Rédaction » (?type=redaction) listant TOUS les contenus maison par date décroissante (remplace les onglets séparés edito/tribune/article/newsletter ; onglets restants : À la une | Rédaction | Dessins | Podcasts | Vidéos | Lives). Workers cron inchangés (breves 12 * * * *, medias 32 * * * *). Déployé.
- **V2.6.99→101 retours Ben (captcha mobile + tri du flux)** : (a) CAPTCHA MOBILE : le widget Turnstile était monté via le sélecteur d'ID à deux-points de useId (`#:r0:`), fragile sur navigateurs mobiles (Safari iOS) → montage via l'ÉLÉMENT DOM directement (components/formulaires/CaptchaTurnstile.tsx) + recours visible si échec (message + bouton « Réessayer », gestion error/timeout-callback, retry auto, timeout 20s) au lieu d'un bouton de soumission grisé muet. (b) TRI DU FLUX : d'abord passé à created_at (« flux vivant ») PUIS revenu à `publie_le` décroissant sur demande Ben (« classer par dates les plus récents d'abord ») dans listerFluxMedias. (c) VIEUX CONTENUS : un filtre anti-vieux (45j) + nettoyage avaient été ajoutés puis RETIRÉS sur consigne Ben (« n'enlève pas les vieux, mets-les juste en bas ») ; les 37 contenus supprimés par erreur ont été réimportés ; le tri par date suffit à reléguer les anciens en bas. État : 92 brèves, 43 dessins, 41 podcasts, 40 vidéos, 9 lives. Déployé, vérifié en ligne.
- **V2.6.98 routine horaire vivante** (Ben : « pas d'import depuis 4h, pas normal ») : DIAGNOSTIC = le cron tournait bien (2 workers actifs : maintenant-cron-breves 12 * * * *, maintenant-cron-medias 32 * * * *, imports vérifiés à 07h/08h) ; le « il y a 4h » que Ben voyait = la date de PUBLICATION des articles, pas d'import. VRAI défaut corrigé : listerFluxMedias triait par publie_le → un contenu importé mais publié il y a longtemps ne remontait pas → flux figé. Passé à `created_at` (ordre d'arrivée dans la revue) : chaque import horaire remonte en tête, flux vivant (vérifié en ligne). + REPLI DE FORMAT dans /api/cron/import-medias : si le format de l'heure (heure%4) est épuisé (cas live : 9 sources toutes bloquées par la règle 1 source/24h), bascule sur les autres formats → garantit 1 contenu multi-format CHAQUE heure. Endpoints retestés en prod (medias→1 podcast, breves→2 brèves de 2 sources). 1114 tests verts, déployé.
- **V2.6.97 revue multi-format, retours Ben** : (1) FLUX PRINCIPAL VARIE : l'onglet « À la une » mêle desormais tous les formats externes (breves+dessins+videos+lives+podcasts) antichrono, plus seulement les breves (page media : medias = flux.filter(provenance_externe non null)). (2) REDACTION RESTAUREE : bug ou les 112 contenus externes noyaient les articles maison → listerMediasMaison filtre `provenance_externe IS NULL` (pas juste type!=breve). (3) TAG MINIMUM 1 : assignerTags repli sur TAG_PAR_DEFAUT='Politique' si rien ne matche ; 58 medias re-tagues en prod (scripts/retaguer-medias.ts). (4) SURTITRE des cartes externes multi-format = badge format (Dessin/Live/Video/Podcast) + nom du media source (les breves gardent juste le nom du media). (5) UNE ADMIN LIBRE : BoutonMettreALaUne sur CHAQUE carte en mode admin (emplacement 'article' partage avec la home), la une prend l'epingle quel que soit le type (auto = maison recent, jamais breve). (6) ROUTINE HORAIRE EQUILIBREE : import-breves passe a 2 breves/heure, nouveau comportement import-medias = 1 contenu multi-format par heure en ROTATION (heure%4 : podcast/video/live/dessin → 6/format/jour), worker maintenant-cron-medias repasse de quotidien a HORAIRE (32 * * * *). 1114 tests verts, deploye. Manifest v2-6-96 (le multi-format) couvre la base.
- **V2.6.96 revue de presse MULTI-FORMAT** (demande Ben : podcasts, videos, lives, dessins en plus des breves) : 3 listes de 27 sources engagees validees (docs/sources-podcasts-videos-dessins-proposition.md, Charlie Hebdo exclu, France Culture garde). AUCUNE migration (table media a deja media_url/vignette_url/type/tags). Analyseur RSS etendu additif (lib/import-breves/rss.ts : audioUrl enclosure podcast, videoId yt:videoId, itunes:image). Module lib/import-medias/ (sources-medias.ts + importer-medias.ts : audio podcast, embed youtube-nocookie video/live, image dessin avec repli og:image, images copiees bucket medias/<format>/, regle 1 source/24h, rotation par jour, iteration multi-articles/source). UI : components/media/MediaEmbed.tsx (facade video clic→iframe youtube-nocookie + lecteur audio HTML5), MosaiqueMedias generalise aux 4 formats (VisuelMedia, libelles Ecouter/Regarder/Voir sur le site source), TOUS les onglets suivent les 3 logiques (une/redaction/separateur Revue de presse). CSP : frame-src youtube-nocookie + img-src i.ytimg.com. Cron QUOTIDIEN : route app/api/cron/import-medias (9/format/jour, rotation quantieme) + Worker maintenant-cron-medias (17 5 * * *, secret pose, teste OK). Distinction video/live PAR SOURCE (flux YT ne marque pas les directs). Peuplement : 41 dessins, 35 podcasts, 27 videos, 9 lives, 85 breves. 1114 tests verts, deploye, verifie en ligne. Manifest : docs/manifests/v2-6-96-revue-presse-multiformat.md.
- **V2.6.95 revue de presse a 72 breves** (decisions Ben) : les 4 breves sous 6 lignes (2 Libe, 2 NYT dont Hockney) SUPPRIMEES (arbitrage explicite « supprime et remplace ») ; complement a 72 breves exactement (en plus des 4 contenus redaction) via scripts/completer-breves.ts (--dry-run/--confirm, reutilise lib/import-breves : 6 lignes mini avec etoffage, chasse image renforcee, regle 1 breve/source/jour en comptant l'EXISTANT en base, idempotent par source_url) ; 34 creees en 1 passe, les candidates trop courtes (Hockney, SpaceX, Crepol...) ecartees d'elles-memes par la regle. CRON HORAIRE VERIFIE VIVANT : 1 breve a 11h12, 12h12, 13h12 UTC (planification 12 * * * * du Worker maintenant-cron-breves). Etat final verifie : 72 breves, 0 entite restante, 0 sans image, 0 format logo, corps tous >= 360 car (1 breve cron passee 1 min avant le deploiement de la regle etoffee a posteriori). 72 cartes visibles sur la page en ligne.
- **V2.6.94 vignettes emoji/miniatures** (constat Ben : breves Regards avec le « doigt jaune » = emoji WordPress 👉 pris pour image d'article) : filtre estImageTechnique (emojis, smileys, LOGOS, pictos, SVG) dans l'extraction d'image du flux (rss.ts) + nouvelle chasse chercherImageCorpsArticle (premiere image `/wp-content/uploads/` du corps de la page, pour les sites SANS og:image comme Regards) inseree avant le logo ; 7 vignettes remplacees en prod via data-migration/reparer-vignettes-emoji.mjs (detection : vignette importante < 15 Ko ; 2 Regards, 3 Guardian, 2 Blast passees en vraies images haute definition). 1113 tests verts, deploye.
- **V2.6.93 textes de breves + partage OG** (demandes Ben) : (a) regle NOUVELLE aucune breve sous ~6 lignes (MIN_CARACTERES_EXTRAIT=360 dans importer.ts, etoffage automatique depuis les paragraphes de la page article via chercherTexteArticle/extraireParagraphes) ; (b) decodage COMPLET des entites HTML des flux (table ENTITES_NOMMEES dans rss.ts, 2 passes pour les doubles encodages &amp;rsquo;, figcaption retirees avec contenu) ; 24 breves reparees en prod (data-migration/reparer-texte-breves.mjs : 8 nettoyees + 16 etoffees, 4 IRREPARABLES a arbitrer par Ben : 2 Libe + 2 NYT pages bloquees aux robots) ; (c) partage reseaux sociaux : image OG par defaut du site (logo dans app/layout.tsx), images SVG remplacees par le logo raster dans metadataPourPartage (robots ne lisent pas le SVG), pages Decider+agenda branchees sur le helper, espaces normalises dans les descriptions OG ; verifie en prod avec UA facebookexternalhit sur 9 types de pages (titre+description+image 200 partout). Le mauvais apercu Messenger de Ben = cache Facebook (~30 j), rafraichissable via developers.facebook.com/tools/debug. 1111 tests verts. Manifest : docs/manifests/v2-6-93-textes-breves-et-partage-og.md.
**Dernière mise à jour (antérieure)** : 2026-06-09 (V2.6.80 : propagation du fix captcha, bouton bloqué et message d'attente tant que la vérif anti-robot Turnstile n'est pas validée, aux 29 formulaires restants ; lien magique vérifié OK par Ben)
**Dernier chantier terminé** : **V2.6.80 propagation du fix captcha aux formulaires** (porte technique/UI, autonome). Le gating « bouton désactivé + message d'attente tant que le jeton Turnstile n'est pas obtenu » n'était posé que sur la connexion (V2.6.76/77) ; propagé aux **29 formulaires** restants affichant `<CaptchaTurnstile>` (créations pétition/cagnotte/campagne/mobilisation/moment/sondage/produit/minimarché/boutique/offre/service, dons euros+99-coin, adhésions gratuit/euros/99-coin, achat marché, notation, vote, signature pétition, composeur réseau, rejoindre commune/fédération, participer mobilisation/moment, + auth reset/magic-link/inscription). Pattern identique à la référence : état `hydrate`, `captchaValide` (`watch('token_turnstile')` en react-hook-form, `token !== ''` en useState), message `aria-live` éditable (clé CMS `messageCaptchaEnAttente` ou constante de module selon le composant), `disabled={... || !hydrate || !captchaValide}`. Boutons Annuler/Fermer/Quitter non gatés. **1030 tests verts**, typecheck + build verts, lint propre sur les 29 fichiers. Manifest : `docs/manifests/v2-6-80-propagation-fix-captcha.md`.
**Dernier chantier terminé (antérieur)** : **Frais Stripe unifiés 3 % + 0,30 €** (décision Lilou/Ben 2026-06-09, porte « argent réel »). Sur tous les paiements **euros** (Stripe), frais de **3 % + 0,30 €** quel que soit le type (adhésion, don cagnotte, achat marché, et futurs dons réseau/médias + entraide payante), **à la charge du payeur**, **ajoutés au-dessus** (le bénéficiaire reçoit le plein). **Remplace** l'ancien « 5 % déduit » (marché/cagnottes) et le 0 % adhésion. **99-coin (T99CP) inchangé : 0 frais plateforme** (seul coût = gas Polygon). Calcul centralisé dans `lib/payments/frais.ts` (`calculerFraisEuros` = 3 % + 0,30 €, nouveau `totalAvecFraisEuros`, `calculerFraisT99CP` → 0n), réutilisable par les flux futurs. 3 flux euros branchés mis à jour (adhésion/cagnottes/marché) + 3 formulaires affichent le surcoût. **1030 tests verts**, typecheck + lint verts. Manifest : `docs/manifests/frais-stripe-3pct-030.md`. **Reste** : vérif E2E navigateur des 3 formulaires (Stripe en mock) ; brancher les flux euros non encore construits (don réseau/médias, entraide payante) le moment venu — ils appelleront le même helper. **Contexte câblage 2026-06-09** : token Cloudflare en place (`CLOUDFLARE_API_TOKEN` dans `.env.local`, déploiement Workers/OpenNext débloqué) ; Brevo SMTP vérifié OK (Supabase Auth → smtp-relay.brevo.com) — penser à corriger `site_url` Supabase (encore `localhost:3000`) avant la prod.
**Dernier chantier terminé (antérieur)** : **polish final post-revue (V2.6.66→V2.6.71)** sur `main` (tip `279e06a`). Solde les items Q1-Q6 + manques mineurs de l'audit `docs/revue/08`. Tout local, additif, **aucune migration**, rien poussé au distant. **P1** (`1273359`) modales `max-h-[90dvh]` (alignées C18) + `ogType:'article'` sur la fiche journal. **P2** (`c4f0ec2`) Q1 (réutilise le type `StatutReservation` au lieu de 5 réinlines), Q4 (2 variantes `formaterEurosEntier`/`formaterEurosDecimales` dans `lib/format-euros.ts` qui répliquent EXACTEMENT les styles inline ; 8 fichiers migrés ; affichage identique prouvé par 6 tests d'équivalence), Q5 (`estIbanValide` branché dans le Zod du reversement). **P3** (`0590268`) Q3 (helper `exigerSession()` appliqué à ~40 gardes dans 16 fichiers `app/actions/*`, messages préservés, `session` extrait seulement si utilisé) + Q6 (`executerTransitionDemandeur` factorise les 3 transitions demandeur de `reservation.ts`, comportement et messages préservés). **P4** (`b673fcc`) Q2 (`LIBELLE_TYPE_CAGNOTTE` centralisé depuis 3 copies ; `LIBELLE_VISIBILITE_SALLE` ajouté à `lib/decider.ts`, console admin Décider harmonisée vers la forme longue canonique). **P5** (`285adb2`) retrait des 3 stubs orphelins `components/home/Page*Stub.tsx` (~180 lignes) + `export-claudeai/` rangé sous `docs/archives/` (reste gitignoré). **P6** (`279e06a`) wallet 99-coin de la vendeureuse affiché sur l'achat marché (`FormulaireAchat`, modèle encadré cagnotte) via helper `walletVendeureuse` en requête séparée défensive (dégradation propre à `null` si la colonne est absente sur le distant). **1027 tests verts** (+6), lint (18 warnings préexistants)/typecheck/build verts. Manifest : `docs/manifests/v2-6-polish-final-revue.md`. **Restes signalés (non bloquants)** : helpers musée `lib/siret.ts`/`lib/distance-gps.ts` (à câbler ou retirer, arbitrage Lilou/Ben) ; `V2/` à ranger manuellement (verrou Windows sur le zip) ; S3 (révoquer `SUPABASE_ACCESS_TOKEN`). Migrations en attente Phase M **inchangées**.
**Dernier chantier terminé (antérieur)** : V2.6.63 (**alignement des paiements 99-coin sur la doctrine §19** + fondation wallet) sur `main` (tip `b4fd641`). Arc de 4 chantiers : **V2.6.60 (C17)** adhésion 99-coin sans wallet intégré (tx_hash obligatoire, redirection the99coinproject.org, garde-fou `enregistrerHashConsomme` branché pour la 1ʳᵉ fois) ; **V2.6.61** marché 99-coin aligné pareil ; **V2.6.62** référence wallet sur le profil (migration ADDITIVE `20260602170000_personne_wallet_t99cp.sql`, colonne `wallet_t99cp` + CHECK 0x+40hex, **appliquée+vérifiée LOCAL** ; champ éditable dans `/profil/informations`) ; **V2.6.63** SEL : retrait du faux versement trésorerie (code mort `crediterPrestationsEnAttente`+`envoyerTransaction`, superseder par la réservation générique V2.2.2). **Résultat : plus AUCUNE simulation de transaction T99CP dans le code applicatif.** **DÉCISION ARCHITECTURE PAIEMENTS (Lilou/Ben 2026-05-31, en mémoire `paiements-architecture.md`)** : tout se paie entre membres (Stripe Connect en €, wallet 0x en 99-coin) SAUF 4 exceptions plateforme (don réseau social, don Maintenant Médias, adhésion, cagnottes plateforme) ; wallet obligatoire pour cagnotte, facultatif si offre gratuite. **FEATURE À VENIR (pas un nettoyage, transversale, touche modèle éco + argent = porte)** : intégrer le paiement 99-coin entre membres au flux de réservation générique (étape « confirmee » : bénéficiaire paie la prestataire vers son wallet profil + colle le reçu + garde-fou type 'sel'), pour SEL/prêt/hébergement/covoiturage payants. Décisions actées : wallet sur le profil, bénéficiaire colle le reçu. Petit reste : afficher le wallet vendeur sur l'achat marché. 1021 tests verts. **Avant (même date)** : V2.6.54 (D5 : frais de port marché), V2.6.55→59 (revue : C24 zodResolver, C26 aria-label, nettoyage flèches publiées + statut). **Migrations en attente Phase M** : D1 `compter_membres_actifs`, C5 `afficher_nom`, C13 `organisation_numero_orm`, C14 `signature_compte_snapshot`, C15 `contenu_organisation_soft_delete`, D5 `produit_marche_frais_port`, wallet `personne_wallet_t99cp` (+ index C28). **S3 reste à faire par Lilou/Ben** : révoquer `SUPABASE_ACCESS_TOKEN`.
**Dernier chantier terminé (antérieur)** : V2.6.54 (décision **D5 : frais de port marché**) sur `main` (tip `5c9dfc5`). Migration ADDITIVE `20260602160000_produit_marche_frais_port.sql` (colonne `frais_port_centimes` default 0, CHECK 0..100000), **appliquée + vérifiée LOCAL** (test transactionnel : total envoi 2190, bornes rejetées, rollback), **distant en Phase M**. Helper pur `lib/marche/port.ts` (`portFactureCentimes`, partagé action+formulaire, +7 tests → **1020 verts**). Champ port en création (conditionnel envoi) ; choix de remise + récap total euros + note POL + **alerte « Prévois du POL »** dans `FormulaireAchat` ; port ajouté au total Stripe (euros) sans commission sur le port (CDC §Commission) ; pour le 99-coin, port réglé en POL au taux du moment (référence affichée + journalisée, conversion réelle = chantier wallet T99CP). **Non-régression par construction** : default 0 + helper renvoyant 0 + dégradation propre sur le distant (colonne absente avant Phase M = `undefined` traité comme 0). Manifest `docs/manifests/v2-6-54-D5-frais-de-port-marche.md`. **Écart V1→V2 signalé (non touché)** : frais plateforme 5 % marché vs CDC « pas de commission » (préexistant, hors périmètre D5). **Migrations en attente Phase M** : D1 `compter_membres_actifs`, C5 `afficher_nom`, C13 `organisation_numero_orm`, C14 `signature_compte_snapshot`, C15 `contenu_organisation_soft_delete`, D5 `produit_marche_frais_port` (+ index C28). **S3 reste à faire par Lilou/Ben** : révoquer `SUPABASE_ACCESS_TOKEN`.
**Dernier chantier terminé (antérieur)** : chantier B (organisations + mandat), V2.6.13→V2.6.16. Spec `docs/specs/09_RESEAU-SOCIAL-V2.md` §7 ; manifest `docs/manifests/v2-6-13-a-16-chantier-B-organisations.md`. **B.1** entité `organisation` (espace_type de plus, page suivable, création). **B.2** rôle `gestionnaire_espace` polymorphe + badge officiel (admin) + cooptation + panneau de gestion. **B.3** revendications concurrentes (file d'attente) + console admin `/admin/national/organisations`. **B.4** contenus « Portés par » une organisation (table polymorphe `contenu_organisation`, sur pétition/cagnotte/mobilisation/campagne). 4 migrations (`20260601100000`→`130000`). **L'épopée réseau V2 est complète** (A, A.2b, C, D, B). **DÉPLOIEMENT DISTANT** : depuis le 30/05, mode « tout sur Supabase » (décision Lilou/Ben). Toutes les migrations V2.6 (commentaires, amitié, messagerie, flux, organisations) sont **appliquées au distant Francfort**, via l'API Management + jeton perso (dans `.env.local`, à révoquer en fin de session). Données vérifiées intactes (17 746 signatures, 35 011 communes, 15 737 profils). Sauvegarde REST avant push (`scripts/backup-rest.ts`). Refinements signalés non bloquants : création-auto d'orga à la déclaration au lancement (§7.3), types `sondage`/`moment` non câblés en UI.
**Dernier chantier terminé (antérieur)** : cycle V2.6.1→V2.6.11 (épopée réseau social V2 A/A.2b/C/D) : sur `main` (tip `b909e6e`). Spec : `docs/specs/09_RESEAU-SOCIAL-V2.md`. Manifest : `docs/manifests/v2-6-cycle-reseau-social-v2.md`. **Chantier A** (commentaires polymorphes `commentaire_objet` sur 10 contenus). **Chantier A.2b** (auteur·ice cliquable → profil réseau sur 10 fiches via `LienAuteurReseau` + `numeroReseauDe`). **Chantier C** (bouton « Suivre dans le réseau » sur fédération/GT/groupe local/campagne, réutilise `abonnement_espace_reseau`). **Chantier D** : amitié STOCKÉE (table `amitie`, cycle demande→acceptation, distincte du suivi ; `est_ami_reseau` réimplémenté + backfill des suivis mutuels existants ; Server Actions demanderAmi/accepterAmi/refuserAmi/retirerAmi ; `BoutonAmitie` + page `/s-informer/reseau/amis`), préférences `demande_ami_ouverte`/`messagerie_ouverte` (top-level `preferences_visibilite`), messagerie VERROUILLÉE (ami·es par défaut, helper `peut_envoyer_message_reseau` + RLS durcie), flux RE-CLASSÉ (moi → ami·es → ami·es d'ami·es → suivi·es → reste, RPC `cercle_amical_reseau`). **3 nouvelles migrations LOCALES** (`20260601000000_amitie`, `20260601010000_message_reseau_verrou`, `20260601020000_cercle_amical_reseau`) à appliquer au distant au matin (Phase M) ; dégradation propre en attendant (V2.6.11). **Écarts V1→V2 signalés** (amitié stockée, messagerie resserrée, flux ré-ordonné) : voir manifest. **Chantier B** (organisations + mandat) NON entrepris : porte de gouvernance, à concevoir avec Lilou/Ben. 1009 tests verts. **Bonus** : `mettreAJourPreferencesVisibilite` fusionne désormais le jsonb (corrige une perte silencieuse). 
**Dernier chantier terminé (antérieur)** : cycle V2.5.57→V2.5.63 (audit accessibilité exhaustif) : sur `main` (tip `38f6edd`). Audit WCAG 2.1 AA de TOUT le site (156 pages, 7 layouts, 134 composants) avec exigence admin/membre malvoyant·e (lecteur d'écran + clavier). 7 commits : lien d'évitement (`LienEvitement` + CMS) ; éditeur rich text TipTap nommé (`labelA11y`) ; champs filtre/recherche admin étiquetés ; boutons de modération nommés par objet (`libelleObjet`) ; erreurs de formulaire reliées (`aria-invalid`/`aria-describedby`) ; annonces vocales `aria-live` sur ~23 composants ; libellés humains des enums ; intitulés sr-only des données admin ; QR 2FA accessible ; menu profil downgrade ; CartePost confirmation inline ; icônes décoratives `aria-hidden` ; `<dl>` corrigés. **Bug fonctionnel corrigé** : scroll de fond jamais bloqué derrière la modale de signature. Aucune nouvelle dépendance. 1005 tests verts, lint+typecheck verts, smoke test dev OK. Détail exhaustif : `docs/audits/a11y-audit-2026-05-29.md` ; manifest : `docs/manifests/v2-5-57-a-63-audit-accessibilite-exhaustif.md`.
**Dernier chantier terminé (antérieur)** : V2.5.56 (centralisation formatteurs de date) : sur `main` (tip `1c732aa`). 2 nouveaux helpers (`formaterDateMoyenne`, `formaterDateLongueHeure`) + 12 pages migrées (32 sites d'appel), formats identiques garantis. 3 tests ajoutés (1005 verts). Voir `docs/manifests/v2-5-56-centralisation-formatteurs-date.md`.
**Dernier chantier terminé (antérieur)** : V2.5.55 (centralisation rendu rich text pages de détail) : sur `main` (tip `81acacb`). Refactor pur, 7 fichiers / 8 blocs, net -36 lignes. Les pages de détail (cagnottes, campagnes, mobilisations, pétitions, journal, OJ+PV Décider, bio réseau) rendaient leur HTML via un `dangerouslySetInnerHTML` inline avec une classe `prose` dupliquée et incomplète (sans style pour images, embeds, tables, code). Toutes migrées vers `<RenduRiche>` (chemin canonique, typographie complète). Aucune perte de comportement, fallbacks texte/Markdown conservés. La bio réseau garde sa contrainte `max-w-2xl` via un parent. 1002 tests verts, lint+typecheck verts, smoke test dev OK. Voir `docs/manifests/v2-5-55-rendu-riche-pages-detail.md`.
**Dernier chantier terminé (antérieur)** : V2.5.53 (rich text infrastructure pétitions + cagnottes) — sur `main` (tip `9ae43b3`). Migration `20260530920000_petition_cagnotte_texte_html.sql` (2 ALTER TABLE additifs). Server Actions + types + rendu pages enrichi. **Toutes les 6 tables avec texte long ont désormais une version HTML riche optionnelle**.
**Dernier chantier terminé (antérieur)** : V2.5.52 (rich text infrastructure mobilisations) — sur `main` (tip `8767762`). Migration `20260530910000_mobilisation_description_html.sql`. Schema Zod + Server Action + types + rendu page priorise HTML.
**Dernier chantier terminé (antérieur)** : V2.5.51 (UI rich text dans `FormulaireCreationCampagne`) — sur `main` (tip `7532ade`). Switch Riche/Markdown, pré-remplissage au bascule. Texte plat reste obligatoire (résumé/SEO) mais devient pliable en mode Riche.
**Dernier chantier terminé (antérieur)** : V2.5.50 (rich text infrastructure campagne) — sur `main` (tip `fafaa75`). Migration `20260530900000_campagne_texte_html.sql`. Server Action + types + rendu page enrichi.
**Dernier chantier terminé (antérieur)** : V2.5.49 (bio rich text profil personne) — sur `main` (tip `7a869e1`). 2 migrations (`20260530800000_personne_bio_html.sql` + `20260530810000_personne_affichage_bio_html.sql` qui étend la RPC `personne_affichage` avec `bio_html`). UI complète dans `FormulaireInformations` + rendu sur profil réseau.
**Dernier chantier terminé (antérieur)** : V2.5.48 (recherche console CMS étendue à `valeur_html`) — sur `main` (tip `0fbf926`). Strip des balises HTML pour matching textuel sur la version riche.
**Dernier chantier terminé (antérieur)** : V2.5.47 (maj CLAUDE.md cycle V2.5.40→V2.5.46) — sur `main` (tip `1a5b3be`).
**Dernier chantier terminé (antérieur)** : V2.5.46 (migration 2 dernières pages stub vers PageEditorialeCMS) — sur `main` (tip `2bbd46d`). `comprendre/doctrine` et `a-propos` étaient encore sur `PageEditorialeStub`. Migrés sur `PageEditorialeCMS` → mode rich text activé + édition admin inline + bascule Markdown/Riche. 100 % des pages éditoriales sur CMS.
**Dernier chantier terminé (antérieur)** : V2.5.45 (`RenduContenuAuto` Server Component) — sur `main` (tip `9d4672c`). Wrapper de commodité au-dessus de `RenduRiche` qui évite de dupliquer le pattern `lireContenuEditorial` + choix md/html. Permet de migrer progressivement les usages directs de `MarkdownLeger`.
**Dernier chantier terminé (antérieur)** : V2.5.44 (page démo rich text `/admin/national/rich-text-demo`) — sur `main` (tip `01c6225`). 4 sections (rendu complet, sanitization anti-XSS, où c'est branché, allowlist). Lien ajouté dans la nav admin nationale.
**Dernier chantier terminé (antérieur)** : V2.5.43 (barre de progression rich text dans console CMS) — sur `main` (tip `9ff6734`). Compte rich text vs markdown, ratio %, mini-barre avec aria. Permet à Lilou/Ben de suivre l'avancée de la migration.
**Dernier chantier terminé (antérieur)** : V2.5.42 (tests invariants prefs notif réseau — cap 1000 franchi) — sur `main` (tip `a302bb2`). 9 tests sur `modeNotifReseauSchema`, `PREFERENCES_NOTIFICATIONS_DEFAUT`, `preferencesNotificationsSchema` strict. **1002 tests verts**.
**Dernier chantier terminé (antérieur)** : V2.5.41 (script `convertir-tout-en-riche.ts`) — sur `main` (tip `ad2dd08`). Sous-chantier V2.5.40.b. Bascule en masse les `valeur_md` → `valeur_html` via convertisseur + sanitize. Idempotent, `--dry-run`/`--confirm`.
**Dernier chantier terminé (antérieur)** : V2.5.40 (manifest groupé cycle V2.5.31→V2.5.39) — sur `main` (tip `36d4e1a`). Manifest unifié dans `docs/manifests/v2-5-31-a-39-cycle-extension-rich-text-et-prefs-reseau.md`.
**Dernier chantier terminé (antérieur)** : V2.5.39 (branchement routage cloche/email selon prefs réseau) — sur `main` (tip `572fd41`). Sous-chantier V2.5.30.a partie 2/2. Nouveau `lib/preference-notif-reseau.ts` lit la pref de la personne destinataire. `poserNotificationTemplee` route : `aucune` → early return / `cloche` → cloche seule / `mail_immediat` → cloche + email via `envoyerEmailTemplee`. Pas de régression sur les autres types de notifs.
**Dernier chantier terminé (antérieur)** : V2.5.38 (prefs notifs réseau email — schéma + UI) — sur `main` (tip `c6762a3`). Sous-chantier V2.5.30.a partie 1/2. Schéma `preferencesNotificationsSchema` étendu avec `reseau_message_recu`/`reseau_post_commente`/`reseau_post_soutenu` × 5 modes (cloche/mail_immediat/digest_quotidien/digest_hebdo/aucune). UI nouvelle section dans `/profil/notifications` avec 3 sélecteurs. Page de lecture fait un merge avec DEFAUT pour ne pas écraser les vieilles prefs.
**Dernier chantier terminé (antérieur)** : V2.5.37 (rich text UI Décider OJ + PV) — sur `main` (tip `0aac684`). Complète V2.5.35 (infrastructure). `FormulaireMajReunion` gagne `SwitchMode` réutilisable, OJ et PV éditables en Riche ou Markdown indépendamment. Page de rendu priorise contenu_html.
**Dernier chantier terminé (antérieur)** : V2.5.36 (tests intégration md→html→sanitize) — sur `main` (tip `c887bee`). 2 tests anti-régression du pipeline réel. 993 tests verts.
**Dernier chantier terminé (antérieur)** : V2.5.35 (infrastructure rich text Décider OJ + PV) — sur `main` (tip `03f05c7`). Migration `20260530700000_reunion_decider_html.sql` (additive). Server Actions `creerReunion`/`majReunion` acceptent les champs HTML optionnels avec sanitization. UI au V2.5.37.
**Dernier chantier terminé (antérieur)** : V2.5.34 (mise à jour CLAUDE.md V2.5.30→V2.5.33) — sur `main` (tip `00f760c`).
**Dernier chantier terminé (antérieur)** : V2.5.33 (rich text étendu au journal-affiche) — sur `main` (tip `6eb39a1`). Migration `20260530600000_journal_affiche_contenu_html.sql` (additive). `mettreAJourEditionAction`/`creerEditionJournalAction` acceptent `contenu_html` avec sanitization. `FormulaireMajEdition` gagne le switch Riche/Markdown avec pré-remplissage automatique au bascule. Page article priorise `contenu_html`, fallback `contenu_md`. 991 tests verts.
**Dernier chantier terminé (antérieur)** : V2.5.32 (badge Riche + aperçu HTML strippé dans console CMS) — sur `main` (tip `05b0e48`). Badge brand-tint « Riche » à côté de la clé quand `valeur_html` posé. Aperçu textuel intelligent (strip balises). Compteur de caractères sur la version active.
**Dernier chantier terminé (antérieur)** : V2.5.31 (convertisseur Markdown→HTML au bascule Riche) — sur `main` (tip `82f3e29`). Sous-chantier V2.5.30.c. `lib/rich-text/markdown-vers-html.ts` retourne du HTML minimal compatible TipTap. Au bascule Markdown→Riche, le HTML est pré-rempli avec la conversion du Markdown courant — plus de perte de contenu. 16 nouveaux tests (991 verts total).
**Dernier chantier terminé (antérieur)** : V2.5.30 (manifest groupé cycle V2.5.23→V2.5.29 : rich text + logo + hotfix) — sur `main` (tip `dcd57dd`). Manifest unifié dans `docs/manifests/v2-5-23-a-29-cycle-rich-text-logo.md`.
**Dernier chantier terminé (antérieur)** : V2.5.29 (mise à jour CLAUDE.md cycle rich text) — sur `main` (tip `96cacc7`).
**Dernier chantier terminé (antérieur)** : V2.5.28 (tests sécurité `sanitizeRichHtml` — 18 tests d'allowlist positive et négative anti-XSS) — sur `main` (tip `526d9cd`). 975 tests verts (957 + 18).
**Dernier chantier terminé (antérieur)** : V2.5.27 (sous-chantier V2.5.16.d templates email réseau) — sur `main` (tip `efc9d70`). `TypeEmail` étendu avec `reseau_message_recu`/`reseau_post_commente`/`reseau_post_soutenu`. Templates définis + 3 nouvelles previews iframe dans `/admin/national/emails-preview`. **Pas appelés automatiquement** (anti-spam) : un futur système de préférences utilisateurice gérera l'opt-in.
**Dernier chantier terminé (antérieur)** : V2.5.26 (mode rich text dans console CMS admin) — sur `main` (tip `5188d76`). `EditeurInlineCMS` gagne le switch Riche/Markdown comme `ContenuEditableAdmin`. `ContenuListe` propage `valeurHtml`. La console permet désormais d'éditer en rich text n'importe quelle clé CMS (libellés courts, intros, corps d'emails).
**Dernier chantier terminé (antérieur)** : V2.5.25 (rich text branché sur PageEditorialeCMS + emails + hotfix CMS auth) — sur `main` (tip `54c0dc9`). `ContenuEditableAdmin` gère 2 modes (Markdown/Riche). `envoyerEmailTemplee` consomme `valeur_html` prioritairement. **Hotfix critical** : la Server Action `mettreAJourContenuEditorialAction` fallback gracieusement sur `est_admin_general` quand le RPC `peut_editer_cms` n'existe pas (cas du distant, migration V2.5.15 jamais appliquée). Restaure le droit d'édition CMS pour Lilou/Ben sur le distant. Server Action permet aussi les mises à jour partielles (md seul OU html seul).
**Dernier chantier terminé (antérieur)** : V2.5.24 (logo officiel intégré footer/emails/adhésion/auth) — sur `main` (tip `733befc`). Logo poing levé+coquelicot fourni par Lilou/Ben le 2026-05-29, stocké en `public/logo/maintenant.png`. Intégré dans 4 emplacements : Footer (remplace wordmark texte), gabarit email (bandeau identitaire), layout (auth) (header + grand format au-dessus du formulaire), page d'adhésion (bandeau d'entrée).
**Dernier chantier terminé (antérieur)** : V2.5.23 (fondations rich text + fix hydration button) — sur `main` (tip `7b6a5fa`). Migration `20260530500000_contenu_editorial_html.sql` (colonne `valeur_html` nullable additive). `lib/rich-text/sanitize.ts` avec allowlist stricte sanitize-html. 4 composants TipTap : `EditeurRiche`, `BarreOutilsRiche`, `EditeurRicheAvecToolbar`, `RenduRiche`. **Fix bonus inclus** : `BoutonEditerInline` passe de `<button>` à `<span role="button">`, résout l'erreur d'hydration button-dans-button quand `TexteEditableAdmin` est utilisé dans le `<button>` déclencheur de `ModaleSignaturePetition`.
**Cycle V2.5.23→V2.5.28 (résumé)** : chantier rich text complet en 6 commits. Toutes les zones éditables CMS (libellés, intros, articles éditoriaux, corps d'emails) peuvent désormais être éditées en mode riche (couleurs, polices, tailles, listes, citations, alignement, lien, image, embed YouTube/Vimeo/Spotify/SoundCloud/PeerTube) OU en Markdown au choix. HTML sanitizé à l'enregistrement (allowlist stricte de balises/attributs/CSS/iframe-hostnames, 18 tests de sécurité). Migration `20260530500000_contenu_editorial_html.sql` (additive) à appliquer au distant via `supabase db push` au matin (Phase M).
**Dernier chantier terminé (antérieur)** : V2.4.154 (libellés UI éditables sur 3 formulaires hors-pattern) — sur `main` (tip `9efa40f`). Branche `libelles?` sur `fil-groupe/FormulairePosterMessage` (9 clés), `groupe-entraide-local/FormulaireCreationGroupeEntraide` (18 clés), `reseau/ModaleMessage` (12 clés, interpolation `{nom}` pour personnaliser au destinataire). Pattern uniforme désormais sur 100 % des formulaires UI du codebase. 912 tests verts.
**Dernier chantier terminé (antérieur)** : V2.4.153 (libellés UI sur 2 formulaires admin) — sur `main` (tip `45d1a99`). `admin/national/FormulaireAccorderDroit` (21 clés) + `admin/tresorerie/FormulaireInitierReversement` (19 clés). Les sous-composants `RechercheBeneficiaire`/`RechercheCommune` reçoivent les placeholders/aria via props (pas de prop drilling).
**Dernier chantier terminé (antérieur)** : V2.4.152 (libellés UI sur 7 formulaires restants Zod+RHF) — sur `main` (tip `23349c5`). FormulaireNotation, FormulaireRetrait mobilisation, FormulaireModeration pétition, FormulaireModerationCampagne, FormulaireEditionPetition, FormulaireCreationBoutique, FormulaireCreationMinimarche. ~80 nouvelles clés UI éditables.
**Cycle V2.4.149→V2.4.154 (résumé)** : 24 formulaires couverts, ~280 nouvelles clés UI éditables admin via CMS. Pattern stable et exporté : interface `LibellesXxx` + constante `LIBELLES_DEFAUT` en fallback + prop optionnel surchargeable par la page parente. Pour les chaînes paramétrées, interpolation `.replace('{n}', ...)` simple sans bibliothèque ICU.
**Dernier chantier terminé (antérieur)** : V2.4.90 (`lib/helpers-purs.ts` index unifié) — sur `main` (tip `c3837f1`). Point d'export unique pour les 38 helpers purs accumulés V2.4.28-V2.4.89, organisés en 11 sections (identité, formatage, sécurité, texte, collections, géo, couleur, URL, CSV, business, timestamp, log).
**Dernier chantier terminé (antérieur)** : V2.4.89 (`lib/timestamp.ts` + 13 tests) — sur `main` (tip `b419129`). 912 tests verts (cap 900 franchi). `isoEnSecondes`/`secondesEnIso`/`maintenantEnSecondes`/`estTimestampValide` (détecte le piège classique ms-au-lieu-de-s).
**Dernier chantier terminé (antérieur)** : V2.4.88 (`lib/generer-password.ts` + 13 tests) — sur `main` (tip `5696000`). 899 tests verts. 3 générateurs via `crypto.getRandomValues` : password, code numérique, token URL-safe anti-confusion.
**Dernier chantier terminé (antérieur)** : V2.4.87 (`lib/validation-url.ts` + 15 tests) — sur `main` (tip `03f1c39`). `estUrlValide` (refuse javascript:/data:/file:), allowlist domaines, `parserUrl`.
**Dernier chantier terminé (antérieur)** : V2.4.86 (`lib/normaliser-recherche.ts` + 15 tests) — sur `main` (tip `c0a5899`). `normaliserRecherche`, `contientTexte` (match permissif accent+casse), `comparerPermissif` (tri).
**Dernier chantier terminé (antérieur)** : V2.4.85 (composant `Skeleton` UI) — sur `main` (tip `f7e672a`). 3 variantes (`Skeleton`, `SkeletonText`, `SkeletonCarte`), animate-pulse, aria-busy.
**Dernier chantier terminé (antérieur)** : V2.4.84 (`lib/force-password.ts` + 13 tests) — sur `main` (tip `1e0b095`). 6 critères → score 0-6, 5 niveaux, suggestions FR. UX uniquement (sécurité = double opt-in).
**Dernier chantier terminé (antérieur)** : V2.4.83 (`lib/duree.ts` + 15 tests) — sur `main` (tip `226ffed`). `formaterDureeCompacte` 2 unités max, `formaterDureeLongue` pluralisée.
**Dernier chantier terminé (antérieur)** : V2.4.82 (`lib/log-erreur.ts` + 14 tests) — sur `main` (tip `0b7dcf8`). 828 tests verts. Logger centralisé, format structuré, échec silencieux pour ne pas casser l'appelant.
**Dernier chantier terminé (antérieur)** : V2.4.81 (`lib/ago-compact.ts` + 7 tests) — sur `main` (tip `9688c2f`). Variante compacte de `formaterRelativePassee` : « 5min », « 3h », « 2j », « 4mo », « 1a ».
**Dernier chantier terminé (antérieur)** : V2.4.80 (composant `Avatar` UI + branchement admin personnes) — sur `main` (tip `2534b44`). 3 tailles (32/48/64px), photo ou initiales+couleur HSL déterministe, exporté depuis `components/ui`.
**Dernier chantier terminé (antérieur)** : V2.4.79 (`lib/sha256.ts` Web Crypto + 10 tests) — sur `main` (tip `cf310a6`). 807 tests verts. SHA-256 natif Node 16+/Edge, vecteurs officiels.
**Dernier chantier terminé (antérieur)** : V2.4.78 (`lib/avatar-couleur.ts` + 10 tests) — sur `main` (tip `10109a1`). `avatarHsl` déterministe via FNV-1a + `initialesPourAvatar` (2 chars max).
**Dernier chantier terminé (antérieur)** : V2.4.77 (`lib/group-by.ts` + 10 tests) — sur `main` (tip `81fda8a`). `groupBy`, `groupByObjet`, `countBy`, génériques typés.
**Dernier chantier terminé (antérieur)** : V2.4.76 (`lib/siret.ts` + 15 tests, Luhn) — sur `main` (tip `51861ef`). 779 tests verts. SIREN/SIRET INSEE officiels + format groupes.
**Dernier chantier terminé (antérieur)** : V2.4.75 (`lib/pourcentage.ts` + 14 tests) — sur `main` (tip `278cf65`). 4 helpers : `pourcentage` (safe), `Arrondi`, `Clampe` (0-100 pour CSS), `formaterPourcentage`.
**Dernier chantier terminé (antérieur)** : V2.4.74 (`lib/chunk.ts` + 11 tests) — sur `main` (tip `bf3fa04`). Découpage en batch pour `in (...)` Supabase / envois mail / écritures, plus `nbChunks`.
**Dernier chantier terminé (antérieur)** : V2.4.73 (`lib/hash.ts` FNV-1a + 10 tests) — sur `main` (tip `a28a0e1`). 739 tests verts. Hash non-crypto 32 bits pour cache-busting / ETag, vecteurs officiels validés.
**Dernier chantier terminé (antérieur)** : V2.4.72 (`lib/conversion-99coin.ts` + 12 tests) — sur `main` (tip `833d6db`). Convention V1 : 1 T99CP = 10 ¢ (taux configurable). `coinsEnCentimes`, `centimesEnCoins`, `totalCentimes`.
**Dernier chantier terminé (antérieur)** : V2.4.71 (`lib/validation-fichier.ts` + 13 tests) — sur `main` (tip `8be7cce`). `validerFichier` retourne `{ok, code?, message?}` avec 4 codes possibles, `TAILLES` constante, `mimeAutorise`.
**Dernier chantier terminé (antérieur)** : V2.4.70 (`lib/couleur-hex.ts` + 25 tests) — sur `main` (tip `3a95160`). 704 tests verts. 5 helpers : `estHexValide`, `normaliserHex`, `hexEnRgb`, `luminanceRelative` (WCAG), `contrastTexte` (seuil 0.179).
**Dernier chantier terminé (antérieur)** : V2.4.69 (`lib/iban.ts` + 14 tests) — sur `main` (tip `da35468`). ISO 13616 mod 97, longueurs par pays (12 pays), `normaliserIban`/`estIbanValide`/`formaterIban`.
**Dernier chantier terminé (antérieur)** : V2.4.68 (Pagination sur admin communes — 35 011 lignes) — sur `main` (tip `84e6299`). 2ᵉ usage Pagination, PAR_PAGE=50, vraiment utile sur le référentiel des communes.
**Dernier chantier terminé (antérieur)** : V2.4.67 (branchement `Pagination` sur admin personnes) — sur `main` (tip `641a62a`). Premier vrai usage du composant : `listerPersonnesAdminPagine` retourne `{lignes, total}`, PAR_PAGE=50, count exact, second appel ciblé si page>1.
**Dernier chantier terminé (antérieur)** : V2.4.66 (exports CSV moments + médias + sondages) — sur `main` (tip `c8e94ef`). 3 routes admin, BOM UTF-8, filename horodaté.
**Dernier chantier terminé (antérieur)** : V2.4.65 (`lib/capitalisation.ts` + 15 tests) — sur `main` (tip `4a2e813`). 665 tests verts. 3 helpers : `capitaliser`, `titreCase` (Title Case avec séparateurs unicode), `decapitaliser`.
**Dernier chantier terminé (antérieur)** : V2.4.64 (`lib/uuid.ts` + 12 tests) — sur `main` (tip `fbc5d4a`). 650 tests verts. `estUuidValide` (v1-v5, case insensible) + `normaliserUuid`.
**Dernier chantier terminé (antérieur)** : V2.4.63 (`lib/code-postal-fr.ts` + 14 tests) — sur `main` (tip `ebe82a2`). `estCodePostalFrValide` + `extraireDepartementFr` (métropole 2 chiffres, DROM 97x/98x 3 chiffres).
**Dernier chantier terminé (antérieur)** : V2.4.62 (`lib/age.ts` + 12 tests) — sur `main` (tip `2c5d7f9`). `calculerAge` (tient compte mois/jour) + `estMajeur`.
**Dernier chantier terminé (antérieur)** : V2.4.61 (`lib/telephone-fr.ts` + 19 tests) — sur `main` (tip `4aca486`). 612 tests verts. 3 helpers : `estTelephoneFrValide`, `normaliserTelephoneFr`, `formaterTelephoneFr`.
**Dernier chantier terminé (antérieur)** : V2.4.60 (admin réservations `/admin/national/reservations`) — sur `main` (tip `9eab993`). Vue globale, recherche message, double filtre statut+type (cycle D8 complet), motif de décision en exergue.
**Dernier chantier terminé (antérieur)** : V2.4.59 (`lib/url.ts` + 12 tests) — sur `main` (tip `99a16ea`). 593 tests verts. 4 helpers : `urlAbsolue`, `lienPartageMailto`, `lienPartageMastodon`, `extraireDomaine`.
**Dernier chantier terminé (antérieur)** : V2.4.58 (composant `Pagination` UI réutilisable) — sur `main` (tip `a82ffc6`). Server Component branché sur `lib/pagination.ts`, fenêtre + ellipses, préserve les params de recherche, exporté depuis `components/ui`.
**Dernier chantier terminé (antérieur)** : V2.4.57 (admin campagnes `/admin/national/campagnes`) — sur `main` (tip `f88cc9e`). Filtres statut, raison de rejet visible.
**Dernier chantier terminé (antérieur)** : V2.4.56 (`estSlugValide` + 8 tests) — sur `main` (tip `ea7afea`). 581 tests verts. Validation idempotente des slugs en paramètre.
**Dernier chantier terminé (antérieur)** : V2.4.55 (`lib/pagination.ts` + 13 tests) — sur `main` (tip `2c9391f`). `paginer({page, parPage, total})` + `lirePageDepuisParams`, 1-indexé côté UI, traduit en range Supabase 0-indexé.
**Dernier chantier terminé (antérieur)** : V2.4.54 (admin médias `/admin/national/medias`) — sur `main` (tip `9345164`). Double filtre statut+type (9 types), vignette 96px, badge provenance externe.
**Dernier chantier terminé (antérieur)** : V2.4.53 (`lib/distance-gps.ts` + 10 tests, haversine) — sur `main` (tip `d13b115`). 560 tests verts. `distanceMetres` / `distanceKmArrondie` / `formaterDistance`. Utile pour cartes « près de moi ».
**Dernier chantier terminé (antérieur)** : V2.4.52 (RSS feed `/feed-journal.xml` journal-affiche) — sur `main` (tip `9fb68f3`). Distinct du flux principal, cache 1h.
**Dernier chantier terminé (antérieur)** : V2.4.51 (admin moments solidaires `/admin/national/moments`) — sur `main` (tip `fe6eca5`). Double filtre statut (6) + type (8), tri date décroissante.
**Dernier chantier terminé (antérieur)** : V2.4.50 (`lib/email-valide.ts` + 13 tests) — sur `main` (tip `4b26879`). 550 tests verts. Validation pragmatique + normalisation (trim+lowercase).
**Dernier chantier terminé (antérieur)** : V2.4.49 (`lib/temps-lecture.ts` + 11 tests + branchement) — sur `main` (tip `be9f190`). 200 mots/min, min 1 min, ligne d'attribution média gagne « · X min de lecture ».
**Dernier chantier terminé (antérieur)** : V2.4.48 (admin sondages `/admin/national/sondages`) — sur `main` (tip `29015d2`). Double filtre statut + mode, recherche titre/question, nb d'options.
**Dernier chantier terminé (antérieur)** : V2.4.47 (`lib/format-date.ts` + 7 tests) — sur `main` (tip `349a285`). 525 tests verts. 4 helpers centralisés : `formaterDateCourte`, `formaterDateLongue`, `formaterDateHeure`, `formaterDateIso`.
**Dernier chantier terminé (antérieur)** : V2.4.46 (healthcheck `/api/health`) — sur `main` (tip `fbdf253`). 200/503 selon état Supabase, JSON avec uptime + timestamp + checks, no-store.
**Dernier chantier terminé (antérieur)** : V2.4.45 (admin groupes d'entraide locaux) — sur `main` (tip `07d51b4`). Recherche par nom/zone, filtre statut, badges outils activés.
**Dernier chantier terminé (antérieur)** : V2.4.44 (`lib/format-t99cp.ts` + 10 tests) — sur `main` (tip `03a6d54`). 518 tests verts. Extraction du formaterT99CP avec BigInt pour préservation précision, suffixe configurable.
**Dernier chantier terminé (antérieur)** : V2.4.43 (`lib/texte-apercu.ts` + 10 tests) — sur `main` (tip `a979dd8`). 3 helpers : `tronquerCaracteres` (respect mots), `tronquerMots`, alias `apercu`.
**Dernier chantier terminé (antérieur)** : V2.4.42 (RSS feed `/feed.xml` + auto-discovery) — sur `main` (tip `8cb29ed`). 30 articles, format RSS 2.0 + Atom-compatible, cache CDN 15min, `<link rel="alternate" type="application/rss+xml">` dans le `<head>` du site.
**Dernier chantier terminé (antérieur)** : V2.4.41 (`lib/pluriel.ts` + 12 tests) — sur `main` (tip `654dc4e`). 498 tests verts. Helpers `accorder` et `compter` pour remplacer `${n > 1 ? 's' : ''}` répété partout.
**Dernier chantier terminé (antérieur)** : V2.4.40 (sitemap.xml dynamique + robots.txt) — sur `main` (tip `c3e17b7`). 35 pages statiques + URLs publiées agrégées en parallèle, lastmod = updated_at, cache CDN 1h ; robots.txt cache 24h.
**Dernier chantier terminé (antérieur)** : V2.4.39 (exports CSV cagnottes + mobilisations) — sur `main` (tip `ac95df1`). Cagnottes avec montants agrégés via `cagnotte_compteur`.
**Dernier chantier terminé (antérieur)** : V2.4.38 (export CSV pétitions) — sur `main` (tip `11a6bbf`). 8 colonnes avec `nombre_signatures` via jointure sur `petition_compteur`.
**Dernier chantier terminé (antérieur)** : V2.4.37 (exports CSV communes + fédérations) — sur `main` (tip `fd84beb`). 2 routes GET admin, BOM UTF-8 pour Excel, filename horodaté.
**Dernier chantier terminé (antérieur)** : V2.4.36 (`lib/export-csv.ts` RFC 4180 + 13 tests + endpoint export personnes) — sur `main` (tip `cf6ac54`). 486 tests verts.
**Dernier chantier terminé (antérieur)** : V2.4.35 (page admin `/admin/national/federations` filtrable) — sur `main` (tip `afb1bb3`). Compteur communes par fédération via 1 requête agrégée (anti N+1), filtres mot-clé + type.
**Dernier chantier terminé (antérieur)** : V2.4.34 (`lib/format-euros.ts` + 10 tests) — sur `main` (tip `4024f63`). 2 variantes `formaterEurosDepuisCentimes` + `formaterEuros`. Résilient à l'espace insécable étroite U+202F. 473 tests verts.
**Dernier chantier terminé (antérieur)** : V2.4.33 (page admin `/admin/national/communes` filtrable) — sur `main` (tip `c61a651`). 3 compteurs (Total/Libres/Coquilles), filtres mot-clé + département + statut, lien vers page publique.
**Dernier chantier terminé (antérieur)** : V2.4.32 (helper `formaterRelativePassee` + 15 tests + branchement dashboard) — sur `main` (tip `708dd82`). Dual de `formaterRelativeAVenir`, retourne « il y a X min/h/jours/mois/an(s) ». Dashboard membre : colonne date des activités récentes passe de date statique à temps relatif avec `title=` au survol.
**Dernier chantier terminé (antérieur)** : V2.4.31 (extraire `formaterTailleOctets` + 5 tests) — sur `main` (tip `718b29c`). 448 tests verts.
**Dernier chantier terminé (antérieur)** : V2.4.30 (extraire `trierParPertinence` + 7 tests) — sur `main` (tip `4a54cd1`). Non-mutation garantie, insensible casse, prefixe avant infixe, longueur à égalité.
**Dernier chantier terminé (antérieur)** : V2.4.29 (page admin `/admin/national/personnes` filtrable) — sur `main` (tip `6ab2bf8`). Recherche email/prénom/nom (ilike OR), filtre par statut (tous/actifs/anonymisés/suppression), badges visuels.
**Dernier chantier terminé (antérieur)** : V2.4.28 (helper `slugifier` commun + 9 tests) — sur `main` (tip `91a7506`). Extrait des slugify locaux Décider+Journal qui se répétaient. DRY -16 lignes.
**Dernier chantier terminé (antérieur)** : V2.4.27 (dashboard admin enrichi : état modération en tête) — sur `main` (tip `e813175`). Alerte « N éléments en attente » + 6 cartes file modération en haut de `/admin`.
**Dernier chantier terminé (antérieur)** : V2.4.26 (footer enrichi avec colonne Explorer) — sur `main` (tip `23a5bb5`). 4e colonne : Recherche / Agenda / Cartes / Décider / Maintenant Médias.
**Dernier chantier terminé (antérieur)** : V2.4.25 (bibliothèque d'images admin `/admin/national/images`) — sur `main` (tip `d0c0d58`). Liste fichiers Supabase Storage bucket `media`, filtres par préfixe, vignettes + métadonnées + URL clickable.
**Dernier chantier terminé (antérieur)** : V2.4.24 (recherche globale `/recherche`) — sur `main` (tip `494a47d`). 11 requêtes ilike parallèles sur pétition/mobilisation/cagnotte/commune/fédération/média/sondage/salle/journal/groupe/campagne, tri pertinence, icône Search dans header.
**Dernier chantier terminé (antérieur)** : V2.4.23 (compteur réunions à venir dans dashboard membre) — sur `main` (tip `9cd163f`). 16e requête parallèle dans `chargerDashboardMembre`, nouvelle carte « Réunions à venir » liée à `/profil/decider`.
**Dernier chantier terminé (antérieur)** : V2.4.22 (page `/profil/decider` Mes réunions Décider) — sur `main` (tip `69a76c4`). Prochaines réunions visibles (RLS) + dernières décisions. Dashboard membre lié à `Mes réunions Décider` + `Mes créations` (qui n'était pas lié).
**Dernier chantier terminé (antérieur)** : V2.4.21 (section « Décisions récentes » sur index Décider) — sur `main` (tip `77b990a`). Helper `listerDernieresReunionsAvecPV`, grille 2 colonnes des 6 dernières réunions terminées avec PV.
**Dernier chantier terminé (antérieur)** : V2.4.20 (prochaines réunions agrégées sur index Décider) — sur `main` (tip `b9eb4f5`). Helper `listerProchainesReunionsToutesSalles` avec jointure salle, 10 prochaines réunions cliquables.
**Dernier chantier terminé (antérieur)** : V2.4.19 (édition d'une édition journal existante admin) — sur `main` (tip `dc0effe`). Server Action `mettreAJourEditionAction` (6 champs partiels), formulaire inline visible aux admins sur `/s-informer/journal/[slug]`.
**Dernier chantier terminé (antérieur)** : V2.4.18 (page individuelle de réunion Décider + édition admin) — sur `main` (tip `592c6f8`). `/s-informer/decider/[slug]/[reunionId]` avec rendu Markdown OJ+PV, formulaire admin (statut/OJ/PV), helper `chargerReunionParId`, action `mettreAJourReunionAction`.
**Dernier chantier terminé (antérieur)** : V2.4.17 (téléversement image dans formulaire journal) — sur `main` (tip `e56e905`). Remplace le champ URL nu de la couverture par `ChampImageObjet` (V2.3.4) + `TeleverseurImage` (V2.0.3) ; téléversement direct via Supabase Storage, préfixe `journal-affiche`.
**Dernier chantier terminé (antérieur)** : V2.4.16 (extraire `MarkdownLeger` en module réutilisable) — sur `main` (tip `3eea44f`). `components/contenu/MarkdownLeger.tsx` partagé entre `ContenuEditableAdmin` (CMS) et la page individuelle du journal-affiche, qui rend maintenant le contenu en HTML structuré au lieu d'un `<pre>`.
**Dernier chantier terminé (antérieur)** : V2.4.15 (planifier réunion Décider inline admin) — sur `main` (tip `bfdf714`). Formulaire admin-only sur `/s-informer/decider/[slug]` (titre, début/fin, mode, ordre du jour) qui appelle `creerReunionAction` (V2.4.12), visible si `estAdminCourant() = true`.
**Dernier chantier terminé (antérieur)** : V2.4.14 (file de modération globale `/admin/moderation`) — sur `main` (tip `1e04f0f`). 15 compteurs « en attente d'action » par module, classés du plus urgent au moins urgent, badges danger/warning/default selon nombre, lien direct vers chaque console spécialisée. `lib/admin/file-moderation.ts` (Promise.all des compteurs `count exact head only`).
**Dernier chantier terminé (antérieur)** : V2.4.13 (`BoutonAdminEditer` systématique sur 9 pages d'espaces individuels) — sur `main` (tip `1b51896`).
**Chantiers V2.4 livrés (résumé)** : V2.4.1 CMS minimal, V2.4.2 `estAdminCourant`+`PageEditorialeCMS`, V2.4.3 cartes étendues+hébergement, V2.4.4 agenda agrégé+catch-all, V2.4.5 dashboard membre profond, V2.4.6 console contenus éditoriaux, V2.4.7 Mes créations (14 axes), V2.4.8 `BoutonAdminEditer`, V2.4.9 migration 10 pages CMS, V2.4.10 Décider MVP, V2.4.11 Journal-affiche MVP, V2.4.12 consoles admin Décider/Journal, V2.4.13 `BoutonAdminEditer` partout, V2.4.14 file modération globale, V2.4.15 planifier réunion Décider, V2.4.16 `MarkdownLeger` réutilisable, V2.4.17 upload image journal. **3 nouvelles migrations en attente distant** : `20260527130000_contenu_editorial.sql`, `20260527140000_decider.sql`, `20260527150000_journal_affiche.sql` (en plus des 10 V2.x déjà en attente). **13 migrations totales** à appliquer au matin.
**Dernier chantier terminé (antérieur)** : V2.3.46 (tests unitaires des helpers purs filtres + compter-membres) — 14 nouveaux tests, total 427 verts.
**Dernier chantier terminé (antérieur)** : V2.3.45 (lien recherche depuis header réseau).
**Dernier chantier terminé (antérieur)** : V2.3.44 (page recherche réseau social /s-informer/reseau/recherche) — personnes par numéro M+7/prénom + posts par texte.
**Dernier chantier terminé (antérieur)** : V2.3.43 (page admin audit journal D8bis /admin/national/audit) — lecture des 200 dernières transitions D8 avec auteur respectant visibilité.
**Dernier chantier terminé (antérieur)** : V2.3.42 (stats admin étendues V2) — 11 indicateurs ajoutés (caisses, soldes, transactions sortantes, réservations, membres campagnes/GT/groupes).
**Dernier chantier terminé (antérieur)** : V2.3.41 (helper compterMembresEspace + formaterMembres) — affichage « X membres » sur pages GT et campagne.
**Dernier chantier terminé (antérieur)** : V2.3.40 (filtre par type d'offre dashboards réservation) — 2e axe de filtre URL ?statut=X&type=Y.
**Dernier chantier terminé (antérieur)** : V2.3.39 (page index /co-construire liste des GT) + lien GT actif dans Mes groupes.
**Dernier chantier terminé (antérieur)** : V2.3.38 (page individuelle GT thématique /co-construire/[slug] + Rejoindre/Quitter).
**Dernier chantier terminé (antérieur)** : V2.3.37 (notif `moderation_me_concerne` sur retrait contenu réseau) — auteur du contenu reçoit une cloche avec la raison du retrait.
**Dernier chantier terminé (antérieur)** : V2.3.36 (confirmer/annuler transaction sortante côté admin) — Server Actions confirmerTransactionSortanteAction + annulerTransactionSortanteAction. Composant client BoutonsTransactionSortante branché dans détail caisse.
**Dernier chantier terminé (antérieur)** : V2.3.35 (téléchargement justificatif URL signée 60s) — Server Action obtenirUrlJustificatifAction + composant client LienJustificatif. Branché dans liste transactions sortantes.
**Dernier chantier terminé (antérieur)** : V2.3.34 (bouton Rejoindre/Quitter campagne) — Server Actions rejoindreCampagne + quitterCampagne. Branché dans /mobiliser/campagnes/[slug]. Utilise concrètement appartenance_campagne V2.3.29.
**Dernier chantier terminé (antérieur)** : V2.3.33 (UI initier reversement D12bis) — Server Action initierTransactionSortanteAction + FormulaireInitierReversement avec ChampDocument. Branché dans détail caisse.
**Dernier chantier terminé (antérieur)** : V2.3.32 (ChampDocument + adapter Storage justificatifs) — lib/storage/justificatifs/ (Mock + Supabase). app/actions/justificatif.ts (téléversement réservé admin). components/ui/ChampDocument.tsx (variante ChampImageObjet pour PDF). Bucket Supabase à créer manuellement.
**Dernier chantier terminé (antérieur)** : V2.3.31 (Mes contributions financières côté profil) — lib/mes-contributions.ts + section dans /profil/contributions qui lit transaction_entrante.payeur_personne_id. Totaux par canal.
**Dernier chantier terminé (antérieur)** : V2.3.30 (notifs réseau typées) — refactor poserNotification dans s-informer/reseau/actions.ts. Types reseau_post_commente / reseau_post_soutenu / reseau_message_recu.
**Dernier chantier terminé (antérieur)** : V2.3.29 (appartenance_campagne + Mes groupes 6 axes) — migration locale 20260527120000. Découverte que appartenance_groupe_entraide_local existait déjà en V1 (réutilisée). Page Mes groupes étendue à 6 axes.
**Dernier chantier terminé (antérieur)** : V2.3.28 (liste entrées dans détail caisse + script backfill) — section transaction_entrante dans /admin/national/tresorerie/[caisseId]. scripts/backfill-caisses.ts (--dry-run / --confirm, idempotent).
**Dernier chantier terminé (antérieur)** : V2.3.27 (branchement caisses ↔ flux V1) — lib/caisse-flux.ts (obtenirOuCreerCaisseGlobale/Cagnotte + poserEntreeCaisse idempotent 23505). Branchement dans confirmerDonEuros + faireDonT99CP + confirmerAdhesionEuros + adhererT99CP.
**Dernier chantier terminé (antérieur)** : V2.3.26 (transaction_entrante + solde caisse) — migration locale 20260527110000 (table avec source_type/source_id polymorphe + index unique anti-doublon). lib/caisse-solde.ts (calculerSoldeCaisse + variante batch). Solde affiché dans dashboard et détail caisse.
**Dernier chantier terminé (antérieur)** : V2.3.25 (cloche in-app + branchement D8) — réutilise table V1 notification chantier 8.1. lib/notification.ts + HeaderCloche + /profil/notifications-recues. Branchement dans toutes les Server Actions D8 (V2.3.13-21).
**Fix Ultraplan livré sur branche** : `fix/ci-playwright-multi-browser` (poussée sur GitHub, attend création PR). Diagnostic du doc Ultraplan appliqué intégralement (Playwright Firefox/WebKit filtrés via PLAYWRIGHT_FULL, env Supabase placeholders dans CI, workflow ci-cross-browser.yml mensuel ajouté).
**Dernier chantier terminé (antérieur)** : V2.3.24 (filtres par statut sur dashboard demandeur + factorisation `lib/reservation-filtres.ts`) — sur `main` (tip `d8c524f`). Voir `docs/manifests/v2-3-24-filtres-statut-demandeur.md`.
**Dernier chantier terminé (antérieur)** : V2.3.23 (filtres par statut sur dashboard propriétaire `/profil/demandes-reservations?statut=X`) — barre de 8 chips ronds avec compteurs, filtre côté TS. Voir `docs/manifests/v2-3-23-filtres-statut-dashboard-proprietaire.md`.
**Dernier chantier terminé (antérieur)** : V2.3.22 (page « Mes groupes » côté profil) — sur `main` (tip `7abd237`). 4 axes couverts (communes, fédérations indirect via commune, confédérations indirect via fédération, GT thématiques direct). Voir `docs/manifests/v2-3-22-mes-groupes.md`.
**Dernier chantier terminé (antérieur)** : V2.3.21 (bouton « Signaler un litige » côté propriétaire, transition D8 `acceptee → litige`) — symétrie avec V2.3.16. Voir `docs/manifests/v2-3-21-signaler-litige-proprietaire.md`.
**Dernier chantier terminé (antérieur)** : V2.3.20 (identités caisse + tests helper) — branche `nomAffichageRespectantVisibilite` dans la page de détail caisse + 7 tests Vitest sur le helper pur (413 tests verts maintenant). Voir `docs/manifests/v2-3-20-identites-caisse-tests-identite.md`.
**Dernier chantier terminé (antérieur)** : V2.3.19 (helper `nomAffichageRespectantVisibilite`) — module `lib/reseau/identite.ts` réutilisant la RPC V1 `personne_affichage(cible)` SECURITY DEFINER. Branché dans `HistoriqueTransitions`, `/profil/demandes-reservations`, `/profil/reservations`, `/admin/moderation/reservations`. Voir `docs/manifests/v2-3-19-nom-affichage-visibilite.md`.
**Dernier chantier terminé (antérieur)** : V2.3.18 (page de détail caisse `/admin/national/tresorerie/[caisseId]`) — drill-down lecture seule avec 3 sections (entête + réceptacles + transactions sortantes). Voir `docs/manifests/v2-3-18-detail-caisse.md`.
**Dernier chantier terminé (antérieur)** : V2.3.17 (console admin résolution de litige, `/admin/moderation/reservations`) — arbitrage `litige → confirmee | annulee` avec motif obligatoire. Privilège admin documenté qui contourne `transitionAutorisee`. Voir `docs/manifests/v2-3-17-moderation-litige-reservation.md`.
**Dernier chantier terminé (antérieur)** : V2.3.16 (bouton « Signaler un litige » côté demandeur, transition D8 `realisee → litige`) — sur `main` (tip `a2c0552`). Avec ça le cycle D8 est navigable de bout en bout côté UI : créer → accepter/refuser → marquer réalisée → confirmer / litige / annuler, le tout journalisé via `reservation_journal` (V2.3.15). Voir `docs/manifests/v2-3-16-signaler-litige-reservation.md`.
**Dernier chantier terminé (antérieur)** : V2.3.15 (journal des transitions D8 sur réservation, doctrine D8bis) — migration locale `20260527090000_reservation_journal.sql` (10ᵉ en attente), helpers TS, affichage `<details>` Historique côté demandeur ET propriétaire. Voir `docs/manifests/v2-3-15-journal-transitions-reservation.md`.
**Dernier chantier terminé (antérieur)** : V2.3.14 (bouton « Confirmer » côté demandeur, transition `realisee → confirmee`) — corrige aussi la dette V2.3.11 (BoutonAnnuler jamais branché dans la page malgré son manifest). Voir `docs/manifests/v2-3-14-confirmation-reservation.md`.
**Dernier chantier terminé (antérieur)** : V2.3.13 (dashboard propriétaire d'offre, `/profil/demandes-reservations`, Server Actions accepter/refuser/marquer réalisée) — FK polymorphe gérée applicativement (6 requêtes max, batch via `Map`). Voir `docs/manifests/v2-3-13-dashboard-proprietaire-offre.md`.
**Dernier chantier terminé (antérieur)** : V2.2.2 (Réservation, composant transversal D8) — sur `feature/v2-2-2-reservation`. Migration `20260527040000_reservation.sql` (table `reservation` : FK polymorphe `(offre_type, offre_id)` avec CHECK liste fermée, `demandeur_personne_id` FK `personne`, créneau `debut`/`fin` nullable avec CHECK cohérence, `quantite` default 1, `message_amorce` 1-2000 chars, `statut` machine à états D8 (7 valeurs : `proposee`/`acceptee`/`refusee`/`realisee`/`confirmee`/`annulee`/`litige`), `motif_decision` nullable, `transaction_id` nullable, 3 index actifs, trigger `updated_at`, 4 policies RLS). Helpers TS : `lib/reservation-amorce.ts` (pur, `genererMessageAmorce` bornée à 2000 chars, formatage `Intl.DateTimeFormat` français) + `lib/reservation.ts` (CRUD + helper pur `transitionAutorisee` pour la machine à états). `types/database.ts` enrichi à la main. 9 tests verts (`tests/unit/reservation/amorce-transitions.test.ts`). Migration NON appliquée distant cette nuit (à `supabase db push` au matin). Pas d'intégration applicative sous-espace V2.2.2 : reportée en VAGUE 3 (chaque sous-espace pose sa Server Action de réservation au cas par cas). Voir `docs/manifests/v2-2-2-reservation.md`.
**Dernier chantier terminé (antérieur)** : V2.2.1 (FilDeGroupe §18) — sur `feature/v2-2-1-fil-groupe`, mergé dans `main` (tip `7106fd0`). Composant transversal réutilisable + helper SQL `est_membre_espace`. Voir `docs/manifests/v2-2-1-fil-groupe.md`.
**Dernier chantier terminé (antérieur)** : 7.5 (réseau social) — sur `feature/phase-13-integration`. Le réseau social n'est plus un stub : migration 039 (`relation_reseau`, `post_reseau`, `commentaire_reseau`, `reaction_reseau`, `message_reseau` + RLS + helpers de visibilité), flux hiérarchisé transparent + publications/commentaires/soutiens (`/s-informer/reseau`), profil par numéro M+7 (`/s-informer/reseau/[numero]`), messagerie interne (`/s-informer/reseau/messages` + modal réutilisable), modération a posteriori (`/admin/moderation/reseau`). **Décision A** intégrée : sur la page commune, liste des co-membres (nom + prénom complets, respect visibilité) visible uniquement entre membres, nom cliquable vers le profil réseau + bouton message. 300 tests verts (voir `docs/manifests/phase-13-chantier-7.5-reseau-social.md`). Migration 039 appliquée sur le distant le 2026-05-25 (réseau social actif). **Tout fusionné dans `main` le 2026-05-25** (fast-forward depuis `feature/phase-13-integration`, tip `a778861`) : `main` contient désormais l'ensemble phases 0 à 13 + réseau social. Voir aussi `docs/ETAT-DES-LIEUX.md` (synthèse exhaustive pour le cahier des charges V2).
**Dernier chantier terminé (antérieur)** : 13.3-E (profil unifié) — sur `feature/phase-13-integration`. Chaque signataire (y compris les importés sans compte) a une identité durable : table `profil_unifie` + numéro public « M » + 7 lettres (ex. `MABCDEFG`), stable au-delà de l'email (migration 038). Génération côté base (trigger + format `^M[A-Z]{7}$` + anti-collision + anti gros mots). Rattachement compte ↔ signatures à la vérification de l'email (`rattacher_profil_unifie`, callback auth) ; flux de signature et import branchés (`trouver_ou_creer_profil_unifie`, service_role) ; numéro affiché sur `/profil/informations`. Révise la réconciliation du 2026-05-24. 290 tests verts, lint + typecheck verts (voir `docs/manifests/phase-13-chantier-13.3-e-profil-unifie.md`). Avant (mêmes branche) : 13.3-C/D (carte clusterisée `/communes`, fiche `/communes/[code_insee]`, « Mes contributions »), + révision doctrine §7B (coquilles vides pour tout le référentiel : script `precreer-communes`, garde anti-doublon de nom). Intégration 13.1/13.2/13.3 : voir `docs/manifests/phase-13-integration.md`.
**Dernier chantier terminé (antérieur)** : 12 — Polish global post-revue (chantiers 12.1 à 12.6), voir `docs/manifests/phase-12-polish-revue-globale.md`.
**État du projet** : V1 livrée et fonctionnelle (phases 0-13). Cycle V2 ouvert le 26/05/2026. **VAGUE 0/1/2/3 fermées 27/05**. **VAGUE 3 finitions étendues nuit 27/05** : 34 chantiers V2.3.13→V2.3.46 livrés sur ce cycle. Cycle D8 réservation complet et symétrique (V2.3.13-21), journal D8bis + audit (V2.3.15, V2.3.43), helper identité affichable (V2.3.19), Mes groupes 6 axes + page GT individuelle + index (V2.3.22, V2.3.29, V2.3.38, V2.3.39), filtres dashboards 2 axes (V2.3.23-24, V2.3.40), compteur membres (V2.3.41), notifications cloche V2 (V2.3.25, V2.3.30, V2.3.37), trésorerie complète bout-en-bout avec entrées/sorties/solde/justificatif/initier/confirmer (V2.3.26-28, V2.3.32-36), Mes contributions financières (V2.3.31), Rejoindre/Quitter campagne + GT (V2.3.34, V2.3.38), stats admin V2 (V2.3.42), recherche réseau (V2.3.44-45), tests filtres + compter-membres (V2.3.46). **427 tests verts** (+14 sur V2.3.46, +7 sur V2.3.20). **12 migrations posées localement** à appliquer au matin via `supabase db push` (V2.1.1/2/3 + V2.2.1/2/3 + V2.3.2/3/8 + V2.3.15/26/29) puis 3 backfills (`scripts/backfill-consentement.ts`, `backfill-droits.ts`, `backfill-caisses.ts`, tous `--dry-run` puis `--confirm`). **Fix Ultraplan CI Playwright** poussé sur branche `fix/ci-playwright-multi-browser` (PR à créer manuellement). Chantiers 7.3/7.6 toujours en stubs honnêtes. Chantier 2.2 (8 pages éditoriales) bloqué tant que les textes ne sont pas fournis. **Chantier 10.1 (migration Base44)** : signatures déjà importées (17 746), reste membres + newsletter à finaliser (cf. doc). **Reste à faire par Lilou/Ben** : voir `docs/A-FAIRE-LILOU-BEN.md` (doc finale exhaustive pour Claude.ai).
**Chantiers bloqués / en attente d'arbitrage** : 2.2 demande à Lilou/Ben de rédiger les 8 textes éditoriaux listés dans `docs/CONTENUS-A-ARBITRER.md`. Préalables Supabase : `supabase db push` les migrations 1.1 + 012-033 + Brevo SMTP. Préalable Stripe : `npm install stripe` + clés `sk_test_...`. Préalable SEL prod : poser un cron Cloudflare Worker pour `crediterPrestationsEnAttente` toutes les heures. Préalable Marché prod : poser un cron qui expire les annonces inactives 3 mois (chantier 11.3). Préalable Adhésion prod : poser un cron quotidien qui appelle `envoyerRelancesAdhesion(14)` (chantier 11.3). Préalable Communes : Lilou/Ben fournit le CSV des 2100-2300 communes puis lancer `npx tsx scripts/import-communes.ts <fichier.csv> --dry-run` puis `--confirm`. Préalable Moments prod : poser un cron horaire pour la transition annonce→en_cours→termine (chantier 11.3). Préalable Playwright multi-viewports : `npx playwright install` pour télécharger Firefox + WebKit en local. Préalable phase 13 (sur `feature/phase-13-integration`) : pour tester l'édition des dates de pétition et la plateforme de données contre la base distante, appliquer les migrations 13.x (dates pétition 035, `commune_reference`, CP/INSEE) via `supabase db push` ou `scripts/appliquer-sql-distant.ts` (DDL pur, sans PII) ; les scripts d'import signataires/communes restent à lancer en `--confirm` après feu vert. Distant 13.3-C/D/E APPLIQUÉ le 2026-05-25 (autorisé par Lilou/Ben) : migrations 037 (`compteurs_commune`) et 038 (`profil_unifie`) appliquées ; `precreer-communes --confirm` lancé (35 011 coquilles `pre_creee`) ; remplissage profils unifiés vérifié (15 737 profils, 17 746/17 746 signatures reliées) ; import signataires déjà complet (17 746). `types/database.ts` maintenu à la main (régénération CLI optionnelle, non nécessaire). Reste éventuel : réclamation d'un email antérieur (hors v1).

### Branche principale

`main` (renommée depuis `master` pendant le chantier 0.2). Toutes les branches feature partent de `main`.

### Préalables externes attendus

- **Avant chantier 1.1** : création du projet Supabase (région Francfort), récupération des trois clés (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`).
- **Au moment voulu** : création du repo GitHub distant et `git push -u origin main` pour activer la CI.
- **Quand accessible** : compte Cloudflare Pages pour le déploiement (cf. CLAUDE.md §6).
- **Au chantier qui les branche** : clés Brevo, Stripe, LiveKit, Turnstile, T99CP (le site tourne en local avec mocks d'ici là).

### Décisions politiques en attente d'arbitrage de Lilou/Ben

- Q5 : services proposés selon statut (adhérent·e / sympathisant·e / donateur·ice).
- Q13 : boucle d'engagement, mécaniques transverses.
- Q14 : indicateurs publics du mouvement.

### Données externes attendues de Lilou/Ben

- CSV de cartographie des 2100-2300 communes pré-créées (avant chantier 5.2).
- Contenus éditoriaux pour les pages : Doctrine, Commune libre, Assemblée Confédérale, Monnaie 99-coin, FAQ, Ressources, À propos. À arbitrer au fur et à mesure via les placeholders.
- Coordonnées de l'association (adresse, RNA, email contact, email DPD) pour finaliser la politique de confidentialité.
- Choix collégial du·de la DPD bénévole.

**Index unique des contenus en attente** : `docs/CONTENUS-A-ARBITRER.md`. C'est la liste à parcourir avec Lilou/Ben pour passer du squelette technique à une publication réelle.

---

## 12. Liens vers les specs

Tout est dans `docs/specs/` :

| Fichier | Quand le consulter |
|---|---|
| `00_INDEX.md` | Premier accès au pack |
| `01_ARCHITECTURE.md` | Avant chaque chantier (référence centrale) |
| `02_STACK.md` | Pour toute décision technique |
| `03_VOCABULAIRE.md` | À chaque création d'UI ou de copy |
| `04_DESIGN-TOKENS.md` | Avant chaque chantier UI |
| `05_RGPD.md` | Avant chaque chantier qui touche aux données personnelles |
| `06_DOCTRINES.md` | À chaque décision d'UX ou de produit |
| `08_PLAN_CHANTIERS.md` | Pour savoir quel chantier vient ensuite |
| `arborescence.mermaid` | Pour visualiser la structure du site |
